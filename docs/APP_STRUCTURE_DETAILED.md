# APP_STRUCTURE_DETAILED

Detailed bilingual (EN/RU) file guide for the Life OS / Counter repository.

**Generated from tree scan at SHA `b4206b4` (2026-07-03).**

Concise canonical map: [`APP_STRUCTURE.md`](APP_STRUCTURE.md).

---

## Root project files

### `.cursorrules`

EN:

- **Purpose:** Project file.
- **Contains:** Supporting source or config. Symbols: (library).
- **Does:** See path prefix for ownership.
- **Connected to:** Cross-check `docs/APP_STRUCTURE.md` layer rules.

RU:

- **Назначение:** Файл проекта.
- **Что внутри:** См. англ. блок Purpose/Contains.
- **Что делает:** Смотри префикс пути для владения.
- **Как связано с общей логикой:** Правила слоёв — `docs/APP_STRUCTURE.md`.


### `.gitignore`

EN:

- **Purpose:** Project file.
- **Contains:** Supporting source or config. Symbols: (library).
- **Does:** See path prefix for ownership.
- **Connected to:** Cross-check `docs/APP_STRUCTURE.md` layer rules.

RU:

- **Назначение:** Файл проекта.
- **Что внутри:** См. англ. блок Purpose/Contains.
- **Что делает:** Смотри префикс пути для владения.
- **Как связано с общей логикой:** Правила слоёв — `docs/APP_STRUCTURE.md`.


### `.metadata`

EN:

- **Purpose:** Project file.
- **Contains:** Supporting source or config. Symbols: (library).
- **Does:** See path prefix for ownership.
- **Connected to:** Cross-check `docs/APP_STRUCTURE.md` layer rules.

RU:

- **Назначение:** Файл проекта.
- **Что внутри:** См. англ. блок Purpose/Contains.
- **Что делает:** Смотри префикс пути для владения.
- **Как связано с общей логикой:** Правила слоёв — `docs/APP_STRUCTURE.md`.


### `AGENTS.md`

EN:

- **Purpose:** Root project manifest or meta doc.
- **Contains:** Build/analyzer/deploy configuration or changelog. Symbols: (library).
- **Does:** Defines dependencies, lints, deploy entrypoints.
- **Connected to:** First stop for tooling and AI context.

RU:

- **Назначение:** Файл проекта.
- **Что внутри:** См. англ. блок Purpose/Contains.
- **Что делает:** Смотри префикс пути для владения.
- **Как связано с общей логикой:** Правила слоёв — `docs/APP_STRUCTURE.md`.


### `CHANGELOG.md`

EN:

- **Purpose:** Root project manifest or meta doc.
- **Contains:** Build/analyzer/deploy configuration or changelog. Symbols: (library).
- **Does:** Defines dependencies, lints, deploy entrypoints.
- **Connected to:** First stop for tooling and AI context.

RU:

- **Назначение:** Файл проекта.
- **Что внутри:** См. англ. блок Purpose/Contains.
- **Что делает:** Смотри префикс пути для владения.
- **Как связано с общей логикой:** Правила слоёв — `docs/APP_STRUCTURE.md`.


### `CLAUDE.md`

EN:

- **Purpose:** Root project manifest or meta doc.
- **Contains:** Build/analyzer/deploy configuration or changelog. Symbols: (library).
- **Does:** Defines dependencies, lints, deploy entrypoints.
- **Connected to:** First stop for tooling and AI context.

RU:

- **Назначение:** Файл проекта.
- **Что внутри:** См. англ. блок Purpose/Contains.
- **Что делает:** Смотри префикс пути для владения.
- **Как связано с общей логикой:** Правила слоёв — `docs/APP_STRUCTURE.md`.


### `README.md`

EN:

- **Purpose:** Root project manifest or meta doc.
- **Contains:** Build/analyzer/deploy configuration or changelog. Symbols: (library).
- **Does:** Defines dependencies, lints, deploy entrypoints.
- **Connected to:** First stop for tooling and AI context.

RU:

- **Назначение:** Файл проекта.
- **Что внутри:** См. англ. блок Purpose/Contains.
- **Что делает:** Смотри префикс пути для владения.
- **Как связано с общей логикой:** Правила слоёв — `docs/APP_STRUCTURE.md`.


### `analysis_options.yaml`

EN:

- **Purpose:** Root project manifest or meta doc.
- **Contains:** Build/analyzer/deploy configuration or changelog. Symbols: (library).
- **Does:** Defines dependencies, lints, deploy entrypoints.
- **Connected to:** First stop for tooling and AI context.

RU:

- **Назначение:** Файл проекта.
- **Что внутри:** См. англ. блок Purpose/Contains.
- **Что делает:** Смотри префикс пути для владения.
- **Как связано с общей логикой:** Правила слоёв — `docs/APP_STRUCTURE.md`.


### `android.ps1`

EN:

- **Purpose:** Project file.
- **Contains:** Supporting source or config. Symbols: (library).
- **Does:** See path prefix for ownership.
- **Connected to:** Cross-check `docs/APP_STRUCTURE.md` layer rules.

RU:

- **Назначение:** Файл проекта.
- **Что внутри:** См. англ. блок Purpose/Contains.
- **Что делает:** Смотри префикс пути для владения.
- **Как связано с общей логикой:** Правила слоёв — `docs/APP_STRUCTURE.md`.


### `pubspec.lock`

EN:

- **Purpose:** Project file.
- **Contains:** Supporting source or config. Symbols: (library).
- **Does:** See path prefix for ownership.
- **Connected to:** Cross-check `docs/APP_STRUCTURE.md` layer rules.

RU:

- **Назначение:** Файл проекта.
- **Что внутри:** См. англ. блок Purpose/Contains.
- **Что делает:** Смотри префикс пути для владения.
- **Как связано с общей логикой:** Правила слоёв — `docs/APP_STRUCTURE.md`.


### `pubspec.yaml`

EN:

- **Purpose:** Root project manifest or meta doc.
- **Contains:** Build/analyzer/deploy configuration or changelog. Symbols: (library).
- **Does:** Defines dependencies, lints, deploy entrypoints.
- **Connected to:** First stop for tooling and AI context.

RU:

- **Назначение:** Файл проекта.
- **Что внутри:** См. англ. блок Purpose/Contains.
- **Что делает:** Смотри префикс пути для владения.
- **Как связано с общей логикой:** Правила слоёв — `docs/APP_STRUCTURE.md`.


### `update.ps1`

EN:

- **Purpose:** Root project manifest or meta doc.
- **Contains:** Build/analyzer/deploy configuration or changelog. Symbols: (library).
- **Does:** Defines dependencies, lints, deploy entrypoints.
- **Connected to:** First stop for tooling and AI context.

RU:

- **Назначение:** Файл проекта.
- **Что внутри:** См. англ. блок Purpose/Contains.
- **Что делает:** Смотри префикс пути для владения.
- **Как связано с общей логикой:** Правила слоёв — `docs/APP_STRUCTURE.md`.


## lib/ — application Dart

### `lib/app_shell.dart`

EN:

- **Purpose:** Project file.
- **Contains:** Supporting source or config. Symbols: (library).
- **Does:** See path prefix for ownership.
- **Connected to:** Cross-check `docs/APP_STRUCTURE.md` layer rules.

RU:

- **Назначение:** Файл проекта.
- **Что внутри:** См. англ. блок Purpose/Contains.
- **Что делает:** Смотри префикс пути для владения.
- **Как связано с общей логикой:** Правила слоёв — `docs/APP_STRUCTURE.md`.


### `lib/core/app_build_info.dart`

EN:

- **Purpose:** Core foundation utility or widget.
- **Contains:** Theme, time, diagnostics, navigation helpers. Symbols: (library).
- **Does:** Shared non-feature code; must not import `features/` or Brain HTTP.
- **Connected to:** Used across UI modules and sometimes Brain (time helpers).

RU:

- **Назначение:** Базовый слой (core).
- **Что внутри:** Тема, виджеты DS, time/voice утилиты.
- **Что делает:** Не импортирует `features/` и Brain HTTP.
- **Как связано с общей логикой:** Переиспользуется UI и частично Brain.


### `lib/core/app_colors.dart`

EN:

- **Purpose:** Core foundation utility or widget.
- **Contains:** Theme, time, diagnostics, navigation helpers. Symbols: (library).
- **Does:** Shared non-feature code; must not import `features/` or Brain HTTP.
- **Connected to:** Used across UI modules and sometimes Brain (time helpers).

RU:

- **Назначение:** Базовый слой (core).
- **Что внутри:** Тема, виджеты DS, time/voice утилиты.
- **Что делает:** Не импортирует `features/` и Brain HTTP.
- **Как связано с общей логикой:** Переиспользуется UI и частично Brain.


### `lib/core/app_icons.dart`

EN:

- **Purpose:** Core foundation utility or widget.
- **Contains:** Theme, time, diagnostics, navigation helpers. Symbols: AppTimezoneIconKey.
- **Symbols:** `AppTimezoneIconKey`
- **Does:** Shared non-feature code; must not import `features/` or Brain HTTP.
- **Connected to:** Used across UI modules and sometimes Brain (time helpers).

RU:

- **Назначение:** Базовый слой (core).
- **Что внутри:** Тема, виджеты DS, time/voice утилиты.
- **Что делает:** Не импортирует `features/` и Brain HTTP.
- **Как связано с общей логикой:** Переиспользуется UI и частично Brain.


### `lib/core/app_snackbar.dart`

EN:

- **Purpose:** Core foundation utility or widget.
- **Contains:** Theme, time, diagnostics, navigation helpers. Symbols: AppSnack.
- **Symbols:** `AppSnack`
- **Does:** Shared non-feature code; must not import `features/` or Brain HTTP.
- **Connected to:** Used across UI modules and sometimes Brain (time helpers).

RU:

- **Назначение:** Базовый слой (core).
- **Что внутри:** Тема, виджеты DS, time/voice утилиты.
- **Что делает:** Не импортирует `features/` и Brain HTTP.
- **Как связано с общей логикой:** Переиспользуется UI и частично Brain.


### `lib/core/category_color_palette.dart`

EN:

- **Purpose:** Core foundation utility or widget.
- **Contains:** Theme, time, diagnostics, navigation helpers. Symbols: (library).
- **Does:** Shared non-feature code; must not import `features/` or Brain HTTP.
- **Connected to:** Used across UI modules and sometimes Brain (time helpers).

RU:

- **Назначение:** Базовый слой (core).
- **Что внутри:** Тема, виджеты DS, time/voice утилиты.
- **Что делает:** Не импортирует `features/` и Brain HTTP.
- **Как связано с общей логикой:** Переиспользуется UI и частично Brain.


### `lib/core/constants.dart`

EN:

- **Purpose:** Core foundation utility or widget.
- **Contains:** Theme, time, diagnostics, navigation helpers. Symbols: (library).
- **Does:** Shared non-feature code; must not import `features/` or Brain HTTP.
- **Connected to:** Used across UI modules and sometimes Brain (time helpers).

RU:

- **Назначение:** Базовый слой (core).
- **Что внутри:** Тема, виджеты DS, time/voice утилиты.
- **Что делает:** Не импортирует `features/` и Brain HTTP.
- **Как связано с общей логикой:** Переиспользуется UI и частично Brain.


### `lib/core/date_pager_settle_gate.dart`

EN:

- **Purpose:** Core foundation utility or widget.
- **Contains:** Theme, time, diagnostics, navigation helpers. Symbols: (library).
- **Does:** Shared non-feature code; must not import `features/` or Brain HTTP.
- **Connected to:** Used across UI modules and sometimes Brain (time helpers).

RU:

- **Назначение:** Базовый слой (core).
- **Что внутри:** Тема, виджеты DS, time/voice утилиты.
- **Что делает:** Не импортирует `features/` и Brain HTTP.
- **Как связано с общей логикой:** Переиспользуется UI и частично Brain.


### `lib/core/date_swipe_physics.dart`

EN:

- **Purpose:** Core foundation utility or widget.
- **Contains:** Theme, time, diagnostics, navigation helpers. Symbols: LightDateSwipePhysics, FeatherDateSwipePhysics.
- **Symbols:** `LightDateSwipePhysics, FeatherDateSwipePhysics`
- **Does:** Shared non-feature code; must not import `features/` or Brain HTTP.
- **Connected to:** Used across UI modules and sometimes Brain (time helpers).

RU:

- **Назначение:** Базовый слой (core).
- **Что внутри:** Тема, виджеты DS, time/voice утилиты.
- **Что делает:** Не импортирует `features/` и Brain HTTP.
- **Как связано с общей логикой:** Переиспользуется UI и частично Brain.


### `lib/core/diagnostics/desktop_voice_log.dart`

EN:

- **Purpose:** Core foundation utility or widget.
- **Contains:** Theme, time, diagnostics, navigation helpers. Symbols: DesktopVoiceLog.
- **Symbols:** `DesktopVoiceLog`
- **Does:** Shared non-feature code; must not import `features/` or Brain HTTP.
- **Connected to:** Used across UI modules and sometimes Brain (time helpers).

RU:

- **Назначение:** Базовый слой (core).
- **Что внутри:** Тема, виджеты DS, time/voice утилиты.
- **Что делает:** Не импортирует `features/` и Brain HTTP.
- **Как связано с общей логикой:** Переиспользуется UI и частично Brain.


### `lib/core/diagnostics/desktop_voice_pipeline.dart`

EN:

- **Purpose:** Core foundation utility or widget.
- **Contains:** Theme, time, diagnostics, navigation helpers. Symbols: (library).
- **Does:** Shared non-feature code; must not import `features/` or Brain HTTP.
- **Connected to:** Used across UI modules and sometimes Brain (time helpers).

RU:

- **Назначение:** Базовый слой (core).
- **Что внутри:** Тема, виджеты DS, time/voice утилиты.
- **Что делает:** Не импортирует `features/` и Brain HTTP.
- **Как связано с общей логикой:** Переиспользуется UI и частично Brain.


### `lib/core/diagnostics/plan_duplicate_log.dart`

EN:

- **Purpose:** Core foundation utility or widget.
- **Contains:** Theme, time, diagnostics, navigation helpers. Symbols: (library).
- **Does:** Shared non-feature code; must not import `features/` or Brain HTTP.
- **Connected to:** Used across UI modules and sometimes Brain (time helpers).

RU:

- **Назначение:** Базовый слой (core).
- **Что внутри:** Тема, виджеты DS, time/voice утилиты.
- **Что делает:** Не импортирует `features/` и Brain HTTP.
- **Как связано с общей логикой:** Переиспользуется UI и частично Brain.


### `lib/core/diagnostics/platform_log.dart`

EN:

- **Purpose:** Core foundation utility or widget.
- **Contains:** Theme, time, diagnostics, navigation helpers. Symbols: (library).
- **Does:** Shared non-feature code; must not import `features/` or Brain HTTP.
- **Connected to:** Used across UI modules and sometimes Brain (time helpers).

RU:

- **Назначение:** Базовый слой (core).
- **Что внутри:** Тема, виджеты DS, time/voice утилиты.
- **Что делает:** Не импортирует `features/` и Brain HTTP.
- **Как связано с общей логикой:** Переиспользуется UI и частично Brain.


### `lib/core/diagnostics/runtime_log.dart`

EN:

- **Purpose:** Core foundation utility or widget.
- **Contains:** Theme, time, diagnostics, navigation helpers. Symbols: (library).
- **Does:** Shared non-feature code; must not import `features/` or Brain HTTP.
- **Connected to:** Used across UI modules and sometimes Brain (time helpers).

RU:

- **Назначение:** Базовый слой (core).
- **Что внутри:** Тема, виджеты DS, time/voice утилиты.
- **Что делает:** Не импортирует `features/` и Brain HTTP.
- **Как связано с общей логикой:** Переиспользуется UI и частично Brain.


### `lib/core/diagnostics/startup_log.dart`

EN:

- **Purpose:** Core foundation utility or widget.
- **Contains:** Theme, time, diagnostics, navigation helpers. Symbols: (library).
- **Does:** Shared non-feature code; must not import `features/` or Brain HTTP.
- **Connected to:** Used across UI modules and sometimes Brain (time helpers).

RU:

- **Назначение:** Базовый слой (core).
- **Что внутри:** Тема, виджеты DS, time/voice утилиты.
- **Что делает:** Не импортирует `features/` и Brain HTTP.
- **Как связано с общей логикой:** Переиспользуется UI и частично Brain.


### `lib/core/link_scalar.dart`

EN:

- **Purpose:** Core foundation utility or widget.
- **Contains:** Theme, time, diagnostics, navigation helpers. Symbols: (library).
- **Does:** Shared non-feature code; must not import `features/` or Brain HTTP.
- **Connected to:** Used across UI modules and sometimes Brain (time helpers).

RU:

- **Назначение:** Базовый слой (core).
- **Что внутри:** Тема, виджеты DS, time/voice утилиты.
- **Что делает:** Не импортирует `features/` и Brain HTTP.
- **Как связано с общей логикой:** Переиспользуется UI и частично Brain.


### `lib/core/navigation/app_navigator.dart`

EN:

- **Purpose:** Core foundation utility or widget.
- **Contains:** Theme, time, diagnostics, navigation helpers. Symbols: (library).
- **Does:** Shared non-feature code; must not import `features/` or Brain HTTP.
- **Connected to:** Used across UI modules and sometimes Brain (time helpers).

RU:

- **Назначение:** Базовый слой (core).
- **Что внутри:** Тема, виджеты DS, time/voice утилиты.
- **Что делает:** Не импортирует `features/` и Brain HTTP.
- **Как связано с общей логикой:** Переиспользуется UI и частично Brain.


### `lib/core/navigation/shell_side_navigation.dart`

EN:

- **Purpose:** Core foundation utility or widget.
- **Contains:** Theme, time, diagnostics, navigation helpers. Symbols: (library).
- **Does:** Shared non-feature code; must not import `features/` or Brain HTTP.
- **Connected to:** Used across UI modules and sometimes Brain (time helpers).

RU:

- **Назначение:** Базовый слой (core).
- **Что внутри:** Тема, виджеты DS, time/voice утилиты.
- **Что делает:** Не импортирует `features/` и Brain HTTP.
- **Как связано с общей логикой:** Переиспользуется UI и частично Brain.


### `lib/core/performance/rebuild_metrics.dart`

EN:

- **Purpose:** Core foundation utility or widget.
- **Contains:** Theme, time, diagnostics, navigation helpers. Symbols: RebuildMetrics.
- **Symbols:** `RebuildMetrics`
- **Does:** Shared non-feature code; must not import `features/` or Brain HTTP.
- **Connected to:** Used across UI modules and sometimes Brain (time helpers).

RU:

- **Назначение:** Базовый слой (core).
- **Что внутри:** Тема, виджеты DS, time/voice утилиты.
- **Что делает:** Не импортирует `features/` и Brain HTTP.
- **Как связано с общей логикой:** Переиспользуется UI и частично Brain.


### `lib/core/performance/runtime_flags.dart`

EN:

- **Purpose:** Core foundation utility or widget.
- **Contains:** Theme, time, diagnostics, navigation helpers. Symbols: (library).
- **Does:** Shared non-feature code; must not import `features/` or Brain HTTP.
- **Connected to:** Used across UI modules and sometimes Brain (time helpers).

RU:

- **Назначение:** Базовый слой (core).
- **Что внутри:** Тема, виджеты DS, time/voice утилиты.
- **Что делает:** Не импортирует `features/` и Brain HTTP.
- **Как связано с общей логикой:** Переиспользуется UI и частично Brain.


### `lib/core/performance/shell_flags.dart`

EN:

- **Purpose:** Core foundation utility or widget.
- **Contains:** Theme, time, diagnostics, navigation helpers. Symbols: (library).
- **Does:** Shared non-feature code; must not import `features/` or Brain HTTP.
- **Connected to:** Used across UI modules and sometimes Brain (time helpers).

RU:

- **Назначение:** Базовый слой (core).
- **Что внутри:** Тема, виджеты DS, time/voice утилиты.
- **Что делает:** Не импортирует `features/` и Brain HTTP.
- **Как связано с общей логикой:** Переиспользуется UI и частично Brain.


### `lib/core/picker_entry_modes.dart`

EN:

- **Purpose:** Core foundation utility or widget.
- **Contains:** Theme, time, diagnostics, navigation helpers. Symbols: (library).
- **Does:** Shared non-feature code; must not import `features/` or Brain HTTP.
- **Connected to:** Used across UI modules and sometimes Brain (time helpers).

RU:

- **Назначение:** Базовый слой (core).
- **Что внутри:** Тема, виджеты DS, time/voice утилиты.
- **Что делает:** Не импортирует `features/` и Brain HTTP.
- **Как связано с общей логикой:** Переиспользуется UI и частично Brain.


### `lib/core/plan_category_lookup.dart`

EN:

- **Purpose:** Core foundation utility or widget.
- **Contains:** Theme, time, diagnostics, navigation helpers. Symbols: PlanCategoryPresentation.
- **Symbols:** `PlanCategoryPresentation`
- **Does:** Shared non-feature code; must not import `features/` or Brain HTTP.
- **Connected to:** Used across UI modules and sometimes Brain (time helpers).

RU:

- **Назначение:** Базовый слой (core).
- **Что внутри:** Тема, виджеты DS, time/voice утилиты.
- **Что делает:** Не импортирует `features/` и Brain HTTP.
- **Как связано с общей логикой:** Переиспользуется UI и частично Brain.


### `lib/core/services/desktop_hotkey_codec.dart`

EN:

- **Purpose:** Core platform/voice service (no feature imports).
- **Contains:** Desktop voice/STT/tray/hotkey pipeline. Symbols: (library).
- **Does:** Bridges OS capabilities; record submit calls Brain via injected hooks.
- **Connected to:** Wired from `main.dart` / shell voice routing.

RU:

- **Назначение:** Базовый слой (core).
- **Что внутри:** Тема, виджеты DS, time/voice утилиты.
- **Что делает:** Не импортирует `features/` и Brain HTTP.
- **Как связано с общей логикой:** Переиспользуется UI и частично Brain.


### `lib/core/services/desktop_stt_diagnostics.dart`

EN:

- **Purpose:** Core platform/voice service (no feature imports).
- **Contains:** Desktop voice/STT/tray/hotkey pipeline. Symbols: DesktopSttDiagnostics.
- **Symbols:** `DesktopSttDiagnostics`
- **Does:** Bridges OS capabilities; record submit calls Brain via injected hooks.
- **Connected to:** Wired from `main.dart` / shell voice routing.

RU:

- **Назначение:** Базовый слой (core).
- **Что внутри:** Тема, виджеты DS, time/voice утилиты.
- **Что делает:** Не импортирует `features/` и Brain HTTP.
- **Как связано с общей логикой:** Переиспользуется UI и частично Brain.


### `lib/core/services/desktop_stt_helper_service.dart`

EN:

- **Purpose:** Core platform/voice service (no feature imports).
- **Contains:** Desktop voice/STT/tray/hotkey pipeline. Symbols: DesktopSttHelperService.
- **Symbols:** `DesktopSttHelperService`
- **Does:** Bridges OS capabilities; record submit calls Brain via injected hooks.
- **Connected to:** Wired from `main.dart` / shell voice routing.

RU:

- **Назначение:** Базовый слой (core).
- **Что внутри:** Тема, виджеты DS, time/voice утилиты.
- **Что делает:** Не импортирует `features/` и Brain HTTP.
- **Как связано с общей логикой:** Переиспользуется UI и частично Brain.


### `lib/core/services/desktop_tray_service.dart`

EN:

- **Purpose:** Core platform/voice service (no feature imports).
- **Contains:** Desktop voice/STT/tray/hotkey pipeline. Symbols: (library).
- **Does:** Bridges OS capabilities; record submit calls Brain via injected hooks.
- **Connected to:** Wired from `main.dart` / shell voice routing.

RU:

- **Назначение:** Базовый слой (core).
- **Что внутри:** Тема, виджеты DS, time/voice утилиты.
- **Что делает:** Не импортирует `features/` и Brain HTTP.
- **Как связано с общей логикой:** Переиспользуется UI и частично Brain.


### `lib/core/services/desktop_tray_service_io.dart`

EN:

- **Purpose:** Core platform/voice service (no feature imports).
- **Contains:** Desktop voice/STT/tray/hotkey pipeline. Symbols: _DesktopTrayListener.
- **Symbols:** `_DesktopTrayListener`
- **Does:** Bridges OS capabilities; record submit calls Brain via injected hooks.
- **Connected to:** Wired from `main.dart` / shell voice routing.

RU:

- **Назначение:** Базовый слой (core).
- **Что внутри:** Тема, виджеты DS, time/voice утилиты.
- **Что делает:** Не импортирует `features/` и Brain HTTP.
- **Как связано с общей логикой:** Переиспользуется UI и частично Brain.


### `lib/core/services/desktop_tray_service_stub.dart`

EN:

- **Purpose:** Core platform/voice service (no feature imports).
- **Contains:** Desktop voice/STT/tray/hotkey pipeline. Symbols: (library).
- **Does:** Bridges OS capabilities; record submit calls Brain via injected hooks.
- **Connected to:** Wired from `main.dart` / shell voice routing.

RU:

- **Назначение:** Базовый слой (core).
- **Что внутри:** Тема, виджеты DS, time/voice утилиты.
- **Что делает:** Не импортирует `features/` и Brain HTTP.
- **Как связано с общей логикой:** Переиспользуется UI и частично Brain.


### `lib/core/services/desktop_voice_acceptance_bridge.dart`

EN:

- **Purpose:** Core platform/voice service (no feature imports).
- **Contains:** Desktop voice/STT/tray/hotkey pipeline. Symbols: (library).
- **Does:** Bridges OS capabilities; record submit calls Brain via injected hooks.
- **Connected to:** Wired from `main.dart` / shell voice routing.

RU:

- **Назначение:** Базовый слой (core).
- **Что внутри:** Тема, виджеты DS, time/voice утилиты.
- **Что делает:** Не импортирует `features/` и Brain HTTP.
- **Как связано с общей логикой:** Переиспользуется UI и частично Brain.


### `lib/core/services/desktop_voice_attempt_log.dart`

EN:

- **Purpose:** Core platform/voice service (no feature imports).
- **Contains:** Desktop voice/STT/tray/hotkey pipeline. Symbols: DesktopVoiceAttemptStatus, DesktopVoiceAttempt.
- **Symbols:** `DesktopVoiceAttemptStatus, DesktopVoiceAttempt`
- **Does:** Bridges OS capabilities; record submit calls Brain via injected hooks.
- **Connected to:** Wired from `main.dart` / shell voice routing.

RU:

- **Назначение:** Базовый слой (core).
- **Что внутри:** Тема, виджеты DS, time/voice утилиты.
- **Что делает:** Не импортирует `features/` и Brain HTTP.
- **Как связано с общей логикой:** Переиспользуется UI и частично Brain.


### `lib/core/services/desktop_voice_audio_capture.dart`

EN:

- **Purpose:** Core platform/voice service (no feature imports).
- **Contains:** Desktop voice/STT/tray/hotkey pipeline. Symbols: DesktopVoiceAudioCapture.
- **Symbols:** `DesktopVoiceAudioCapture`
- **Does:** Bridges OS capabilities; record submit calls Brain via injected hooks.
- **Connected to:** Wired from `main.dart` / shell voice routing.

RU:

- **Назначение:** Базовый слой (core).
- **Что внутри:** Тема, виджеты DS, time/voice утилиты.
- **Что делает:** Не импортирует `features/` и Brain HTTP.
- **Как связано с общей логикой:** Переиспользуется UI и частично Brain.


### `lib/core/services/desktop_voice_benchmark_service.dart`

EN:

- **Purpose:** Core platform/voice service (no feature imports).
- **Contains:** Desktop voice/STT/tray/hotkey pipeline. Symbols: DesktopVoiceBenchmarkService.
- **Symbols:** `DesktopVoiceBenchmarkService`
- **Does:** Bridges OS capabilities; record submit calls Brain via injected hooks.
- **Connected to:** Wired from `main.dart` / shell voice routing.

RU:

- **Назначение:** Базовый слой (core).
- **Что внутри:** Тема, виджеты DS, time/voice утилиты.
- **Что делает:** Не импортирует `features/` и Brain HTTP.
- **Как связано с общей логикой:** Переиспользуется UI и частично Brain.


### `lib/core/services/desktop_voice_command_normalize.dart`

EN:

- **Purpose:** Core platform/voice service (no feature imports).
- **Contains:** Desktop voice/STT/tray/hotkey pipeline. Symbols: DesktopVoiceNormalizedCommand.
- **Symbols:** `DesktopVoiceNormalizedCommand`
- **Does:** Bridges OS capabilities; record submit calls Brain via injected hooks.
- **Connected to:** Wired from `main.dart` / shell voice routing.

RU:

- **Назначение:** Базовый слой (core).
- **Что внутри:** Тема, виджеты DS, time/voice утилиты.
- **Что делает:** Не импортирует `features/` и Brain HTTP.
- **Как связано с общей логикой:** Переиспользуется UI и частично Brain.


### `lib/core/services/desktop_voice_confirmation.dart`

EN:

- **Purpose:** Core platform/voice service (no feature imports).
- **Contains:** Desktop voice/STT/tray/hotkey pipeline. Symbols: (library).
- **Does:** Bridges OS capabilities; record submit calls Brain via injected hooks.
- **Connected to:** Wired from `main.dart` / shell voice routing.

RU:

- **Назначение:** Базовый слой (core).
- **Что внутри:** Тема, виджеты DS, time/voice утилиты.
- **Что делает:** Не импортирует `features/` и Brain HTTP.
- **Как связано с общей логикой:** Переиспользуется UI и частично Brain.


### `lib/core/services/desktop_voice_engine.dart`

EN:

- **Purpose:** Core platform/voice service (no feature imports).
- **Contains:** Desktop voice/STT/tray/hotkey pipeline. Symbols: DesktopVoiceEngineId, DesktopVoiceEngineBenchmark.
- **Symbols:** `DesktopVoiceEngineId, DesktopVoiceEngineBenchmark`
- **Does:** Bridges OS capabilities; record submit calls Brain via injected hooks.
- **Connected to:** Wired from `main.dart` / shell voice routing.

RU:

- **Назначение:** Базовый слой (core).
- **Что внутри:** Тема, виджеты DS, time/voice утилиты.
- **Что делает:** Не импортирует `features/` и Brain HTTP.
- **Как связано с общей логикой:** Переиспользуется UI и частично Brain.


### `lib/core/services/desktop_voice_hotkey.dart`

EN:

- **Purpose:** Core platform/voice service (no feature imports).
- **Contains:** Desktop voice/STT/tray/hotkey pipeline. Symbols: DesktopVoiceHotkeyAction.
- **Symbols:** `DesktopVoiceHotkeyAction`
- **Does:** Bridges OS capabilities; record submit calls Brain via injected hooks.
- **Connected to:** Wired from `main.dart` / shell voice routing.

RU:

- **Назначение:** Базовый слой (core).
- **Что внутри:** Тема, виджеты DS, time/voice утилиты.
- **Что делает:** Не импортирует `features/` и Brain HTTP.
- **Как связано с общей логикой:** Переиспользуется UI и частично Brain.


### `lib/core/services/desktop_voice_hotkey_io.dart`

EN:

- **Purpose:** Core platform/voice service (no feature imports).
- **Contains:** Desktop voice/STT/tray/hotkey pipeline. Symbols: (library).
- **Does:** Bridges OS capabilities; record submit calls Brain via injected hooks.
- **Connected to:** Wired from `main.dart` / shell voice routing.

RU:

- **Назначение:** Базовый слой (core).
- **Что внутри:** Тема, виджеты DS, time/voice утилиты.
- **Что делает:** Не импортирует `features/` и Brain HTTP.
- **Как связано с общей логикой:** Переиспользуется UI и частично Brain.


### `lib/core/services/desktop_voice_hotkey_markers.dart`

EN:

- **Purpose:** Core platform/voice service (no feature imports).
- **Contains:** Desktop voice/STT/tray/hotkey pipeline. Symbols: (library).
- **Does:** Bridges OS capabilities; record submit calls Brain via injected hooks.
- **Connected to:** Wired from `main.dart` / shell voice routing.

RU:

- **Назначение:** Базовый слой (core).
- **Что внутри:** Тема, виджеты DS, time/voice утилиты.
- **Что делает:** Не импортирует `features/` и Brain HTTP.
- **Как связано с общей логикой:** Переиспользуется UI и частично Brain.


### `lib/core/services/desktop_voice_hotkey_stub.dart`

EN:

- **Purpose:** Core platform/voice service (no feature imports).
- **Contains:** Desktop voice/STT/tray/hotkey pipeline. Symbols: (library).
- **Does:** Bridges OS capabilities; record submit calls Brain via injected hooks.
- **Connected to:** Wired from `main.dart` / shell voice routing.

RU:

- **Назначение:** Базовый слой (core).
- **Что внутри:** Тема, виджеты DS, time/voice утилиты.
- **Что делает:** Не импортирует `features/` и Brain HTTP.
- **Как связано с общей логикой:** Переиспользуется UI и частично Brain.


### `lib/core/services/desktop_voice_native_overlay.dart`

EN:

- **Purpose:** Core platform/voice service (no feature imports).
- **Contains:** Desktop voice/STT/tray/hotkey pipeline. Symbols: (library).
- **Does:** Bridges OS capabilities; record submit calls Brain via injected hooks.
- **Connected to:** Wired from `main.dart` / shell voice routing.

RU:

- **Назначение:** Базовый слой (core).
- **Что внутри:** Тема, виджеты DS, time/voice утилиты.
- **Что делает:** Не импортирует `features/` и Brain HTTP.
- **Как связано с общей логикой:** Переиспользуется UI и частично Brain.


### `lib/core/services/desktop_voice_overlay_bridge.dart`

EN:

- **Purpose:** Core platform/voice service (no feature imports).
- **Contains:** Desktop voice/STT/tray/hotkey pipeline. Symbols: (library).
- **Does:** Bridges OS capabilities; record submit calls Brain via injected hooks.
- **Connected to:** Wired from `main.dart` / shell voice routing.

RU:

- **Назначение:** Базовый слой (core).
- **Что внутри:** Тема, виджеты DS, time/voice утилиты.
- **Что делает:** Не импортирует `features/` и Brain HTTP.
- **Как связано с общей логикой:** Переиспользуется UI и частично Brain.


### `lib/core/services/desktop_voice_overlay_host.dart`

EN:

- **Purpose:** Core platform/voice service (no feature imports).
- **Contains:** Desktop voice/STT/tray/hotkey pipeline. Symbols: (library).
- **Does:** Bridges OS capabilities; record submit calls Brain via injected hooks.
- **Connected to:** Wired from `main.dart` / shell voice routing.

RU:

- **Назначение:** Базовый слой (core).
- **Что внутри:** Тема, виджеты DS, time/voice утилиты.
- **Что делает:** Не импортирует `features/` и Brain HTTP.
- **Как связано с общей логикой:** Переиспользуется UI и частично Brain.


### `lib/core/services/desktop_voice_overlay_host_io.dart`

EN:

- **Purpose:** Core platform/voice service (no feature imports).
- **Contains:** Desktop voice/STT/tray/hotkey pipeline. Symbols: (library).
- **Does:** Bridges OS capabilities; record submit calls Brain via injected hooks.
- **Connected to:** Wired from `main.dart` / shell voice routing.

RU:

- **Назначение:** Базовый слой (core).
- **Что внутри:** Тема, виджеты DS, time/voice утилиты.
- **Что делает:** Не импортирует `features/` и Brain HTTP.
- **Как связано с общей логикой:** Переиспользуется UI и частично Brain.


### `lib/core/services/desktop_voice_overlay_host_stub.dart`

EN:

- **Purpose:** Core platform/voice service (no feature imports).
- **Contains:** Desktop voice/STT/tray/hotkey pipeline. Symbols: (library).
- **Does:** Bridges OS capabilities; record submit calls Brain via injected hooks.
- **Connected to:** Wired from `main.dart` / shell voice routing.

RU:

- **Назначение:** Базовый слой (core).
- **Что внутри:** Тема, виджеты DS, time/voice утилиты.
- **Что делает:** Не импортирует `features/` и Brain HTTP.
- **Как связано с общей логикой:** Переиспользуется UI и частично Brain.


### `lib/core/services/desktop_voice_overlay_service.dart`

EN:

- **Purpose:** Core platform/voice service (no feature imports).
- **Contains:** Desktop voice/STT/tray/hotkey pipeline. Symbols: (library).
- **Does:** Bridges OS capabilities; record submit calls Brain via injected hooks.
- **Connected to:** Wired from `main.dart` / shell voice routing.

RU:

- **Назначение:** Базовый слой (core).
- **Что внутри:** Тема, виджеты DS, time/voice утилиты.
- **Что делает:** Не импортирует `features/` и Brain HTTP.
- **Как связано с общей логикой:** Переиспользуется UI и частично Brain.


### `lib/core/services/desktop_voice_recognizer.dart`

EN:

- **Purpose:** Core platform/voice service (no feature imports).
- **Contains:** Desktop voice/STT/tray/hotkey pipeline. Symbols: DesktopVoiceRecognitionResult.
- **Symbols:** `DesktopVoiceRecognitionResult`
- **Does:** Bridges OS capabilities; record submit calls Brain via injected hooks.
- **Connected to:** Wired from `main.dart` / shell voice routing.

RU:

- **Назначение:** Базовый слой (core).
- **Что внутри:** Тема, виджеты DS, time/voice утилиты.
- **Что делает:** Не импортирует `features/` и Brain HTTP.
- **Как связано с общей логикой:** Переиспользуется UI и частично Brain.


### `lib/core/services/desktop_voice_recognizer_factory.dart`

EN:

- **Purpose:** Core platform/voice service (no feature imports).
- **Contains:** Desktop voice/STT/tray/hotkey pipeline. Symbols: (library).
- **Does:** Bridges OS capabilities; record submit calls Brain via injected hooks.
- **Connected to:** Wired from `main.dart` / shell voice routing.

RU:

- **Назначение:** Базовый слой (core).
- **Что внутри:** Тема, виджеты DS, time/voice утилиты.
- **Что делает:** Не импортирует `features/` и Brain HTTP.
- **Как связано с общей логикой:** Переиспользуется UI и частично Brain.


### `lib/core/services/desktop_voice_recognizer_io.dart`

EN:

- **Purpose:** Core platform/voice service (no feature imports).
- **Contains:** Desktop voice/STT/tray/hotkey pipeline. Symbols: DesktopVoiceRecognizerGolosHelper.
- **Symbols:** `DesktopVoiceRecognizerGolosHelper`
- **Does:** Bridges OS capabilities; record submit calls Brain via injected hooks.
- **Connected to:** Wired from `main.dart` / shell voice routing.

RU:

- **Назначение:** Базовый слой (core).
- **Что внутри:** Тема, виджеты DS, time/voice утилиты.
- **Что делает:** Не импортирует `features/` и Brain HTTP.
- **Как связано с общей логикой:** Переиспользуется UI и частично Brain.


### `lib/core/services/desktop_voice_recognizer_stub.dart`

EN:

- **Purpose:** Core platform/voice service (no feature imports).
- **Contains:** Desktop voice/STT/tray/hotkey pipeline. Symbols: _DesktopVoiceRecognizerUnsupported.
- **Symbols:** `_DesktopVoiceRecognizerUnsupported`
- **Does:** Bridges OS capabilities; record submit calls Brain via injected hooks.
- **Connected to:** Wired from `main.dart` / shell voice routing.

RU:

- **Назначение:** Базовый слой (core).
- **Что внутри:** Тема, виджеты DS, time/voice утилиты.
- **Что делает:** Не импортирует `features/` и Brain HTTP.
- **Как связано с общей логикой:** Переиспользуется UI и частично Brain.


### `lib/core/services/desktop_voice_record_submit.dart`

EN:

- **Purpose:** Core platform/voice service (no feature imports).
- **Contains:** Desktop voice/STT/tray/hotkey pipeline. Symbols: DesktopVoiceWriteRecordRequest, DesktopVoiceWriteRecordFn, DesktopVoiceSubmitOutcome.
- **Symbols:** `DesktopVoiceWriteRecordRequest, DesktopVoiceWriteRecordFn, DesktopVoiceSubmitOutcome`
- **Does:** Bridges OS capabilities; record submit calls Brain via injected hooks.
- **Connected to:** Wired from `main.dart` / shell voice routing.

RU:

- **Назначение:** Базовый слой (core).
- **Что внутри:** Тема, виджеты DS, time/voice утилиты.
- **Что делает:** Не импортирует `features/` и Brain HTTP.
- **Как связано с общей логикой:** Переиспользуется UI и частично Brain.


### `lib/core/services/desktop_voice_settings.dart`

EN:

- **Purpose:** Core platform/voice service (no feature imports).
- **Contains:** Desktop voice/STT/tray/hotkey pipeline. Symbols: DesktopVoiceHotkeyConfig, DesktopVoiceSettings.
- **Symbols:** `DesktopVoiceHotkeyConfig, DesktopVoiceSettings`
- **Does:** Bridges OS capabilities; record submit calls Brain via injected hooks.
- **Connected to:** Wired from `main.dart` / shell voice routing.

RU:

- **Назначение:** Базовый слой (core).
- **Что внутри:** Тема, виджеты DS, time/voice утилиты.
- **Что делает:** Не импортирует `features/` и Brain HTTP.
- **Как связано с общей логикой:** Переиспользуется UI и частично Brain.


### `lib/core/services/desktop_voice_smoke_bridge.dart`

EN:

- **Purpose:** Core platform/voice service (no feature imports).
- **Contains:** Desktop voice/STT/tray/hotkey pipeline. Symbols: (library).
- **Does:** Bridges OS capabilities; record submit calls Brain via injected hooks.
- **Connected to:** Wired from `main.dart` / shell voice routing.

RU:

- **Назначение:** Базовый слой (core).
- **Что внутри:** Тема, виджеты DS, time/voice утилиты.
- **Что делает:** Не импортирует `features/` и Brain HTTP.
- **Как связано с общей логикой:** Переиспользуется UI и частично Brain.


### `lib/core/services/desktop_voice_user_error.dart`

EN:

- **Purpose:** Core platform/voice service (no feature imports).
- **Contains:** Desktop voice/STT/tray/hotkey pipeline. Symbols: DesktopVoiceErrorStage, DesktopVoiceUserError.
- **Symbols:** `DesktopVoiceErrorStage, DesktopVoiceUserError`
- **Does:** Bridges OS capabilities; record submit calls Brain via injected hooks.
- **Connected to:** Wired from `main.dart` / shell voice routing.

RU:

- **Назначение:** Базовый слой (core).
- **Что внутри:** Тема, виджеты DS, time/voice утилиты.
- **Что делает:** Не импортирует `features/` и Brain HTTP.
- **Как связано с общей логикой:** Переиспользуется UI и частично Brain.


### `lib/core/services/desktop_voice_window_flags.dart`

EN:

- **Purpose:** Core platform/voice service (no feature imports).
- **Contains:** Desktop voice/STT/tray/hotkey pipeline. Symbols: (library).
- **Does:** Bridges OS capabilities; record submit calls Brain via injected hooks.
- **Connected to:** Wired from `main.dart` / shell voice routing.

RU:

- **Назначение:** Базовый слой (core).
- **Что внутри:** Тема, виджеты DS, time/voice утилиты.
- **Что делает:** Не импортирует `features/` и Brain HTTP.
- **Как связано с общей логикой:** Переиспользуется UI и частично Brain.


### `lib/core/services/desktop_win_speech_service.dart`

EN:

- **Purpose:** Core platform/voice service (no feature imports).
- **Contains:** Desktop voice/STT/tray/hotkey pipeline. Symbols: DesktopWinSpeechService.
- **Symbols:** `DesktopWinSpeechService`
- **Does:** Bridges OS capabilities; record submit calls Brain via injected hooks.
- **Connected to:** Wired from `main.dart` / shell voice routing.

RU:

- **Назначение:** Базовый слой (core).
- **Что внутри:** Тема, виджеты DS, time/voice утилиты.
- **Что делает:** Не импортирует `features/` и Brain HTTP.
- **Как связано с общей логикой:** Переиспользуется UI и частично Brain.


### `lib/core/services/pcm_audio_utils.dart`

EN:

- **Purpose:** Core platform/voice service (no feature imports).
- **Contains:** Desktop voice/STT/tray/hotkey pipeline. Symbols: (library).
- **Does:** Bridges OS capabilities; record submit calls Brain via injected hooks.
- **Connected to:** Wired from `main.dart` / shell voice routing.

RU:

- **Назначение:** Базовый слой (core).
- **Что внутри:** Тема, виджеты DS, time/voice утилиты.
- **Что делает:** Не импортирует `features/` и Brain HTTP.
- **Как связано с общей логикой:** Переиспользуется UI и частично Brain.


### `lib/core/services/speech_engine_handle.dart`

EN:

- **Purpose:** Core platform/voice service (no feature imports).
- **Contains:** Desktop voice/STT/tray/hotkey pipeline. Symbols: (library).
- **Does:** Bridges OS capabilities; record submit calls Brain via injected hooks.
- **Connected to:** Wired from `main.dart` / shell voice routing.

RU:

- **Назначение:** Базовый слой (core).
- **Что внутри:** Тема, виджеты DS, time/voice утилиты.
- **Что делает:** Не импортирует `features/` и Brain HTTP.
- **Как связано с общей логикой:** Переиспользуется UI и частично Brain.


### `lib/core/services/speech_listen_locale.dart`

EN:

- **Purpose:** Core platform/voice service (no feature imports).
- **Contains:** Desktop voice/STT/tray/hotkey pipeline. Symbols: (library).
- **Does:** Bridges OS capabilities; record submit calls Brain via injected hooks.
- **Connected to:** Wired from `main.dart` / shell voice routing.

RU:

- **Назначение:** Базовый слой (core).
- **Что внутри:** Тема, виджеты DS, time/voice утилиты.
- **Что делает:** Не импортирует `features/` и Brain HTTP.
- **Как связано с общей логикой:** Переиспользуется UI и частично Brain.


### `lib/core/shell_adaptive.dart`

EN:

- **Purpose:** Core foundation utility or widget.
- **Contains:** Theme, time, diagnostics, navigation helpers. Symbols: (library).
- **Does:** Shared non-feature code; must not import `features/` or Brain HTTP.
- **Connected to:** Used across UI modules and sometimes Brain (time helpers).

RU:

- **Назначение:** Базовый слой (core).
- **Что внутри:** Тема, виджеты DS, time/voice утилиты.
- **Что делает:** Не импортирует `features/` и Brain HTTP.
- **Как связано с общей логикой:** Переиспользуется UI и частично Brain.


### `lib/core/shell_layout_state.dart`

EN:

- **Purpose:** Core foundation utility or widget.
- **Contains:** Theme, time, diagnostics, navigation helpers. Symbols: ShellLayoutController, ShellLayoutScope.
- **Symbols:** `ShellLayoutController, ShellLayoutScope`
- **Does:** Shared non-feature code; must not import `features/` or Brain HTTP.
- **Connected to:** Used across UI modules and sometimes Brain (time helpers).

RU:

- **Назначение:** Базовый слой (core).
- **Что внутри:** Тема, виджеты DS, time/voice утилиты.
- **Что делает:** Не импортирует `features/` и Brain HTTP.
- **Как связано с общей логикой:** Переиспользуется UI и частично Brain.


### `lib/core/tag_contrast.dart`

EN:

- **Purpose:** Core foundation utility or widget.
- **Contains:** Theme, time, diagnostics, navigation helpers. Symbols: (library).
- **Does:** Shared non-feature code; must not import `features/` or Brain HTTP.
- **Connected to:** Used across UI modules and sometimes Brain (time helpers).

RU:

- **Назначение:** Базовый слой (core).
- **Что внутри:** Тема, виджеты DS, time/voice утилиты.
- **Что делает:** Не импортирует `features/` и Brain HTTP.
- **Как связано с общей логикой:** Переиспользуется UI и частично Brain.


### `lib/core/theme.dart`

EN:

- **Purpose:** Core foundation utility or widget.
- **Contains:** Theme, time, diagnostics, navigation helpers. Symbols: (library).
- **Does:** Shared non-feature code; must not import `features/` or Brain HTTP.
- **Connected to:** Used across UI modules and sometimes Brain (time helpers).

RU:

- **Назначение:** Базовый слой (core).
- **Что внутри:** Тема, виджеты DS, time/voice утилиты.
- **Что делает:** Не импортирует `features/` и Brain HTTP.
- **Как связано с общей логикой:** Переиспользуется UI и частично Brain.


### `lib/core/time/app_clock.dart`

EN:

- **Purpose:** Core foundation utility or widget.
- **Contains:** Theme, time, diagnostics, navigation helpers. Symbols: WallNowFn, AppClock.
- **Symbols:** `WallNowFn, AppClock`
- **Does:** Shared non-feature code; must not import `features/` or Brain HTTP.
- **Connected to:** Used across UI modules and sometimes Brain (time helpers).

RU:

- **Назначение:** Базовый слой (core).
- **Что внутри:** Тема, виджеты DS, time/voice утилиты.
- **Что делает:** Не импортирует `features/` и Brain HTTP.
- **Как связано с общей логикой:** Переиспользуется UI и частично Brain.


### `lib/core/time/category_timezone_options.dart`

EN:

- **Purpose:** Core foundation utility or widget.
- **Contains:** Theme, time, diagnostics, navigation helpers. Symbols: CategoryDefaultTimezoneOption.
- **Symbols:** `CategoryDefaultTimezoneOption`
- **Does:** Shared non-feature code; must not import `features/` or Brain HTTP.
- **Connected to:** Used across UI modules and sometimes Brain (time helpers).

RU:

- **Назначение:** Базовый слой (core).
- **Что внутри:** Тема, виджеты DS, time/voice утилиты.
- **Что делает:** Не импортирует `features/` и Brain HTTP.
- **Как связано с общей логикой:** Переиспользуется UI и частично Brain.


### `lib/core/time/plan_time_labels.dart`

EN:

- **Purpose:** Core foundation utility or widget.
- **Contains:** Theme, time, diagnostics, navigation helpers. Symbols: (library).
- **Does:** Shared non-feature code; must not import `features/` or Brain HTTP.
- **Connected to:** Used across UI modules and sometimes Brain (time helpers).

RU:

- **Назначение:** Базовый слой (core).
- **Что внутри:** Тема, виджеты DS, time/voice утилиты.
- **Что делает:** Не импортирует `features/` и Brain HTTP.
- **Как связано с общей логикой:** Переиспользуется UI и частично Brain.


### `lib/core/time/plan_time_visible_window.dart`

EN:

- **Purpose:** Core foundation utility or widget.
- **Contains:** Theme, time, diagnostics, navigation helpers. Symbols: (library).
- **Does:** Shared non-feature code; must not import `features/` or Brain HTTP.
- **Connected to:** Used across UI modules and sometimes Brain (time helpers).

RU:

- **Назначение:** Базовый слой (core).
- **Что внутри:** Тема, виджеты DS, time/voice утилиты.
- **Что делает:** Не импортирует `features/` и Brain HTTP.
- **Как связано с общей логикой:** Переиспользуется UI и частично Brain.


### `lib/core/time/profile_timezone_actions.dart`

EN:

- **Purpose:** Core foundation utility or widget.
- **Contains:** Theme, time, diagnostics, navigation helpers. Symbols: ProfileTimezoneShortLabelFn, SaveProfileTimezoneFn, CurrentUserSettingsFn.
- **Symbols:** `ProfileTimezoneShortLabelFn, SaveProfileTimezoneFn, CurrentUserSettingsFn, ProfileTimezoneActions`
- **Does:** Shared non-feature code; must not import `features/` or Brain HTTP.
- **Connected to:** Used across UI modules and sometimes Brain (time helpers).

RU:

- **Назначение:** Базовый слой (core).
- **Что внутри:** Тема, виджеты DS, time/voice утилиты.
- **Что делает:** Не импортирует `features/` и Brain HTTP.
- **Как связано с общей логикой:** Переиспользуется UI и частично Brain.


### `lib/core/time/profile_timezone_catalog.dart`

EN:

- **Purpose:** Core foundation utility or widget.
- **Contains:** Theme, time, diagnostics, navigation helpers. Symbols: ProfileTimezoneCatalogEntry.
- **Symbols:** `ProfileTimezoneCatalogEntry`
- **Does:** Shared non-feature code; must not import `features/` or Brain HTTP.
- **Connected to:** Used across UI modules and sometimes Brain (time helpers).

RU:

- **Назначение:** Базовый слой (core).
- **Что внутри:** Тема, виджеты DS, time/voice утилиты.
- **Что делает:** Не импортирует `features/` и Brain HTTP.
- **Как связано с общей логикой:** Переиспользуется UI и частично Brain.


### `lib/core/time/wall_clock.dart`

EN:

- **Purpose:** Core foundation utility or widget.
- **Contains:** Theme, time, diagnostics, navigation helpers. Symbols: (library).
- **Does:** Shared non-feature code; must not import `features/` or Brain HTTP.
- **Connected to:** Used across UI modules and sometimes Brain (time helpers).

RU:

- **Назначение:** Базовый слой (core).
- **Что внутри:** Тема, виджеты DS, time/voice утилиты.
- **Что делает:** Не импортирует `features/` и Brain HTTP.
- **Как связано с общей логикой:** Переиспользуется UI и частично Brain.


### `lib/core/url_strategy_stub.dart`

EN:

- **Purpose:** Core foundation utility or widget.
- **Contains:** Theme, time, diagnostics, navigation helpers. Symbols: (library).
- **Does:** Shared non-feature code; must not import `features/` or Brain HTTP.
- **Connected to:** Used across UI modules and sometimes Brain (time helpers).

RU:

- **Назначение:** Базовый слой (core).
- **Что внутри:** Тема, виджеты DS, time/voice утилиты.
- **Что делает:** Не импортирует `features/` и Brain HTTP.
- **Как связано с общей логикой:** Переиспользуется UI и частично Brain.


### `lib/core/web_redirect.dart`

EN:

- **Purpose:** Core foundation utility or widget.
- **Contains:** Theme, time, diagnostics, navigation helpers. Symbols: (library).
- **Does:** Shared non-feature code; must not import `features/` or Brain HTTP.
- **Connected to:** Used across UI modules and sometimes Brain (time helpers).

RU:

- **Назначение:** Базовый слой (core).
- **Что внутри:** Тема, виджеты DS, time/voice утилиты.
- **Что делает:** Не импортирует `features/` и Brain HTTP.
- **Как связано с общей логикой:** Переиспользуется UI и частично Brain.


### `lib/core/widgets/app_bar_live_clock.dart`

EN:

- **Purpose:** Canonical design-system widget.
- **Contains:** Reusable UI primitive per `docs/DESIGN_SYSTEM.md`. Symbols: AppBarLiveClock, _AppBarLiveClockState.
- **Symbols:** `AppBarLiveClock, _AppBarLiveClockState`
- **Does:** Feature screens compose these instead of raw Material buttons.
- **Connected to:** Component Lab demonstrates mappings.

RU:

- **Назначение:** Базовый слой (core).
- **Что внутри:** Тема, виджеты DS, time/voice утилиты.
- **Что делает:** Не импортирует `features/` и Brain HTTP.
- **Как связано с общей логикой:** Переиспользуется UI и частично Brain.


### `lib/core/widgets/app_button.dart`

EN:

- **Purpose:** Canonical design-system widget.
- **Contains:** Reusable UI primitive per `docs/DESIGN_SYSTEM.md`. Symbols: AppButtonVariant, AppButtonSize, AppButton.
- **Symbols:** `AppButtonVariant, AppButtonSize, AppButton`
- **Does:** Feature screens compose these instead of raw Material buttons.
- **Connected to:** Component Lab demonstrates mappings.

RU:

- **Назначение:** Базовый слой (core).
- **Что внутри:** Тема, виджеты DS, time/voice утилиты.
- **Что делает:** Не импортирует `features/` и Brain HTTP.
- **Как связано с общей логикой:** Переиспользуется UI и частично Brain.


### `lib/core/widgets/app_icon_button.dart`

EN:

- **Purpose:** Canonical design-system widget.
- **Contains:** Reusable UI primitive per `docs/DESIGN_SYSTEM.md`. Symbols: AppIconButtonVariant, AppIconButtonSize, AppIconButton.
- **Symbols:** `AppIconButtonVariant, AppIconButtonSize, AppIconButton`
- **Does:** Feature screens compose these instead of raw Material buttons.
- **Connected to:** Component Lab demonstrates mappings.

RU:

- **Назначение:** Базовый слой (core).
- **Что внутри:** Тема, виджеты DS, time/voice утилиты.
- **Что делает:** Не импортирует `features/` и Brain HTTP.
- **Как связано с общей логикой:** Переиспользуется UI и частично Brain.


### `lib/core/widgets/app_loading.dart`

EN:

- **Purpose:** Canonical design-system widget.
- **Contains:** Reusable UI primitive per `docs/DESIGN_SYSTEM.md`. Symbols: AppLoadingSize, AppLoading.
- **Symbols:** `AppLoadingSize, AppLoading`
- **Does:** Feature screens compose these instead of raw Material buttons.
- **Connected to:** Component Lab demonstrates mappings.

RU:

- **Назначение:** Базовый слой (core).
- **Что внутри:** Тема, виджеты DS, time/voice утилиты.
- **Что делает:** Не импортирует `features/` и Brain HTTP.
- **Как связано с общей логикой:** Переиспользуется UI и частично Brain.


### `lib/core/widgets/app_mic_level_bars.dart`

EN:

- **Purpose:** Canonical design-system widget.
- **Contains:** Reusable UI primitive per `docs/DESIGN_SYSTEM.md`. Symbols: AppMicLevelBars.
- **Symbols:** `AppMicLevelBars`
- **Does:** Feature screens compose these instead of raw Material buttons.
- **Connected to:** Component Lab demonstrates mappings.

RU:

- **Назначение:** Базовый слой (core).
- **Что внутри:** Тема, виджеты DS, time/voice утилиты.
- **Что делает:** Не импортирует `features/` и Brain HTTP.
- **Как связано с общей логикой:** Переиспользуется UI и частично Brain.


### `lib/core/widgets/app_settings_layout.dart`

EN:

- **Purpose:** Canonical design-system widget.
- **Contains:** Reusable UI primitive per `docs/DESIGN_SYSTEM.md`. Symbols: AppSettingsTab, AppSettingsCategoryTabs, AppHotkeyKeycaps.
- **Symbols:** `AppSettingsTab, AppSettingsCategoryTabs, AppHotkeyKeycaps`
- **Does:** Feature screens compose these instead of raw Material buttons.
- **Connected to:** Component Lab demonstrates mappings.

RU:

- **Назначение:** Базовый слой (core).
- **Что внутри:** Тема, виджеты DS, time/voice утилиты.
- **Что делает:** Не импортирует `features/` и Brain HTTP.
- **Как связано с общей логикой:** Переиспользуется UI и частично Brain.


### `lib/core/widgets/app_state_views.dart`

EN:

- **Purpose:** Canonical design-system widget.
- **Contains:** Reusable UI primitive per `docs/DESIGN_SYSTEM.md`. Symbols: AppErrorState, AppEmptyState.
- **Symbols:** `AppErrorState, AppEmptyState`
- **Does:** Feature screens compose these instead of raw Material buttons.
- **Connected to:** Component Lab demonstrates mappings.

RU:

- **Назначение:** Базовый слой (core).
- **Что внутри:** Тема, виджеты DS, time/voice утилиты.
- **Что делает:** Не импортирует `features/` и Brain HTTP.
- **Как связано с общей логикой:** Переиспользуется UI и частично Brain.


### `lib/core/widgets/app_timezone_icon.dart`

EN:

- **Purpose:** Canonical design-system widget.
- **Contains:** Reusable UI primitive per `docs/DESIGN_SYSTEM.md`. Symbols: AppTimezoneIcon, _AppTimezoneIconPainter.
- **Symbols:** `AppTimezoneIcon, _AppTimezoneIconPainter`
- **Does:** Feature screens compose these instead of raw Material buttons.
- **Connected to:** Component Lab demonstrates mappings.

RU:

- **Назначение:** Базовый слой (core).
- **Что внутри:** Тема, виджеты DS, time/voice утилиты.
- **Что делает:** Не импортирует `features/` и Brain HTTP.
- **Как связано с общей логикой:** Переиспользуется UI и частично Brain.


### `lib/core/widgets/chip_component.dart`

EN:

- **Purpose:** Canonical design-system widget.
- **Contains:** Reusable UI primitive per `docs/DESIGN_SYSTEM.md`. Symbols: CategoryBreadcrumb, CategoryChipVariant, CategoryChip.
- **Symbols:** `CategoryBreadcrumb, CategoryChipVariant, CategoryChip`
- **Does:** Feature screens compose these instead of raw Material buttons.
- **Connected to:** Component Lab demonstrates mappings.

RU:

- **Назначение:** Базовый слой (core).
- **Что внутри:** Тема, виджеты DS, time/voice утилиты.
- **Что делает:** Не импортирует `features/` и Brain HTTP.
- **Как связано с общей логикой:** Переиспользуется UI и частично Brain.


### `lib/core/widgets/compact_nav_controls.dart`

EN:

- **Purpose:** Canonical design-system widget.
- **Contains:** Reusable UI primitive per `docs/DESIGN_SYSTEM.md`. Symbols: AppCompactSegmentLabel, AppCompactTextTab.
- **Symbols:** `AppCompactSegmentLabel, AppCompactTextTab`
- **Does:** Feature screens compose these instead of raw Material buttons.
- **Connected to:** Component Lab demonstrates mappings.

RU:

- **Назначение:** Базовый слой (core).
- **Что внутри:** Тема, виджеты DS, time/voice утилиты.
- **Что делает:** Не импортирует `features/` и Brain HTTP.
- **Как связано с общей логикой:** Переиспользуется UI и частично Brain.


### `lib/core/widgets/confirm_dialog.dart`

EN:

- **Purpose:** Canonical design-system widget.
- **Contains:** Reusable UI primitive per `docs/DESIGN_SYSTEM.md`. Symbols: (library).
- **Does:** Feature screens compose these instead of raw Material buttons.
- **Connected to:** Component Lab demonstrates mappings.

RU:

- **Назначение:** Базовый слой (core).
- **Что внутри:** Тема, виджеты DS, time/voice утилиты.
- **Что делает:** Не импортирует `features/` и Brain HTTP.
- **Как связано с общей логикой:** Переиспользуется UI и частично Brain.


### `lib/core/widgets/day_content_strip.dart`

EN:

- **Purpose:** Canonical design-system widget.
- **Contains:** Reusable UI primitive per `docs/DESIGN_SYSTEM.md`. Symbols: EagerDayItemBuilder, EagerDayContentStripController, EagerDayContentStrip.
- **Symbols:** `EagerDayItemBuilder, EagerDayContentStripController, EagerDayContentStrip, _EagerDayContentStripState`
- **Does:** Feature screens compose these instead of raw Material buttons.
- **Connected to:** Component Lab demonstrates mappings.

RU:

- **Назначение:** Базовый слой (core).
- **Что внутри:** Тема, виджеты DS, time/voice утилиты.
- **Что делает:** Не импортирует `features/` и Brain HTTP.
- **Как связано с общей логикой:** Переиспользуется UI и частично Brain.


### `lib/core/widgets/day_window.dart`

EN:

- **Purpose:** Canonical design-system widget.
- **Contains:** Reusable UI primitive per `docs/DESIGN_SYSTEM.md`. Symbols: (library).
- **Does:** Feature screens compose these instead of raw Material buttons.
- **Connected to:** Component Lab demonstrates mappings.

RU:

- **Назначение:** Базовый слой (core).
- **Что внутри:** Тема, виджеты DS, time/voice утилиты.
- **Что делает:** Не импортирует `features/` и Brain HTTP.
- **Как связано с общей логикой:** Переиспользуется UI и частично Brain.


### `lib/core/widgets/global_app_header.dart`

EN:

- **Purpose:** Canonical design-system widget.
- **Contains:** Reusable UI primitive per `docs/DESIGN_SYSTEM.md`. Symbols: GlobalAppHeader.
- **Symbols:** `GlobalAppHeader`
- **Does:** Feature screens compose these instead of raw Material buttons.
- **Connected to:** Component Lab demonstrates mappings.

RU:

- **Назначение:** Базовый слой (core).
- **Что внутри:** Тема, виджеты DS, time/voice утилиты.
- **Что делает:** Не импортирует `features/` и Brain HTTP.
- **Как связано с общей логикой:** Переиспользуется UI и частично Brain.


### `lib/core/widgets/lazy_indexed_stack.dart`

EN:

- **Purpose:** Canonical design-system widget.
- **Contains:** Reusable UI primitive per `docs/DESIGN_SYSTEM.md`. Symbols: LazyIndexedStack, _LazyIndexedStackState.
- **Symbols:** `LazyIndexedStack, _LazyIndexedStackState`
- **Does:** Feature screens compose these instead of raw Material buttons.
- **Connected to:** Component Lab demonstrates mappings.

RU:

- **Назначение:** Базовый слой (core).
- **Что внутри:** Тема, виджеты DS, time/voice утилиты.
- **Что делает:** Не импортирует `features/` и Brain HTTP.
- **Как связано с общей логикой:** Переиспользуется UI и частично Brain.


### `lib/core/widgets/life_card.dart`

EN:

- **Purpose:** Canonical design-system widget.
- **Contains:** Reusable UI primitive per `docs/DESIGN_SYSTEM.md`. Symbols: LifeCardState, LifeCardDensity, AppTaskCardType.
- **Symbols:** `LifeCardState, LifeCardDensity, AppTaskCardType, LifeCard, AppTaskCard`
- **Does:** Feature screens compose these instead of raw Material buttons.
- **Connected to:** Component Lab demonstrates mappings.

RU:

- **Назначение:** Базовый слой (core).
- **Что внутри:** Тема, виджеты DS, time/voice утилиты.
- **Что делает:** Не импортирует `features/` и Brain HTTP.
- **Как связано с общей логикой:** Переиспользуется UI и частично Brain.


### `lib/core/widgets/mouse_drag_scroll_behavior.dart`

EN:

- **Purpose:** Canonical design-system widget.
- **Contains:** Reusable UI primitive per `docs/DESIGN_SYSTEM.md`. Symbols: MouseDragScrollBehavior.
- **Symbols:** `MouseDragScrollBehavior`
- **Does:** Feature screens compose these instead of raw Material buttons.
- **Connected to:** Component Lab demonstrates mappings.

RU:

- **Назначение:** Базовый слой (core).
- **Что внутри:** Тема, виджеты DS, time/voice утилиты.
- **Что делает:** Не импортирует `features/` и Brain HTTP.
- **Как связано с общей логикой:** Переиспользуется UI и частично Brain.


### `lib/core/widgets/omni_date_time_picker_dialog.dart`

EN:

- **Purpose:** Canonical design-system widget.
- **Contains:** Reusable UI primitive per `docs/DESIGN_SYSTEM.md`. Symbols: _OmniDateTimePickerDialog, _OmniDateTimePickerDialogState.
- **Symbols:** `_OmniDateTimePickerDialog, _OmniDateTimePickerDialogState`
- **Does:** Feature screens compose these instead of raw Material buttons.
- **Connected to:** Component Lab demonstrates mappings.

RU:

- **Назначение:** Базовый слой (core).
- **Что внутри:** Тема, виджеты DS, time/voice утилиты.
- **Что делает:** Не импортирует `features/` и Brain HTTP.
- **Как связано с общей логикой:** Переиспользуется UI и частично Brain.


### `lib/core/widgets/plan_card.dart`

EN:

- **Purpose:** Canonical design-system widget.
- **Contains:** Reusable UI primitive per `docs/DESIGN_SYSTEM.md`. Symbols: PlanCard.
- **Symbols:** `PlanCard`
- **Does:** Feature screens compose these instead of raw Material buttons.
- **Connected to:** Component Lab demonstrates mappings.

RU:

- **Назначение:** Базовый слой (core).
- **Что внутри:** Тема, виджеты DS, time/voice утилиты.
- **Что делает:** Не импортирует `features/` и Brain HTTP.
- **Как связано с общей логикой:** Переиспользуется UI и частично Brain.


### `lib/core/widgets/plan_card/plan_card_controls.dart`

EN:

- **Purpose:** Canonical design-system widget.
- **Contains:** Reusable UI primitive per `docs/DESIGN_SYSTEM.md`. Symbols: (library).
- **Does:** Feature screens compose these instead of raw Material buttons.
- **Connected to:** Component Lab demonstrates mappings.

RU:

- **Назначение:** Базовый слой (core).
- **Что внутри:** Тема, виджеты DS, time/voice утилиты.
- **Что делает:** Не импортирует `features/` и Brain HTTP.
- **Как связано с общей логикой:** Переиспользуется UI и частично Brain.


### `lib/core/widgets/plan_card/plan_card_geometry.dart`

EN:

- **Purpose:** Canonical design-system widget.
- **Contains:** Reusable UI primitive per `docs/DESIGN_SYSTEM.md`. Symbols: (library).
- **Does:** Feature screens compose these instead of raw Material buttons.
- **Connected to:** Component Lab demonstrates mappings.

RU:

- **Назначение:** Базовый слой (core).
- **Что внутри:** Тема, виджеты DS, time/voice утилиты.
- **Что делает:** Не импортирует `features/` и Brain HTTP.
- **Как связано с общей логикой:** Переиспользуется UI и частично Brain.


### `lib/core/widgets/plan_card/plan_card_metrics.dart`

EN:

- **Purpose:** Canonical design-system widget.
- **Contains:** Reusable UI primitive per `docs/DESIGN_SYSTEM.md`. Symbols: (library).
- **Does:** Feature screens compose these instead of raw Material buttons.
- **Connected to:** Component Lab demonstrates mappings.

RU:

- **Назначение:** Базовый слой (core).
- **Что внутри:** Тема, виджеты DS, time/voice утилиты.
- **Что делает:** Не импортирует `features/` и Brain HTTP.
- **Как связано с общей логикой:** Переиспользуется UI и частично Brain.


### `lib/core/widgets/plan_card/plan_card_sections.dart`

EN:

- **Purpose:** Canonical design-system widget.
- **Contains:** Reusable UI primitive per `docs/DESIGN_SYSTEM.md`. Symbols: (library).
- **Does:** Feature screens compose these instead of raw Material buttons.
- **Connected to:** Component Lab demonstrates mappings.

RU:

- **Назначение:** Базовый слой (core).
- **Что внутри:** Тема, виджеты DS, time/voice утилиты.
- **Что делает:** Не импортирует `features/` и Brain HTTP.
- **Как связано с общей логикой:** Переиспользуется UI и частично Brain.


### `lib/core/widgets/plan_card/plan_time_card_density.dart`

EN:

- **Purpose:** Canonical design-system widget.
- **Contains:** Reusable UI primitive per `docs/DESIGN_SYSTEM.md`. Symbols: (library).
- **Does:** Feature screens compose these instead of raw Material buttons.
- **Connected to:** Component Lab demonstrates mappings.

RU:

- **Назначение:** Базовый слой (core).
- **Что внутри:** Тема, виджеты DS, time/voice утилиты.
- **Что делает:** Не импортирует `features/` и Brain HTTP.
- **Как связано с общей логикой:** Переиспользуется UI и частично Brain.


### `lib/core/widgets/plan_time_task_card.dart`

EN:

- **Purpose:** Canonical design-system widget.
- **Contains:** Reusable UI primitive per `docs/DESIGN_SYSTEM.md`. Symbols: (library).
- **Does:** Feature screens compose these instead of raw Material buttons.
- **Connected to:** Component Lab demonstrates mappings.

RU:

- **Назначение:** Базовый слой (core).
- **Что внутри:** Тема, виджеты DS, time/voice утилиты.
- **Что делает:** Не импортирует `features/` и Brain HTTP.
- **Как связано с общей логикой:** Переиспользуется UI и частично Brain.


### `lib/core/widgets/plan_time_task_card/plan_card_controls.dart`

EN:

- **Purpose:** Canonical design-system widget.
- **Contains:** Reusable UI primitive per `docs/DESIGN_SYSTEM.md`. Symbols: PlanCardCheckbox, PlanCardCheckboxState.
- **Symbols:** `PlanCardCheckbox, PlanCardCheckboxState`
- **Does:** Feature screens compose these instead of raw Material buttons.
- **Connected to:** Component Lab demonstrates mappings.

RU:

- **Назначение:** Базовый слой (core).
- **Что внутри:** Тема, виджеты DS, time/voice утилиты.
- **Что делает:** Не импортирует `features/` и Brain HTTP.
- **Как связано с общей логикой:** Переиспользуется UI и частично Brain.


### `lib/core/widgets/plan_time_task_card/plan_card_density.dart`

EN:

- **Purpose:** Canonical design-system widget.
- **Contains:** Reusable UI primitive per `docs/DESIGN_SYSTEM.md`. Symbols: PlanTimeCardVisualDensity.
- **Symbols:** `PlanTimeCardVisualDensity`
- **Does:** Feature screens compose these instead of raw Material buttons.
- **Connected to:** Component Lab demonstrates mappings.

RU:

- **Назначение:** Базовый слой (core).
- **Что внутри:** Тема, виджеты DS, time/voice утилиты.
- **Что делает:** Не импортирует `features/` и Brain HTTP.
- **Как связано с общей логикой:** Переиспользуется UI и частично Brain.


### `lib/core/widgets/plan_time_task_card/plan_card_geometry.dart`

EN:

- **Purpose:** Canonical design-system widget.
- **Contains:** Reusable UI primitive per `docs/DESIGN_SYSTEM.md`. Symbols: (library).
- **Does:** Feature screens compose these instead of raw Material buttons.
- **Connected to:** Component Lab demonstrates mappings.

RU:

- **Назначение:** Базовый слой (core).
- **Что внутри:** Тема, виджеты DS, time/voice утилиты.
- **Что делает:** Не импортирует `features/` и Brain HTTP.
- **Как связано с общей логикой:** Переиспользуется UI и частично Brain.


### `lib/core/widgets/plan_time_task_card/plan_card_layouts.dart`

EN:

- **Purpose:** Canonical design-system widget.
- **Contains:** Reusable UI primitive per `docs/DESIGN_SYSTEM.md`. Symbols: TimeViewDensityBody, TimeViewCardCommon.
- **Symbols:** `TimeViewDensityBody, TimeViewCardCommon`
- **Does:** Feature screens compose these instead of raw Material buttons.
- **Connected to:** Component Lab demonstrates mappings.

RU:

- **Назначение:** Базовый слой (core).
- **Что внутри:** Тема, виджеты DS, time/voice утилиты.
- **Что делает:** Не импортирует `features/` и Brain HTTP.
- **Как связано с общей логикой:** Переиспользуется UI и частично Brain.


### `lib/core/widgets/plan_time_task_card/plan_card_metrics.dart`

EN:

- **Purpose:** Canonical design-system widget.
- **Contains:** Reusable UI primitive per `docs/DESIGN_SYSTEM.md`. Symbols: PlanTimeTaskCardDensity, PlanCardSurface.
- **Symbols:** `PlanTimeTaskCardDensity, PlanCardSurface`
- **Does:** Feature screens compose these instead of raw Material buttons.
- **Connected to:** Component Lab demonstrates mappings.

RU:

- **Назначение:** Базовый слой (core).
- **Что внутри:** Тема, виджеты DS, time/voice утилиты.
- **Что делает:** Не импортирует `features/` и Brain HTTP.
- **Как связано с общей логикой:** Переиспользуется UI и частично Brain.


### `lib/core/widgets/plan_time_task_card/plan_card_progress.dart`

EN:

- **Purpose:** Canonical design-system widget.
- **Contains:** Reusable UI primitive per `docs/DESIGN_SYSTEM.md`. Symbols: PlanCardProgressSlot, PlanCardInvariantBody.
- **Symbols:** `PlanCardProgressSlot, PlanCardInvariantBody`
- **Does:** Feature screens compose these instead of raw Material buttons.
- **Connected to:** Component Lab demonstrates mappings.

RU:

- **Назначение:** Базовый слой (core).
- **Что внутри:** Тема, виджеты DS, time/voice утилиты.
- **Что делает:** Не импортирует `features/` и Brain HTTP.
- **Как связано с общей логикой:** Переиспользуется UI и частично Brain.


### `lib/core/widgets/plan_time_task_card/plan_card_sections.dart`

EN:

- **Purpose:** Canonical design-system widget.
- **Contains:** Reusable UI primitive per `docs/DESIGN_SYSTEM.md`. Symbols: PlanCardTagsRow, PlanCardTimeText, PlanCardFooterRow.
- **Symbols:** `PlanCardTagsRow, PlanCardTimeText, PlanCardFooterRow`
- **Does:** Feature screens compose these instead of raw Material buttons.
- **Connected to:** Component Lab demonstrates mappings.

RU:

- **Назначение:** Базовый слой (core).
- **Что внутри:** Тема, виджеты DS, time/voice утилиты.
- **Что делает:** Не импортирует `features/` и Brain HTTP.
- **Как связано с общей логикой:** Переиспользуется UI и частично Brain.


### `lib/core/widgets/plan_time_task_card/plan_card_tags.dart`

EN:

- **Purpose:** Canonical design-system widget.
- **Contains:** Reusable UI primitive per `docs/DESIGN_SYSTEM.md`. Symbols: TimeViewTagsRow, TimeViewTagStack, TimeViewCompactTagPill.
- **Symbols:** `TimeViewTagsRow, TimeViewTagStack, TimeViewCompactTagPill`
- **Does:** Feature screens compose these instead of raw Material buttons.
- **Connected to:** Component Lab demonstrates mappings.

RU:

- **Назначение:** Базовый слой (core).
- **Что внутри:** Тема, виджеты DS, time/voice утилиты.
- **Что делает:** Не импортирует `features/` и Brain HTTP.
- **Как связано с общей логикой:** Переиспользуется UI и частично Brain.


### `lib/core/widgets/plan_time_task_card/plan_time_task_card.dart`

EN:

- **Purpose:** Canonical design-system widget.
- **Contains:** Reusable UI primitive per `docs/DESIGN_SYSTEM.md`. Symbols: PlanTimeTaskCard, _PlanTimeTaskCardState.
- **Symbols:** `PlanTimeTaskCard, _PlanTimeTaskCardState`
- **Does:** Feature screens compose these instead of raw Material buttons.
- **Connected to:** Component Lab demonstrates mappings.

RU:

- **Назначение:** Базовый слой (core).
- **Что внутри:** Тема, виджеты DS, time/voice утилиты.
- **Что делает:** Не импортирует `features/` и Brain HTTP.
- **Как связано с общей логикой:** Переиспользуется UI и частично Brain.


### `lib/core/widgets/tag_display_mode_scope.dart`

EN:

- **Purpose:** Canonical design-system widget.
- **Contains:** Reusable UI primitive per `docs/DESIGN_SYSTEM.md`. Symbols: TagDisplayModeScope.
- **Symbols:** `TagDisplayModeScope`
- **Does:** Feature screens compose these instead of raw Material buttons.
- **Connected to:** Component Lab demonstrates mappings.

RU:

- **Назначение:** Базовый слой (core).
- **Что внутри:** Тема, виджеты DS, time/voice утилиты.
- **Что делает:** Не импортирует `features/` и Brain HTTP.
- **Как связано с общей логикой:** Переиспользуется UI и частично Brain.


### `lib/core/widgets/timezone_quick_picker.dart`

EN:

- **Purpose:** Canonical design-system widget.
- **Contains:** Reusable UI primitive per `docs/DESIGN_SYSTEM.md`. Symbols: HeaderTimezoneQuickSwitcher, TimezonePickerField.
- **Symbols:** `HeaderTimezoneQuickSwitcher, TimezonePickerField`
- **Does:** Feature screens compose these instead of raw Material buttons.
- **Connected to:** Component Lab demonstrates mappings.

RU:

- **Назначение:** Базовый слой (core).
- **Что внутри:** Тема, виджеты DS, time/voice утилиты.
- **Что делает:** Не импортирует `features/` и Brain HTTP.
- **Как связано с общей логикой:** Переиспользуется UI и частично Brain.


### `lib/data/auth_bridge.dart`

EN:

- **Purpose:** Project file.
- **Contains:** Supporting source or config. Symbols: OAuthSignInResult, PasswordResetRequestResult, AuthBridgeException.
- **Symbols:** `OAuthSignInResult, PasswordResetRequestResult, AuthBridgeException, AuthBridgeCancelled, AuthBridge`
- **Does:** See path prefix for ownership.
- **Connected to:** Cross-check `docs/APP_STRUCTURE.md` layer rules.

RU:

- **Назначение:** Модуль Brain (данные/PocketBase).
- **Что внутри:** Логика домена, кэш, optimistic UI, outbox — см. англ. блок.
- **Что делает:** Единственный слой с HTTP к PocketBase (`database_service.dart` + parts).
- **Как связано с общей логикой:** UI вызывает через `DatabaseService.instance`.


### `lib/data/cache/day_snapshot_window.dart`

EN:

- **Purpose:** Project file.
- **Contains:** Supporting source or config. Symbols: TimelineDaySnapshot, PlansDaySnapshot, WarmSnapshotWindow.
- **Symbols:** `TimelineDaySnapshot, PlansDaySnapshot, WarmSnapshotWindow`
- **Does:** See path prefix for ownership.
- **Connected to:** Cross-check `docs/APP_STRUCTURE.md` layer rules.

RU:

- **Назначение:** Модуль Brain (данные/PocketBase).
- **Что внутри:** Логика домена, кэш, optimistic UI, outbox — см. англ. блок.
- **Что делает:** Единственный слой с HTTP к PocketBase (`database_service.dart` + parts).
- **Как связано с общей логикой:** UI вызывает через `DatabaseService.instance`.


### `lib/data/cache/render_snapshot.dart`

EN:

- **Purpose:** Project file.
- **Contains:** Supporting source or config. Symbols: PlanCardRenderDto, PlansDayRenderSnapshot, TimelineCardRenderDto.
- **Symbols:** `PlanCardRenderDto, PlansDayRenderSnapshot, TimelineCardRenderDto, TimelineDayRenderSnapshot`
- **Does:** See path prefix for ownership.
- **Connected to:** Cross-check `docs/APP_STRUCTURE.md` layer rules.

RU:

- **Назначение:** Модуль Brain (данные/PocketBase).
- **Что внутри:** Логика домена, кэш, optimistic UI, outbox — см. англ. блок.
- **Что делает:** Единственный слой с HTTP к PocketBase (`database_service.dart` + parts).
- **Как связано с общей логикой:** UI вызывает через `DatabaseService.instance`.


### `lib/data/cache/rendered_day_body_cache.dart`

EN:

- **Purpose:** Project file.
- **Contains:** Supporting source or config. Symbols: TimelineDayBodyEntry, PlansDayBodyEntry.
- **Symbols:** `TimelineDayBodyEntry, PlansDayBodyEntry`
- **Does:** See path prefix for ownership.
- **Connected to:** Cross-check `docs/APP_STRUCTURE.md` layer rules.

RU:

- **Назначение:** Модуль Brain (данные/PocketBase).
- **Что внутри:** Логика домена, кэш, optimistic UI, outbox — см. англ. блок.
- **Что делает:** Единственный слой с HTTP к PocketBase (`database_service.dart` + parts).
- **Как связано с общей логикой:** UI вызывает через `DatabaseService.instance`.


### `lib/data/categories/category_cache_helpers.dart`

EN:

- **Purpose:** Category domain Brain module (coordinator or part).
- **Contains:** Category tree, fuzzy match, CRUD, stats bridge, default plan time. Symbols: CategoryCacheExtension.
- **Symbols:** `CategoryCacheExtension`
- **Does:** Hydrates category rules, maps category_id/category_link for records/plans.
- **Connected to:** Categories screen, plan cards, record category picker, stats aggregation.

RU:

- **Назначение:** Модуль Brain (данные/PocketBase).
- **Что внутри:** Логика домена, кэш, optimistic UI, outbox — см. англ. блок.
- **Что делает:** Единственный слой с HTTP к PocketBase (`database_service.dart` + parts).
- **Как связано с общей логикой:** UI вызывает через `DatabaseService.instance`.


### `lib/data/categories/category_crud.dart`

EN:

- **Purpose:** Category domain Brain module (coordinator or part).
- **Contains:** Category tree, fuzzy match, CRUD, stats bridge, default plan time. Symbols: CategoryCrudExtension.
- **Symbols:** `CategoryCrudExtension`
- **Does:** Hydrates category rules, maps category_id/category_link for records/plans.
- **Connected to:** Categories screen, plan cards, record category picker, stats aggregation.

RU:

- **Назначение:** Модуль Brain (данные/PocketBase).
- **Что внутри:** Логика домена, кэш, optimistic UI, outbox — см. англ. блок.
- **Что делает:** Единственный слой с HTTP к PocketBase (`database_service.dart` + parts).
- **Как связано с общей логикой:** UI вызывает через `DatabaseService.instance`.


### `lib/data/categories/category_default_time.dart`

EN:

- **Purpose:** Category domain Brain module (coordinator or part).
- **Contains:** Category tree, fuzzy match, CRUD, stats bridge, default plan time. Symbols: CategoryDefaultTimeExtension.
- **Symbols:** `CategoryDefaultTimeExtension`
- **Does:** Hydrates category rules, maps category_id/category_link for records/plans.
- **Connected to:** Categories screen, plan cards, record category picker, stats aggregation.

RU:

- **Назначение:** Модуль Brain (данные/PocketBase).
- **Что внутри:** Логика домена, кэш, optimistic UI, outbox — см. англ. блок.
- **Что делает:** Единственный слой с HTTP к PocketBase (`database_service.dart` + parts).
- **Как связано с общей логикой:** UI вызывает через `DatabaseService.instance`.


### `lib/data/categories/category_lookup.dart`

EN:

- **Purpose:** Category domain Brain module (coordinator or part).
- **Contains:** Category tree, fuzzy match, CRUD, stats bridge, default plan time. Symbols: CategoryLookupExtension.
- **Symbols:** `CategoryLookupExtension`
- **Does:** Hydrates category rules, maps category_id/category_link for records/plans.
- **Connected to:** Categories screen, plan cards, record category picker, stats aggregation.

RU:

- **Назначение:** Модуль Brain (данные/PocketBase).
- **Что внутри:** Логика домена, кэш, optimistic UI, outbox — см. англ. блок.
- **Что делает:** Единственный слой с HTTP к PocketBase (`database_service.dart` + parts).
- **Как связано с общей логикой:** UI вызывает через `DatabaseService.instance`.


### `lib/data/categories/category_record_bridge.dart`

EN:

- **Purpose:** Category domain Brain module (coordinator or part).
- **Contains:** Category tree, fuzzy match, CRUD, stats bridge, default plan time. Symbols: CategoryRecordBridgeExtension.
- **Symbols:** `CategoryRecordBridgeExtension`
- **Does:** Hydrates category rules, maps category_id/category_link for records/plans.
- **Connected to:** Categories screen, plan cards, record category picker, stats aggregation.

RU:

- **Назначение:** Модуль Brain (данные/PocketBase).
- **Что внутри:** Логика домена, кэш, optimistic UI, outbox — см. англ. блок.
- **Что делает:** Единственный слой с HTTP к PocketBase (`database_service.dart` + parts).
- **Как связано с общей логикой:** UI вызывает через `DatabaseService.instance`.


### `lib/data/categories/category_stats.dart`

EN:

- **Purpose:** Category domain Brain module (coordinator or part).
- **Contains:** Category tree, fuzzy match, CRUD, stats bridge, default plan time. Symbols: CategoryStatsExtension.
- **Symbols:** `CategoryStatsExtension`
- **Does:** Hydrates category rules, maps category_id/category_link for records/plans.
- **Connected to:** Categories screen, plan cards, record category picker, stats aggregation.

RU:

- **Назначение:** Модуль Brain (данные/PocketBase).
- **Что внутри:** Логика домена, кэш, optimistic UI, outbox — см. англ. блок.
- **Что делает:** Единственный слой с HTTP к PocketBase (`database_service.dart` + parts).
- **Как связано с общей логикой:** UI вызывает через `DatabaseService.instance`.


### `lib/data/categories/category_tree.dart`

EN:

- **Purpose:** Category domain Brain module (coordinator or part).
- **Contains:** Category tree, fuzzy match, CRUD, stats bridge, default plan time. Symbols: CategoryTreeExtension.
- **Symbols:** `CategoryTreeExtension`
- **Does:** Hydrates category rules, maps category_id/category_link for records/plans.
- **Connected to:** Categories screen, plan cards, record category picker, stats aggregation.

RU:

- **Назначение:** Модуль Brain (данные/PocketBase).
- **Что внутри:** Логика домена, кэш, optimistic UI, outbox — см. англ. блок.
- **Что делает:** Единственный слой с HTTP к PocketBase (`database_service.dart` + parts).
- **Как связано с общей логикой:** UI вызывает через `DatabaseService.instance`.


### `lib/data/category_fuzzy_match.dart`

EN:

- **Purpose:** Project file.
- **Contains:** Supporting source or config. Symbols: (library).
- **Does:** See path prefix for ownership.
- **Connected to:** Cross-check `docs/APP_STRUCTURE.md` layer rules.

RU:

- **Назначение:** Модуль Brain (данные/PocketBase).
- **Что внутри:** Логика домена, кэш, optimistic UI, outbox — см. англ. блок.
- **Что делает:** Единственный слой с HTTP к PocketBase (`database_service.dart` + parts).
- **Как связано с общей логикой:** UI вызывает через `DatabaseService.instance`.


### `lib/data/category_service.dart`

EN:

- **Purpose:** Category domain Brain module (coordinator or part).
- **Contains:** Category tree, fuzzy match, CRUD, stats bridge, default plan time. Symbols: CategoryServiceExtension.
- **Symbols:** `CategoryServiceExtension`
- **Does:** Hydrates category rules, maps category_id/category_link for records/plans.
- **Connected to:** Categories screen, plan cards, record category picker, stats aggregation.

RU:

- **Назначение:** Модуль Brain (данные/PocketBase).
- **Что внутри:** Логика домена, кэш, optimistic UI, outbox — см. англ. блок.
- **Что делает:** Единственный слой с HTTP к PocketBase (`database_service.dart` + parts).
- **Как связано с общей логикой:** UI вызывает через `DatabaseService.instance`.


### `lib/data/database_service.dart`

EN:

- **Purpose:** Brain library root singleton and `part` host for all PocketBase I/O extensions.
- **Contains:** Shared Brain state, streams, static helpers; `part` declarations for domain extensions. Symbols: _BuildNode.
- **Symbols:** `_BuildNode`
- **Does:** Owns HTTP/PocketBase access; coordinates optimistic cache and offline outboxes.
- **Connected to:** Imported by UI via `DatabaseService.instance`; domain logic lives in `part` files.

RU:

- **Назначение:** Модуль Brain (данные/PocketBase).
- **Что внутри:** Логика домена, кэш, optimistic UI, outbox — см. англ. блок.
- **Что делает:** Единственный слой с HTTP к PocketBase (`database_service.dart` + parts).
- **Как связано с общей логикой:** UI вызывает через `DatabaseService.instance`.


### `lib/data/db_core.dart`

EN:

- **Purpose:** Project file.
- **Contains:** Supporting source or config. Symbols: DbCoreExtension.
- **Symbols:** `DbCoreExtension`
- **Does:** See path prefix for ownership.
- **Connected to:** Cross-check `docs/APP_STRUCTURE.md` layer rules.

RU:

- **Назначение:** Модуль Brain (данные/PocketBase).
- **Что внутри:** Логика домена, кэш, optimistic UI, outbox — см. англ. блок.
- **Что делает:** Единственный слой с HTTP к PocketBase (`database_service.dart` + parts).
- **Как связано с общей логикой:** UI вызывает через `DatabaseService.instance`.


### `lib/data/local_sync/offline_sync_state.dart`

EN:

- **Purpose:** Offline mutation outbox or sync UI state.
- **Contains:** SharedPreferences queues + flush hooks. Symbols: OfflineSyncController.
- **Symbols:** `OfflineSyncController`
- **Does:** Enqueues retriable PB failures; drives offline banner and reconnect drain.
- **Connected to:** Brain flush on boot/resume/reconnect; `_OfflineSyncStatusBar` in shell.

RU:

- **Назначение:** Модуль Brain (данные/PocketBase).
- **Что внутри:** Логика домена, кэш, optimistic UI, outbox — см. англ. блок.
- **Что делает:** Единственный слой с HTTP к PocketBase (`database_service.dart` + parts).
- **Как связано с общей логикой:** UI вызывает через `DatabaseService.instance`.


### `lib/data/local_sync/plan_create_outbox.dart`

EN:

- **Purpose:** Offline mutation outbox or sync UI state.
- **Contains:** SharedPreferences queues + flush hooks. Symbols: (library).
- **Does:** Enqueues retriable PB failures; drives offline banner and reconnect drain.
- **Connected to:** Brain flush on boot/resume/reconnect; `_OfflineSyncStatusBar` in shell.

RU:

- **Назначение:** Модуль Brain (данные/PocketBase).
- **Что внутри:** Логика домена, кэш, optimistic UI, outbox — см. англ. блок.
- **Что делает:** Единственный слой с HTTP к PocketBase (`database_service.dart` + parts).
- **Как связано с общей логикой:** UI вызывает через `DatabaseService.instance`.


### `lib/data/local_sync/plan_mutation_outbox.dart`

EN:

- **Purpose:** Offline mutation outbox or sync UI state.
- **Contains:** SharedPreferences queues + flush hooks. Symbols: (library).
- **Does:** Enqueues retriable PB failures; drives offline banner and reconnect drain.
- **Connected to:** Brain flush on boot/resume/reconnect; `_OfflineSyncStatusBar` in shell.

RU:

- **Назначение:** Модуль Brain (данные/PocketBase).
- **Что внутри:** Логика домена, кэш, optimistic UI, outbox — см. англ. блок.
- **Что делает:** Единственный слой с HTTP к PocketBase (`database_service.dart` + parts).
- **Как связано с общей логикой:** UI вызывает через `DatabaseService.instance`.


### `lib/data/local_sync/record_mutation_outbox.dart`

EN:

- **Purpose:** Offline mutation outbox or sync UI state.
- **Contains:** SharedPreferences queues + flush hooks. Symbols: (library).
- **Does:** Enqueues retriable PB failures; drives offline banner and reconnect drain.
- **Connected to:** Brain flush on boot/resume/reconnect; `_OfflineSyncStatusBar` in shell.

RU:

- **Назначение:** Модуль Brain (данные/PocketBase).
- **Что внутри:** Логика домена, кэш, optimistic UI, outbox — см. англ. блок.
- **Что делает:** Единственный слой с HTTP к PocketBase (`database_service.dart` + parts).
- **Как связано с общей логикой:** UI вызывает через `DatabaseService.instance`.


### `lib/data/local_sync/sync_manager.dart`

EN:

- **Purpose:** Offline mutation outbox or sync UI state.
- **Contains:** SharedPreferences queues + flush hooks. Symbols: SyncManager.
- **Symbols:** `SyncManager`
- **Does:** Enqueues retriable PB failures; drives offline banner and reconnect drain.
- **Connected to:** Brain flush on boot/resume/reconnect; `_OfflineSyncStatusBar` in shell.

RU:

- **Назначение:** Модуль Brain (данные/PocketBase).
- **Что внутри:** Логика домена, кэш, optimistic UI, outbox — см. англ. блок.
- **Что делает:** Единственный слой с HTTP к PocketBase (`database_service.dart` + parts).
- **Как связано с общей логикой:** UI вызывает через `DatabaseService.instance`.


### `lib/data/models.dart`

EN:

- **Purpose:** Immutable data model or model barrel.
- **Contains:** DTOs aligned with `docs/DATA_MAP.md`. Symbols: (library).
- **Does:** Parse/serialize PocketBase fields; no UI imports.
- **Connected to:** Brain + UI read types via `package:counter/data/models.dart`.

RU:

- **Назначение:** Модуль Brain (данные/PocketBase).
- **Что внутри:** Логика домена, кэш, optimistic UI, outbox — см. англ. блок.
- **Что делает:** Единственный слой с HTTP к PocketBase (`database_service.dart` + parts).
- **Как связано с общей логикой:** UI вызывает через `DatabaseService.instance`.


### `lib/data/models/_shared.dart`

EN:

- **Purpose:** Immutable data model or model barrel.
- **Contains:** DTOs aligned with `docs/DATA_MAP.md`. Symbols: CategoryDisplayMode, CategoryDisplayModeWire.
- **Symbols:** `CategoryDisplayMode, CategoryDisplayModeWire`
- **Does:** Parse/serialize PocketBase fields; no UI imports.
- **Connected to:** Brain + UI read types via `package:counter/data/models.dart`.

RU:

- **Назначение:** Модуль Brain (данные/PocketBase).
- **Что внутри:** Логика домена, кэш, optimistic UI, outbox — см. англ. блок.
- **Что делает:** Единственный слой с HTTP к PocketBase (`database_service.dart` + parts).
- **Как связано с общей логикой:** UI вызывает через `DatabaseService.instance`.


### `lib/data/models/category.dart`

EN:

- **Purpose:** Immutable data model or model barrel.
- **Contains:** DTOs aligned with `docs/DATA_MAP.md`. Symbols: Category, CategoryNameInputKind, CategoryNameInputStatus.
- **Symbols:** `Category, CategoryNameInputKind, CategoryNameInputStatus`
- **Does:** Parse/serialize PocketBase fields; no UI imports.
- **Connected to:** Brain + UI read types via `package:counter/data/models.dart`.

RU:

- **Назначение:** Модуль Brain (данные/PocketBase).
- **Что внутри:** Логика домена, кэш, optimistic UI, outbox — см. англ. блок.
- **Что делает:** Единственный слой с HTTP к PocketBase (`database_service.dart` + parts).
- **Как связано с общей логикой:** UI вызывает через `DatabaseService.instance`.


### `lib/data/models/planning.dart`

EN:

- **Purpose:** Immutable data model or model barrel.
- **Contains:** DTOs aligned with `docs/DATA_MAP.md`. Symbols: AiParsedTaskHint, SourcePlanLinkSuggestion, PlanDayOverloadReport.
- **Symbols:** `AiParsedTaskHint, SourcePlanLinkSuggestion, PlanDayOverloadReport, PlanningTask`
- **Does:** Parse/serialize PocketBase fields; no UI imports.
- **Connected to:** Brain + UI read types via `package:counter/data/models.dart`.

RU:

- **Назначение:** Модуль Brain (данные/PocketBase).
- **Что внутри:** Логика домена, кэш, optimistic UI, outbox — см. англ. блок.
- **Что делает:** Единственный слой с HTTP к PocketBase (`database_service.dart` + parts).
- **Как связано с общей логикой:** UI вызывает через `DatabaseService.instance`.


### `lib/data/models/profile.dart`

EN:

- **Purpose:** Immutable data model or model barrel.
- **Contains:** DTOs aligned with `docs/DATA_MAP.md`. Symbols: Profile, UserProfile, ProfileUpdate.
- **Symbols:** `Profile, UserProfile, ProfileUpdate`
- **Does:** Parse/serialize PocketBase fields; no UI imports.
- **Connected to:** Brain + UI read types via `package:counter/data/models.dart`.

RU:

- **Назначение:** Модуль Brain (данные/PocketBase).
- **Что внутри:** Логика домена, кэш, optimistic UI, outbox — см. англ. блок.
- **Что делает:** Единственный слой с HTTP к PocketBase (`database_service.dart` + parts).
- **Как связано с общей логикой:** UI вызывает через `DatabaseService.instance`.


### `lib/data/models/record.dart`

EN:

- **Purpose:** Immutable data model or model barrel.
- **Contains:** DTOs aligned with `docs/DATA_MAP.md`. Symbols: Record.
- **Symbols:** `Record`
- **Does:** Parse/serialize PocketBase fields; no UI imports.
- **Connected to:** Brain + UI read types via `package:counter/data/models.dart`.

RU:

- **Назначение:** Модуль Brain (данные/PocketBase).
- **Что внутри:** Логика домена, кэш, optimistic UI, outbox — см. англ. блок.
- **Что делает:** Единственный слой с HTTP к PocketBase (`database_service.dart` + parts).
- **Как связано с общей логикой:** UI вызывает через `DatabaseService.instance`.


### `lib/data/models/stats.dart`

EN:

- **Purpose:** Immutable data model or model barrel.
- **Contains:** DTOs aligned with `docs/DATA_MAP.md`. Symbols: BasicDayStats, StatsTreeNode, SessionGroup.
- **Symbols:** `BasicDayStats, StatsTreeNode, SessionGroup, StatsNode`
- **Does:** Parse/serialize PocketBase fields; no UI imports.
- **Connected to:** Brain + UI read types via `package:counter/data/models.dart`.

RU:

- **Назначение:** Модуль Brain (данные/PocketBase).
- **Что внутри:** Логика домена, кэш, optimistic UI, outbox — см. англ. блок.
- **Что делает:** Единственный слой с HTTP к PocketBase (`database_service.dart` + parts).
- **Как связано с общей логикой:** UI вызывает через `DatabaseService.instance`.


### `lib/data/models/tag.dart`

EN:

- **Purpose:** Immutable data model or model barrel.
- **Contains:** DTOs aligned with `docs/DATA_MAP.md`. Symbols: Tag.
- **Symbols:** `Tag`
- **Does:** Parse/serialize PocketBase fields; no UI imports.
- **Connected to:** Brain + UI read types via `package:counter/data/models.dart`.

RU:

- **Назначение:** Модуль Brain (данные/PocketBase).
- **Что внутри:** Логика домена, кэш, optimistic UI, outbox — см. англ. блок.
- **Что делает:** Единственный слой с HTTP к PocketBase (`database_service.dart` + parts).
- **Как связано с общей логикой:** UI вызывает через `DatabaseService.instance`.


### `lib/data/pb_config.dart`

EN:

- **Purpose:** Project file.
- **Contains:** Supporting source or config. Symbols: (library).
- **Does:** See path prefix for ownership.
- **Connected to:** Cross-check `docs/APP_STRUCTURE.md` layer rules.

RU:

- **Назначение:** Модуль Brain (данные/PocketBase).
- **Что внутри:** Логика домена, кэш, optimistic UI, outbox — см. англ. блок.
- **Что делает:** Единственный слой с HTTP к PocketBase (`database_service.dart` + parts).
- **Как связано с общей логикой:** UI вызывает через `DatabaseService.instance`.


### `lib/data/plan_service.dart`

EN:

- **Purpose:** Plan/list domain Brain module (coordinator or part).
- **Contains:** Planning CRUD, Time View projection, recurrence, tags, outbox helpers. Symbols: (library).
- **Does:** Fetches/mutates `plans` rows, expands RRULE, queues offline plan mutations.
- **Connected to:** Used by Planning/Lists UI and shell voice routing through `DatabaseService`.

RU:

- **Назначение:** Модуль Brain (данные/PocketBase).
- **Что внутри:** Логика домена, кэш, optimistic UI, outbox — см. англ. блок.
- **Что делает:** Единственный слой с HTTP к PocketBase (`database_service.dart` + parts).
- **Как связано с общей логикой:** UI вызывает через `DatabaseService.instance`.


### `lib/data/plan_time_sequential_cascade.dart`

EN:

- **Purpose:** Project file.
- **Contains:** Supporting source or config. Symbols: PlanTimeSequentialCascadePatch.
- **Symbols:** `PlanTimeSequentialCascadePatch`
- **Does:** See path prefix for ownership.
- **Connected to:** Cross-check `docs/APP_STRUCTURE.md` layer rules.

RU:

- **Назначение:** Модуль Brain (данные/PocketBase).
- **Что внутри:** Логика домена, кэш, optimistic UI, outbox — см. англ. блок.
- **Что делает:** Единственный слой с HTTP к PocketBase (`database_service.dart` + parts).
- **Как связано с общей логикой:** UI вызывает через `DatabaseService.instance`.


### `lib/data/plans/plan_cache_helpers.dart`

EN:

- **Purpose:** Plan/list domain Brain module (coordinator or part).
- **Contains:** Planning CRUD, Time View projection, recurrence, tags, outbox helpers. Symbols: PlanCacheProjectionExtension.
- **Symbols:** `PlanCacheProjectionExtension`
- **Does:** Fetches/mutates `plans` rows, expands RRULE, queues offline plan mutations.
- **Connected to:** Used by Planning/Lists UI and shell voice routing through `DatabaseService`.

RU:

- **Назначение:** Модуль Brain (данные/PocketBase).
- **Что внутри:** Логика домена, кэш, optimistic UI, outbox — см. англ. блок.
- **Что делает:** Единственный слой с HTTP к PocketBase (`database_service.dart` + parts).
- **Как связано с общей логикой:** UI вызывает через `DatabaseService.instance`.


### `lib/data/plans/plan_outbox_helpers.dart`

EN:

- **Purpose:** Plan/list domain Brain module (coordinator or part).
- **Contains:** Planning CRUD, Time View projection, recurrence, tags, outbox helpers. Symbols: PlanOutboxSyncExtension.
- **Symbols:** `PlanOutboxSyncExtension`
- **Does:** Fetches/mutates `plans` rows, expands RRULE, queues offline plan mutations.
- **Connected to:** Used by Planning/Lists UI and shell voice routing through `DatabaseService`.

RU:

- **Назначение:** Модуль Brain (данные/PocketBase).
- **Что внутри:** Логика домена, кэш, optimistic UI, outbox — см. англ. блок.
- **Что делает:** Единственный слой с HTTP к PocketBase (`database_service.dart` + parts).
- **Как связано с общей логикой:** UI вызывает через `DatabaseService.instance`.


### `lib/data/plans/plan_projection_types.dart`

EN:

- **Purpose:** Plan/list domain Brain module (coordinator or part).
- **Contains:** Planning CRUD, Time View projection, recurrence, tags, outbox helpers. Symbols: TimeModeProjectedPlan, PlanTimeModeProjection.
- **Symbols:** `TimeModeProjectedPlan, PlanTimeModeProjection`
- **Does:** Fetches/mutates `plans` rows, expands RRULE, queues offline plan mutations.
- **Connected to:** Used by Planning/Lists UI and shell voice routing through `DatabaseService`.

RU:

- **Назначение:** Модуль Brain (данные/PocketBase).
- **Что внутри:** Логика домена, кэш, optimistic UI, outbox — см. англ. блок.
- **Что делает:** Единственный слой с HTTP к PocketBase (`database_service.dart` + parts).
- **Как связано с общей логикой:** UI вызывает через `DatabaseService.instance`.


### `lib/data/plans/plan_recurrence_helpers.dart`

EN:

- **Purpose:** Plan/list domain Brain module (coordinator or part).
- **Contains:** Planning CRUD, Time View projection, recurrence, tags, outbox helpers. Symbols: PlanRecurrenceExtension.
- **Symbols:** `PlanRecurrenceExtension`
- **Does:** Fetches/mutates `plans` rows, expands RRULE, queues offline plan mutations.
- **Connected to:** Used by Planning/Lists UI and shell voice routing through `DatabaseService`.

RU:

- **Назначение:** Модуль Brain (данные/PocketBase).
- **Что внутри:** Логика домена, кэш, optimistic UI, outbox — см. англ. блок.
- **Что делает:** Единственный слой с HTTP к PocketBase (`database_service.dart` + parts).
- **Как связано с общей логикой:** UI вызывает через `DatabaseService.instance`.


### `lib/data/plans/plan_tags_helpers.dart`

EN:

- **Purpose:** Plan/list domain Brain module (coordinator or part).
- **Contains:** Planning CRUD, Time View projection, recurrence, tags, outbox helpers. Symbols: PlanTagsExtension.
- **Symbols:** `PlanTagsExtension`
- **Does:** Fetches/mutates `plans` rows, expands RRULE, queues offline plan mutations.
- **Connected to:** Used by Planning/Lists UI and shell voice routing through `DatabaseService`.

RU:

- **Назначение:** Модуль Brain (данные/PocketBase).
- **Что внутри:** Логика домена, кэш, optimistic UI, outbox — см. англ. блок.
- **Что делает:** Единственный слой с HTTP к PocketBase (`database_service.dart` + parts).
- **Как связано с общей логикой:** UI вызывает через `DatabaseService.instance`.


### `lib/data/plans/plan_time_cascade_helpers.dart`

EN:

- **Purpose:** Plan/list domain Brain module (coordinator or part).
- **Contains:** Planning CRUD, Time View projection, recurrence, tags, outbox helpers. Symbols: PlanTimeCascadeExtension.
- **Symbols:** `PlanTimeCascadeExtension`
- **Does:** Fetches/mutates `plans` rows, expands RRULE, queues offline plan mutations.
- **Connected to:** Used by Planning/Lists UI and shell voice routing through `DatabaseService`.

RU:

- **Назначение:** Модуль Brain (данные/PocketBase).
- **Что внутри:** Логика домена, кэш, optimistic UI, outbox — см. англ. блок.
- **Что делает:** Единственный слой с HTTP к PocketBase (`database_service.dart` + parts).
- **Как связано с общей логикой:** UI вызывает через `DatabaseService.instance`.


### `lib/data/price_reporter_client_match.dart`

EN:

- **Purpose:** Project file.
- **Contains:** Supporting source or config. Symbols: PriceReporterAliasEntry, PriceReporterClientMatchResult, PriceReporterCategoryNode.
- **Symbols:** `PriceReporterAliasEntry, PriceReporterClientMatchResult, PriceReporterCategoryNode`
- **Does:** See path prefix for ownership.
- **Connected to:** Cross-check `docs/APP_STRUCTURE.md` layer rules.

RU:

- **Назначение:** Модуль Brain (данные/PocketBase).
- **Что внутри:** Логика домена, кэш, optimistic UI, outbox — см. англ. блок.
- **Что делает:** Единственный слой с HTTP к PocketBase (`database_service.dart` + parts).
- **Как связано с общей логикой:** UI вызывает через `DatabaseService.instance`.


### `lib/data/profile/profile_admin.dart`

EN:

- **Purpose:** Profile/tag catalog Brain module (coordinator or part).
- **Contains:** Profile hydration, settings PATCH, timezone, tag catalog CRUD. Symbols: (library).
- **Does:** Loads `profiles` row, projects wall-clock today, manages tag catalog cache.
- **Connected to:** Profile settings, tag manager, tag chips, timezone header picker.

RU:

- **Назначение:** Модуль Brain (данные/PocketBase).
- **Что внутри:** Логика домена, кэш, optimistic UI, outbox — см. англ. блок.
- **Что делает:** Единственный слой с HTTP к PocketBase (`database_service.dart` + parts).
- **Как связано с общей логикой:** UI вызывает через `DatabaseService.instance`.


### `lib/data/profile/profile_cache_helpers.dart`

EN:

- **Purpose:** Profile/tag catalog Brain module (coordinator or part).
- **Contains:** Profile hydration, settings PATCH, timezone, tag catalog CRUD. Symbols: ProfileCacheExtension.
- **Symbols:** `ProfileCacheExtension`
- **Does:** Loads `profiles` row, projects wall-clock today, manages tag catalog cache.
- **Connected to:** Profile settings, tag manager, tag chips, timezone header picker.

RU:

- **Назначение:** Модуль Brain (данные/PocketBase).
- **Что внутри:** Логика домена, кэш, optimistic UI, outbox — см. англ. блок.
- **Что делает:** Единственный слой с HTTP к PocketBase (`database_service.dart` + parts).
- **Как связано с общей логикой:** UI вызывает через `DatabaseService.instance`.


### `lib/data/profile/profile_hydration.dart`

EN:

- **Purpose:** Profile/tag catalog Brain module (coordinator or part).
- **Contains:** Profile hydration, settings PATCH, timezone, tag catalog CRUD. Symbols: ProfileHydrationExtension.
- **Symbols:** `ProfileHydrationExtension`
- **Does:** Loads `profiles` row, projects wall-clock today, manages tag catalog cache.
- **Connected to:** Profile settings, tag manager, tag chips, timezone header picker.

RU:

- **Назначение:** Модуль Brain (данные/PocketBase).
- **Что внутри:** Логика домена, кэш, optimistic UI, outbox — см. англ. блок.
- **Что делает:** Единственный слой с HTTP к PocketBase (`database_service.dart` + parts).
- **Как связано с общей логикой:** UI вызывает через `DatabaseService.instance`.


### `lib/data/profile/profile_preferences.dart`

EN:

- **Purpose:** Profile/tag catalog Brain module (coordinator or part).
- **Contains:** Profile hydration, settings PATCH, timezone, tag catalog CRUD. Symbols: ProfilePreferencesExtension.
- **Symbols:** `ProfilePreferencesExtension`
- **Does:** Loads `profiles` row, projects wall-clock today, manages tag catalog cache.
- **Connected to:** Profile settings, tag manager, tag chips, timezone header picker.

RU:

- **Назначение:** Модуль Brain (данные/PocketBase).
- **Что внутри:** Логика домена, кэш, optimistic UI, outbox — см. англ. блок.
- **Что делает:** Единственный слой с HTTP к PocketBase (`database_service.dart` + parts).
- **Как связано с общей логикой:** UI вызывает через `DatabaseService.instance`.


### `lib/data/profile/profile_settings.dart`

EN:

- **Purpose:** Profile/tag catalog Brain module (coordinator or part).
- **Contains:** Profile hydration, settings PATCH, timezone, tag catalog CRUD. Symbols: ProfileSettingsExtension.
- **Symbols:** `ProfileSettingsExtension`
- **Does:** Loads `profiles` row, projects wall-clock today, manages tag catalog cache.
- **Connected to:** Profile settings, tag manager, tag chips, timezone header picker.

RU:

- **Назначение:** Модуль Brain (данные/PocketBase).
- **Что внутри:** Логика домена, кэш, optimistic UI, outbox — см. англ. блок.
- **Что делает:** Единственный слой с HTTP к PocketBase (`database_service.dart` + parts).
- **Как связано с общей логикой:** UI вызывает через `DatabaseService.instance`.


### `lib/data/profile/profile_timezone.dart`

EN:

- **Purpose:** Profile/tag catalog Brain module (coordinator or part).
- **Contains:** Profile hydration, settings PATCH, timezone, tag catalog CRUD. Symbols: ProfileTimezoneExtension.
- **Symbols:** `ProfileTimezoneExtension`
- **Does:** Loads `profiles` row, projects wall-clock today, manages tag catalog cache.
- **Connected to:** Profile settings, tag manager, tag chips, timezone header picker.

RU:

- **Назначение:** Модуль Brain (данные/PocketBase).
- **Что внутри:** Логика домена, кэш, optimistic UI, outbox — см. англ. блок.
- **Что делает:** Единственный слой с HTTP к PocketBase (`database_service.dart` + parts).
- **Как связано с общей логикой:** UI вызывает через `DatabaseService.instance`.


### `lib/data/profile/tag_catalog.dart`

EN:

- **Purpose:** Profile/tag catalog Brain module (coordinator or part).
- **Contains:** Profile hydration, settings PATCH, timezone, tag catalog CRUD. Symbols: TagCatalogExtension.
- **Symbols:** `TagCatalogExtension`
- **Does:** Loads `profiles` row, projects wall-clock today, manages tag catalog cache.
- **Connected to:** Profile settings, tag manager, tag chips, timezone header picker.

RU:

- **Назначение:** Модуль Brain (данные/PocketBase).
- **Что внутри:** Логика домена, кэш, optimistic UI, outbox — см. англ. блок.
- **Что делает:** Единственный слой с HTTP к PocketBase (`database_service.dart` + parts).
- **Как связано с общей логикой:** UI вызывает через `DatabaseService.instance`.


### `lib/data/profile/tag_display_settings.dart`

EN:

- **Purpose:** Profile/tag catalog Brain module (coordinator or part).
- **Contains:** Profile hydration, settings PATCH, timezone, tag catalog CRUD. Symbols: TagDisplaySettingsExtension.
- **Symbols:** `TagDisplaySettingsExtension`
- **Does:** Loads `profiles` row, projects wall-clock today, manages tag catalog cache.
- **Connected to:** Profile settings, tag manager, tag chips, timezone header picker.

RU:

- **Назначение:** Модуль Brain (данные/PocketBase).
- **Что внутри:** Логика домена, кэш, optimistic UI, outbox — см. англ. блок.
- **Что делает:** Единственный слой с HTTP к PocketBase (`database_service.dart` + parts).
- **Как связано с общей логикой:** UI вызывает через `DatabaseService.instance`.


### `lib/data/profile_service.dart`

EN:

- **Purpose:** Profile/tag catalog Brain module (coordinator or part).
- **Contains:** Profile hydration, settings PATCH, timezone, tag catalog CRUD. Symbols: _ProfileFetchFailedException, ProfileServiceExtension.
- **Symbols:** `_ProfileFetchFailedException, ProfileServiceExtension`
- **Does:** Loads `profiles` row, projects wall-clock today, manages tag catalog cache.
- **Connected to:** Profile settings, tag manager, tag chips, timezone header picker.

RU:

- **Назначение:** Модуль Brain (данные/PocketBase).
- **Что внутри:** Логика домена, кэш, optimistic UI, outbox — см. англ. блок.
- **Что делает:** Единственный слой с HTTP к PocketBase (`database_service.dart` + parts).
- **Как связано с общей логикой:** UI вызывает через `DatabaseService.instance`.


### `lib/data/record_service.dart`

EN:

- **Purpose:** Record/timeline domain Brain module (coordinator or part).
- **Contains:** Start/stop records, timeline VM, realtime, overlap/Highlander, outbox. Symbols: RecordServiceExtension.
- **Symbols:** `RecordServiceExtension`
- **Does:** Maintains record cache, optimistic start/stop, PocketBase records I/O.
- **Connected to:** Timeline tab, edit sheets, Wear lite load path.

RU:

- **Назначение:** Модуль Brain (данные/PocketBase).
- **Что внутри:** Логика домена, кэш, optimistic UI, outbox — см. англ. блок.
- **Что делает:** Единственный слой с HTTP к PocketBase (`database_service.dart` + parts).
- **Как связано с общей логикой:** UI вызывает через `DatabaseService.instance`.


### `lib/data/records/record_cache_helpers.dart`

EN:

- **Purpose:** Record/timeline domain Brain module (coordinator or part).
- **Contains:** Start/stop records, timeline VM, realtime, overlap/Highlander, outbox. Symbols: RecordCacheProjectionExtension.
- **Symbols:** `RecordCacheProjectionExtension`
- **Does:** Maintains record cache, optimistic start/stop, PocketBase records I/O.
- **Connected to:** Timeline tab, edit sheets, Wear lite load path.

RU:

- **Назначение:** Модуль Brain (данные/PocketBase).
- **Что внутри:** Логика домена, кэш, optimistic UI, outbox — см. англ. блок.
- **Что делает:** Единственный слой с HTTP к PocketBase (`database_service.dart` + parts).
- **Как связано с общей логикой:** UI вызывает через `DatabaseService.instance`.


### `lib/data/records/record_crud.dart`

EN:

- **Purpose:** Record/timeline domain Brain module (coordinator or part).
- **Contains:** Start/stop records, timeline VM, realtime, overlap/Highlander, outbox. Symbols: RecordCrudExtension.
- **Symbols:** `RecordCrudExtension`
- **Does:** Maintains record cache, optimistic start/stop, PocketBase records I/O.
- **Connected to:** Timeline tab, edit sheets, Wear lite load path.

RU:

- **Назначение:** Модуль Brain (данные/PocketBase).
- **Что внутри:** Логика домена, кэш, optimistic UI, outbox — см. англ. блок.
- **Что делает:** Единственный слой с HTTP к PocketBase (`database_service.dart` + parts).
- **Как связано с общей логикой:** UI вызывает через `DatabaseService.instance`.


### `lib/data/records/record_ghost_cleanup.dart`

EN:

- **Purpose:** Record/timeline domain Brain module (coordinator or part).
- **Contains:** Start/stop records, timeline VM, realtime, overlap/Highlander, outbox. Symbols: RecordGhostCleanupExtension.
- **Symbols:** `RecordGhostCleanupExtension`
- **Does:** Maintains record cache, optimistic start/stop, PocketBase records I/O.
- **Connected to:** Timeline tab, edit sheets, Wear lite load path.

RU:

- **Назначение:** Модуль Brain (данные/PocketBase).
- **Что внутри:** Логика домена, кэш, optimistic UI, outbox — см. англ. блок.
- **Что делает:** Единственный слой с HTTP к PocketBase (`database_service.dart` + parts).
- **Как связано с общей логикой:** UI вызывает через `DatabaseService.instance`.


### `lib/data/records/record_optimistic.dart`

EN:

- **Purpose:** Record/timeline domain Brain module (coordinator or part).
- **Contains:** Start/stop records, timeline VM, realtime, overlap/Highlander, outbox. Symbols: RecordOptimisticExtension.
- **Symbols:** `RecordOptimisticExtension`
- **Does:** Maintains record cache, optimistic start/stop, PocketBase records I/O.
- **Connected to:** Timeline tab, edit sheets, Wear lite load path.

RU:

- **Назначение:** Модуль Brain (данные/PocketBase).
- **Что внутри:** Логика домена, кэш, optimistic UI, outbox — см. англ. блок.
- **Что делает:** Единственный слой с HTTP к PocketBase (`database_service.dart` + parts).
- **Как связано с общей логикой:** UI вызывает через `DatabaseService.instance`.


### `lib/data/records/record_outbox_helpers.dart`

EN:

- **Purpose:** Record/timeline domain Brain module (coordinator or part).
- **Contains:** Start/stop records, timeline VM, realtime, overlap/Highlander, outbox. Symbols: RecordOutboxSyncExtension.
- **Symbols:** `RecordOutboxSyncExtension`
- **Does:** Maintains record cache, optimistic start/stop, PocketBase records I/O.
- **Connected to:** Timeline tab, edit sheets, Wear lite load path.

RU:

- **Назначение:** Модуль Brain (данные/PocketBase).
- **Что внутри:** Логика домена, кэш, optimistic UI, outbox — см. англ. блок.
- **Что делает:** Единственный слой с HTTP к PocketBase (`database_service.dart` + parts).
- **Как связано с общей логикой:** UI вызывает через `DatabaseService.instance`.


### `lib/data/records/record_overlap_helpers.dart`

EN:

- **Purpose:** Record/timeline domain Brain module (coordinator or part).
- **Contains:** Start/stop records, timeline VM, realtime, overlap/Highlander, outbox. Symbols: RecordOverlapExtension.
- **Symbols:** `RecordOverlapExtension`
- **Does:** Maintains record cache, optimistic start/stop, PocketBase records I/O.
- **Connected to:** Timeline tab, edit sheets, Wear lite load path.

RU:

- **Назначение:** Модуль Brain (данные/PocketBase).
- **Что внутри:** Логика домена, кэш, optimistic UI, outbox — см. англ. блок.
- **Что делает:** Единственный слой с HTTP к PocketBase (`database_service.dart` + parts).
- **Как связано с общей логикой:** UI вызывает через `DatabaseService.instance`.


### `lib/data/records/record_realtime.dart`

EN:

- **Purpose:** Record/timeline domain Brain module (coordinator or part).
- **Contains:** Start/stop records, timeline VM, realtime, overlap/Highlander, outbox. Symbols: RecordRealtimeExtension.
- **Symbols:** `RecordRealtimeExtension`
- **Does:** Maintains record cache, optimistic start/stop, PocketBase records I/O.
- **Connected to:** Timeline tab, edit sheets, Wear lite load path.

RU:

- **Назначение:** Модуль Brain (данные/PocketBase).
- **Что внутри:** Логика домена, кэш, optimistic UI, outbox — см. англ. блок.
- **Что делает:** Единственный слой с HTTP к PocketBase (`database_service.dart` + parts).
- **Как связано с общей логикой:** UI вызывает через `DatabaseService.instance`.


### `lib/data/records/record_timeline_vm.dart`

EN:

- **Purpose:** Record/timeline domain Brain module (coordinator or part).
- **Contains:** Start/stop records, timeline VM, realtime, overlap/Highlander, outbox. Symbols: RecordTimelineVmExtension.
- **Symbols:** `RecordTimelineVmExtension`
- **Does:** Maintains record cache, optimistic start/stop, PocketBase records I/O.
- **Connected to:** Timeline tab, edit sheets, Wear lite load path.

RU:

- **Назначение:** Модуль Brain (данные/PocketBase).
- **Что внутри:** Логика домена, кэш, optimistic UI, outbox — см. англ. блок.
- **Что делает:** Единственный слой с HTTP к PocketBase (`database_service.dart` + parts).
- **Как связано с общей логикой:** UI вызывает через `DatabaseService.instance`.


### `lib/data/recurrence_edit_scope.dart`

EN:

- **Purpose:** Project file.
- **Contains:** Supporting source or config. Symbols: RecurrenceEditScope.
- **Symbols:** `RecurrenceEditScope`
- **Does:** See path prefix for ownership.
- **Connected to:** Cross-check `docs/APP_STRUCTURE.md` layer rules.

RU:

- **Назначение:** Модуль Brain (данные/PocketBase).
- **Что внутри:** Логика домена, кэш, optimistic UI, outbox — см. англ. блок.
- **Что делает:** Единственный слой с HTTP к PocketBase (`database_service.dart` + parts).
- **Как связано с общей логикой:** UI вызывает через `DatabaseService.instance`.


### `lib/data/smart_input_parser.dart`

EN:

- **Purpose:** Project file.
- **Contains:** Supporting source or config. Symbols: SmartTimeParseResult, SmartTimeRangeParseResult.
- **Symbols:** `SmartTimeParseResult, SmartTimeRangeParseResult`
- **Does:** See path prefix for ownership.
- **Connected to:** Cross-check `docs/APP_STRUCTURE.md` layer rules.

RU:

- **Назначение:** Модуль Brain (данные/PocketBase).
- **Что внутри:** Логика домена, кэш, optimistic UI, outbox — см. англ. блок.
- **Что делает:** Единственный слой с HTTP к PocketBase (`database_service.dart` + parts).
- **Как связано с общей логикой:** UI вызывает через `DatabaseService.instance`.


### `lib/data/time_view_fixed_time_policy.dart`

EN:

- **Purpose:** Project file.
- **Contains:** Supporting source or config. Symbols: TimeViewFixedTagPrefs.
- **Symbols:** `TimeViewFixedTagPrefs`
- **Does:** See path prefix for ownership.
- **Connected to:** Cross-check `docs/APP_STRUCTURE.md` layer rules.

RU:

- **Назначение:** Модуль Brain (данные/PocketBase).
- **Что внутри:** Логика домена, кэш, optimistic UI, outbox — см. англ. блок.
- **Что делает:** Единственный слой с HTTP к PocketBase (`database_service.dart` + parts).
- **Как связано с общей логикой:** UI вызывает через `DatabaseService.instance`.


### `lib/data/voice_audio_stub.dart`

EN:

- **Purpose:** Project file.
- **Contains:** Supporting source or config. Symbols: (library).
- **Does:** See path prefix for ownership.
- **Connected to:** Cross-check `docs/APP_STRUCTURE.md` layer rules.

RU:

- **Назначение:** Модуль Brain (данные/PocketBase).
- **Что внутри:** Логика домена, кэш, optimistic UI, outbox — см. англ. блок.
- **Что делает:** Единственный слой с HTTP к PocketBase (`database_service.dart` + parts).
- **Как связано с общей логикой:** UI вызывает через `DatabaseService.instance`.


### `lib/data/voice_audio_web.dart`

EN:

- **Purpose:** Project file.
- **Contains:** Supporting source or config. Symbols: (library).
- **Does:** See path prefix for ownership.
- **Connected to:** Cross-check `docs/APP_STRUCTURE.md` layer rules.

RU:

- **Назначение:** Модуль Brain (данные/PocketBase).
- **Что внутри:** Логика домена, кэш, optimistic UI, outbox — см. англ. блок.
- **Что делает:** Единственный слой с HTTP к PocketBase (`database_service.dart` + parts).
- **Как связано с общей логикой:** UI вызывает через `DatabaseService.instance`.


### `lib/data/voice_command_parser.dart`

EN:

- **Purpose:** Project file.
- **Contains:** Supporting source or config. Symbols: VoiceCommandMatchConfidence, VoiceCommandParseResult, VoiceCommandCategoryCandidate.
- **Symbols:** `VoiceCommandMatchConfidence, VoiceCommandParseResult, VoiceCommandCategoryCandidate, VoiceCommandCategoryIndex`
- **Does:** See path prefix for ownership.
- **Connected to:** Cross-check `docs/APP_STRUCTURE.md` layer rules.

RU:

- **Назначение:** Модуль Brain (данные/PocketBase).
- **Что внутри:** Логика домена, кэш, optimistic UI, outbox — см. англ. блок.
- **Что делает:** Единственный слой с HTTP к PocketBase (`database_service.dart` + parts).
- **Как связано с общей логикой:** UI вызывает через `DatabaseService.instance`.


### `lib/data/web_history.dart`

EN:

- **Purpose:** Project file.
- **Contains:** Supporting source or config. Symbols: (library).
- **Does:** See path prefix for ownership.
- **Connected to:** Cross-check `docs/APP_STRUCTURE.md` layer rules.

RU:

- **Назначение:** Модуль Brain (данные/PocketBase).
- **Что внутри:** Логика домена, кэш, optimistic UI, outbox — см. англ. блок.
- **Что делает:** Единственный слой с HTTP к PocketBase (`database_service.dart` + parts).
- **Как связано с общей логикой:** UI вызывает через `DatabaseService.instance`.


### `lib/data/web_history_stub.dart`

EN:

- **Purpose:** Project file.
- **Contains:** Supporting source or config. Symbols: (library).
- **Does:** See path prefix for ownership.
- **Connected to:** Cross-check `docs/APP_STRUCTURE.md` layer rules.

RU:

- **Назначение:** Модуль Brain (данные/PocketBase).
- **Что внутри:** Логика домена, кэш, optimistic UI, outbox — см. англ. блок.
- **Что делает:** Единственный слой с HTTP к PocketBase (`database_service.dart` + parts).
- **Как связано с общей логикой:** UI вызывает через `DatabaseService.instance`.


### `lib/data/web_history_web.dart`

EN:

- **Purpose:** Project file.
- **Contains:** Supporting source or config. Symbols: (library).
- **Does:** See path prefix for ownership.
- **Connected to:** Cross-check `docs/APP_STRUCTURE.md` layer rules.

RU:

- **Назначение:** Модуль Brain (данные/PocketBase).
- **Что внутри:** Логика домена, кэш, optimistic UI, outbox — см. англ. блок.
- **Что делает:** Единственный слой с HTTP к PocketBase (`database_service.dart` + parts).
- **Как связано с общей логикой:** UI вызывает через `DatabaseService.instance`.


### `lib/features/auth/auth_screen.dart`

EN:

- **Purpose:** Project file.
- **Contains:** Supporting source or config. Symbols: AuthScreen.
- **Symbols:** `AuthScreen`
- **Does:** See path prefix for ownership.
- **Connected to:** Cross-check `docs/APP_STRUCTURE.md` layer rules.

RU:

- **Назначение:** UI-модуль фичи.
- **Что внутри:** Экраны/листы/шиты без прямого HTTP.
- **Что делает:** Optimistic UI через Brain; соблюдает `UX_CONTRACT`.
- **Как связано с общей логикой:** Подключается из shell или соседних shared-компонентов.


### `lib/features/auth/auth_view.dart`

EN:

- **Purpose:** Project file.
- **Contains:** Supporting source or config. Symbols: AuthView, _AuthViewState.
- **Symbols:** `AuthView, _AuthViewState`
- **Does:** See path prefix for ownership.
- **Connected to:** Cross-check `docs/APP_STRUCTURE.md` layer rules.

RU:

- **Назначение:** UI-модуль фичи.
- **Что внутри:** Экраны/листы/шиты без прямого HTTP.
- **Что делает:** Optimistic UI через Brain; соблюдает `UX_CONTRACT`.
- **Как связано с общей логикой:** Подключается из shell или соседних shared-компонентов.


### `lib/features/auth/oauth_session.dart`

EN:

- **Purpose:** Project file.
- **Contains:** Supporting source or config. Symbols: OAuthUser, OAuthSession.
- **Symbols:** `OAuthUser, OAuthSession`
- **Does:** See path prefix for ownership.
- **Connected to:** Cross-check `docs/APP_STRUCTURE.md` layer rules.

RU:

- **Назначение:** UI-модуль фичи.
- **Что внутри:** Экраны/листы/шиты без прямого HTTP.
- **Что делает:** Optimistic UI через Brain; соблюдает `UX_CONTRACT`.
- **Как связано с общей логикой:** Подключается из shell или соседних shared-компонентов.


### `lib/features/calendar/calendar_view.dart`

EN:

- **Purpose:** Project file.
- **Contains:** Supporting source or config. Symbols: _CalendarViewMode, CalendarView, _CalendarViewState.
- **Symbols:** `_CalendarViewMode, CalendarView, _CalendarViewState`
- **Does:** See path prefix for ownership.
- **Connected to:** Cross-check `docs/APP_STRUCTURE.md` layer rules.

RU:

- **Назначение:** UI-модуль фичи.
- **Что внутри:** Экраны/листы/шиты без прямого HTTP.
- **Что делает:** Optimistic UI через Brain; соблюдает `UX_CONTRACT`.
- **Как связано с общей логикой:** Подключается из shell или соседних shared-компонентов.


### `lib/features/categories/category_list_view.dart`

EN:

- **Purpose:** Category manager UI.
- **Contains:** Tree picker, create/edit sheets, visibility prefs. Symbols: _CategoryDepthLayout.
- **Symbols:** `_CategoryDepthLayout`
- **Does:** CRUD UI calling Brain category extensions.
- **Connected to:** More menu → Categories.

RU:

- **Назначение:** UI-модуль фичи.
- **Что внутри:** Экраны/листы/шиты без прямого HTTP.
- **Что делает:** Optimistic UI через Brain; соблюдает `UX_CONTRACT`.
- **Как связано с общей логикой:** Подключается из shell или соседних shared-компонентов.


### `lib/features/categories/category_recursive_tree.dart`

EN:

- **Purpose:** Category manager UI.
- **Contains:** Tree picker, create/edit sheets, visibility prefs. Symbols: CategoryTreeSheetPicked, CategoryTreeSheetAll.
- **Symbols:** `CategoryTreeSheetPicked, CategoryTreeSheetAll`
- **Does:** CRUD UI calling Brain category extensions.
- **Connected to:** More menu → Categories.

RU:

- **Назначение:** UI-модуль фичи.
- **Что внутри:** Экраны/листы/шиты без прямого HTTP.
- **Что делает:** Optimistic UI через Brain; соблюдает `UX_CONTRACT`.
- **Как связано с общей логикой:** Подключается из shell или соседних shared-компонентов.


### `lib/features/categories/category_visibility_prefs.dart`

EN:

- **Purpose:** Category manager UI.
- **Contains:** Tree picker, create/edit sheets, visibility prefs. Symbols: CategoryVisibilityPrefs.
- **Symbols:** `CategoryVisibilityPrefs`
- **Does:** CRUD UI calling Brain category extensions.
- **Connected to:** More menu → Categories.

RU:

- **Назначение:** UI-модуль фичи.
- **Что внутри:** Экраны/листы/шиты без прямого HTTP.
- **Что делает:** Optimistic UI через Brain; соблюдает `UX_CONTRACT`.
- **Как связано с общей логикой:** Подключается из shell или соседних shared-компонентов.


### `lib/features/categories/create_category_dialog.dart`

EN:

- **Purpose:** Category manager UI.
- **Contains:** Tree picker, create/edit sheets, visibility prefs. Symbols: _CreateCategoryDialog, _CreateCategoryDialogState.
- **Symbols:** `_CreateCategoryDialog, _CreateCategoryDialogState`
- **Does:** CRUD UI calling Brain category extensions.
- **Connected to:** More menu → Categories.

RU:

- **Назначение:** UI-модуль фичи.
- **Что внутри:** Экраны/листы/шиты без прямого HTTP.
- **Что делает:** Optimistic UI через Brain; соблюдает `UX_CONTRACT`.
- **Как связано с общей логикой:** Подключается из shell или соседних shared-компонентов.


### `lib/features/dev/component_lab_cards_demo.dart`

EN:

- **Purpose:** Project file.
- **Contains:** Supporting source or config. Symbols: ComponentLabPlanCardsDemo.
- **Symbols:** `ComponentLabPlanCardsDemo`
- **Does:** See path prefix for ownership.
- **Connected to:** Cross-check `docs/APP_STRUCTURE.md` layer rules.

RU:

- **Назначение:** UI-модуль фичи.
- **Что внутри:** Экраны/листы/шиты без прямого HTTP.
- **Что делает:** Optimistic UI через Brain; соблюдает `UX_CONTRACT`.
- **Как связано с общей логикой:** Подключается из shell или соседних shared-компонентов.


### `lib/features/dev/component_lab_view.dart`

EN:

- **Purpose:** Project file.
- **Contains:** Supporting source or config. Symbols: ComponentLabPage, _LabSection, _ButtonsDemo.
- **Symbols:** `ComponentLabPage, _LabSection, _ButtonsDemo`
- **Does:** See path prefix for ownership.
- **Connected to:** Cross-check `docs/APP_STRUCTURE.md` layer rules.

RU:

- **Назначение:** UI-модуль фичи.
- **Что внутри:** Экраны/листы/шиты без прямого HTTP.
- **Что делает:** Optimistic UI через Brain; соблюдает `UX_CONTRACT`.
- **Как связано с общей логикой:** Подключается из shell или соседних shared-компонентов.


### `lib/features/lists/lists_bulk_actions.dart`

EN:

- **Purpose:** Lists/backlog tab UI module.
- **Contains:** Filters, cards, bulk actions, export. Symbols: ListsBulkSelectModeBar, ListsBulkBottomBar.
- **Symbols:** `ListsBulkSelectModeBar, ListsBulkBottomBar`
- **Does:** Backlog plan rows with list-domain tags; no play button on cards.
- **Connected to:** Shell tab index 3.

RU:

- **Назначение:** UI-модуль фичи.
- **Что внутри:** Экраны/листы/шиты без прямого HTTP.
- **Что делает:** Optimistic UI через Brain; соблюдает `UX_CONTRACT`.
- **Как связано с общей логикой:** Подключается из shell или соседних shared-компонентов.


### `lib/features/lists/lists_card.dart`

EN:

- **Purpose:** Lists/backlog tab UI module.
- **Contains:** Filters, cards, bulk actions, export. Symbols: ListsQuadraticChip, BacklogPlanCard.
- **Symbols:** `ListsQuadraticChip, BacklogPlanCard`
- **Does:** Backlog plan rows with list-domain tags; no play button on cards.
- **Connected to:** Shell tab index 3.

RU:

- **Назначение:** UI-модуль фичи.
- **Что внутри:** Экраны/листы/шиты без прямого HTTP.
- **Что делает:** Optimistic UI через Brain; соблюдает `UX_CONTRACT`.
- **Как связано с общей логикой:** Подключается из shell или соседних shared-компонентов.


### `lib/features/lists/lists_empty_state.dart`

EN:

- **Purpose:** Lists/backlog tab UI module.
- **Contains:** Filters, cards, bulk actions, export. Symbols: ListsNoCategoryEmptyPanel, ListsFilteredEmptyPanel, ListsLoadingPanel.
- **Symbols:** `ListsNoCategoryEmptyPanel, ListsFilteredEmptyPanel, ListsLoadingPanel`
- **Does:** Backlog plan rows with list-domain tags; no play button on cards.
- **Connected to:** Shell tab index 3.

RU:

- **Назначение:** UI-модуль фичи.
- **Что внутри:** Экраны/листы/шиты без прямого HTTP.
- **Что делает:** Optimistic UI через Brain; соблюдает `UX_CONTRACT`.
- **Как связано с общей логикой:** Подключается из shell или соседних shared-компонентов.


### `lib/features/lists/lists_export.dart`

EN:

- **Purpose:** Lists/backlog tab UI module.
- **Contains:** Filters, cards, bulk actions, export. Symbols: (library).
- **Does:** Backlog plan rows with list-domain tags; no play button on cards.
- **Connected to:** Shell tab index 3.

RU:

- **Назначение:** UI-модуль фичи.
- **Что внутри:** Экраны/листы/шиты без прямого HTTP.
- **Что делает:** Optimistic UI через Brain; соблюдает `UX_CONTRACT`.
- **Как связано с общей логикой:** Подключается из shell или соседних shared-компонентов.


### `lib/features/lists/lists_filters.dart`

EN:

- **Purpose:** Lists/backlog tab UI module.
- **Contains:** Filters, cards, bulk actions, export. Symbols: ListsTagFilterChip, ListsCategoryChipBar.
- **Symbols:** `ListsTagFilterChip, ListsCategoryChipBar`
- **Does:** Backlog plan rows with list-domain tags; no play button on cards.
- **Connected to:** Shell tab index 3.

RU:

- **Назначение:** UI-модуль фичи.
- **Что внутри:** Экраны/листы/шиты без прямого HTTP.
- **Что делает:** Optimistic UI через Brain; соблюдает `UX_CONTRACT`.
- **Как связано с общей логикой:** Подключается из shell или соседних shared-компонентов.


### `lib/features/lists/lists_inline_add.dart`

EN:

- **Purpose:** Lists/backlog tab UI module.
- **Contains:** Filters, cards, bulk actions, export. Symbols: ListsInlineAddRow.
- **Symbols:** `ListsInlineAddRow`
- **Does:** Backlog plan rows with list-domain tags; no play button on cards.
- **Connected to:** Shell tab index 3.

RU:

- **Назначение:** UI-модуль фичи.
- **Что внутри:** Экраны/листы/шиты без прямого HTTP.
- **Что делает:** Optimistic UI через Brain; соблюдает `UX_CONTRACT`.
- **Как связано с общей логикой:** Подключается из shell или соседних shared-компонентов.


### `lib/features/lists/lists_view.dart`

EN:

- **Purpose:** Lists/backlog tab UI module.
- **Contains:** Filters, cards, bulk actions, export. Symbols: ListsPage, _ListsPageState.
- **Symbols:** `ListsPage, _ListsPageState`
- **Does:** Backlog plan rows with list-domain tags; no play button on cards.
- **Connected to:** Shell tab index 3.

RU:

- **Назначение:** UI-модуль фичи.
- **Что внутри:** Экраны/листы/шиты без прямого HTTP.
- **Что делает:** Optimistic UI через Brain; соблюдает `UX_CONTRACT`.
- **Как связано с общей логикой:** Подключается из shell или соседних shared-компонентов.


### `lib/features/planning/bulk_planning_edit_sheet.dart`

EN:

- **Purpose:** Plans tab UI module.
- **Contains:** Planning day pager, cards, settings, bulk edit. Symbols: BulkPlanningEditResult, BulkEditWallTimes, _BulkPlanningEditSheetBody.
- **Symbols:** `BulkPlanningEditResult, BulkEditWallTimes, _BulkPlanningEditSheetBody, _BulkPlanningEditSheetBodyState`
- **Does:** Optimistic plan interactions; delegates persistence to Brain.
- **Connected to:** Shell tab index 1; uses canonical `PlanTimeTaskCard` / `AppButton`.

RU:

- **Назначение:** UI-модуль фичи.
- **Что внутри:** Экраны/листы/шиты без прямого HTTP.
- **Что делает:** Optimistic UI через Brain; соблюдает `UX_CONTRACT`.
- **Как связано с общей логикой:** Подключается из shell или соседних shared-компонентов.


### `lib/features/planning/plan_time_gesture_contract.dart`

EN:

- **Purpose:** Plans tab UI module.
- **Contains:** Planning day pager, cards, settings, bulk edit. Symbols: (library).
- **Does:** Optimistic plan interactions; delegates persistence to Brain.
- **Connected to:** Shell tab index 1; uses canonical `PlanTimeTaskCard` / `AppButton`.

RU:

- **Назначение:** UI-модуль фичи.
- **Что внутри:** Экраны/листы/шиты без прямого HTTP.
- **Что делает:** Optimistic UI через Brain; соблюдает `UX_CONTRACT`.
- **Как связано с общей логикой:** Подключается из shell или соседних shared-компонентов.


### `lib/features/planning/plan_time_view_layout.dart`

EN:

- **Purpose:** Plans tab UI module.
- **Contains:** Planning day pager, cards, settings, bulk edit. Symbols: TimeViewYScale, PlanTimeViewDurationGrid, PlanTimeViewBlockLayout.
- **Symbols:** `TimeViewYScale, PlanTimeViewDurationGrid, PlanTimeViewBlockLayout, _PlanTimeViewCardSlot`
- **Does:** Optimistic plan interactions; delegates persistence to Brain.
- **Connected to:** Shell tab index 1; uses canonical `PlanTimeTaskCard` / `AppButton`.

RU:

- **Назначение:** UI-модуль фичи.
- **Что внутри:** Экраны/листы/шиты без прямого HTTP.
- **Что делает:** Optimistic UI через Brain; соблюдает `UX_CONTRACT`.
- **Как связано с общей логикой:** Подключается из shell или соседних shared-компонентов.


### `lib/features/planning/planning_day_start_prefs.dart`

EN:

- **Purpose:** Plans tab UI module.
- **Contains:** Planning day pager, cards, settings, bulk edit. Symbols: PlanningSheetTimelinePrefs.
- **Symbols:** `PlanningSheetTimelinePrefs`
- **Does:** Optimistic plan interactions; delegates persistence to Brain.
- **Connected to:** Shell tab index 1; uses canonical `PlanTimeTaskCard` / `AppButton`.

RU:

- **Назначение:** UI-модуль фичи.
- **Что внутри:** Экраны/листы/шиты без прямого HTTP.
- **Что делает:** Optimistic UI через Brain; соблюдает `UX_CONTRACT`.
- **Как связано с общей логикой:** Подключается из shell или соседних shared-компонентов.


### `lib/features/planning/planning_page.dart`

EN:

- **Purpose:** Plans tab UI module.
- **Contains:** Planning day pager, cards, settings, bulk edit. Symbols: (library).
- **Does:** Optimistic plan interactions; delegates persistence to Brain.
- **Connected to:** Shell tab index 1; uses canonical `PlanTimeTaskCard` / `AppButton`.

RU:

- **Назначение:** UI-модуль фичи.
- **Что внутри:** Экраны/листы/шиты без прямого HTTP.
- **Что делает:** Optimistic UI через Brain; соблюдает `UX_CONTRACT`.
- **Как связано с общей логикой:** Подключается из shell или соседних shared-компонентов.


### `lib/features/planning/planning_page_shell.dart`

EN:

- **Purpose:** Plans tab UI module.
- **Contains:** Planning day pager, cards, settings, bulk edit. Symbols: PlanningSwipeWrapper, _PlanningSwipeWrapperState.
- **Symbols:** `PlanningSwipeWrapper, _PlanningSwipeWrapperState`
- **Does:** Optimistic plan interactions; delegates persistence to Brain.
- **Connected to:** Shell tab index 1; uses canonical `PlanTimeTaskCard` / `AppButton`.

RU:

- **Назначение:** UI-модуль фичи.
- **Что внутри:** Экраны/листы/шиты без прямого HTTP.
- **Что делает:** Optimistic UI через Brain; соблюдает `UX_CONTRACT`.
- **Как связано с общей логикой:** Подключается из shell или соседних shared-компонентов.


### `lib/features/planning/planning_sort_mode.dart`

EN:

- **Purpose:** Plans tab UI module.
- **Contains:** Planning day pager, cards, settings, bulk edit. Symbols: PlanSortMode.
- **Symbols:** `PlanSortMode`
- **Does:** Optimistic plan interactions; delegates persistence to Brain.
- **Connected to:** Shell tab index 1; uses canonical `PlanTimeTaskCard` / `AppButton`.

RU:

- **Назначение:** UI-модуль фичи.
- **Что внутри:** Экраны/листы/шиты без прямого HTTP.
- **Что делает:** Optimistic UI через Brain; соблюдает `UX_CONTRACT`.
- **Как связано с общей логикой:** Подключается из shell или соседних shared-компонентов.


### `lib/features/planning/planning_view.dart`

EN:

- **Purpose:** Plans tab UI module.
- **Contains:** Planning day pager, cards, settings, bulk edit. Symbols: (library).
- **Does:** Optimistic plan interactions; delegates persistence to Brain.
- **Connected to:** Shell tab index 1; uses canonical `PlanTimeTaskCard` / `AppButton`.

RU:

- **Назначение:** UI-модуль фичи.
- **Что внутри:** Экраны/листы/шиты без прямого HTTP.
- **Что делает:** Optimistic UI через Brain; соблюдает `UX_CONTRACT`.
- **Как связано с общей логикой:** Подключается из shell или соседних shared-компонентов.


### `lib/features/planning/recurrence_scope_dialog.dart`

EN:

- **Purpose:** Plans tab UI module.
- **Contains:** Planning day pager, cards, settings, bulk edit. Symbols: (library).
- **Does:** Optimistic plan interactions; delegates persistence to Brain.
- **Connected to:** Shell tab index 1; uses canonical `PlanTimeTaskCard` / `AppButton`.

RU:

- **Назначение:** UI-модуль фичи.
- **Что внутри:** Экраны/листы/шиты без прямого HTTP.
- **Что делает:** Optimistic UI через Brain; соблюдает `UX_CONTRACT`.
- **Как связано с общей логикой:** Подключается из shell или соседних shared-компонентов.


### `lib/features/planning/settings/default_plan_category_search.dart`

EN:

- **Purpose:** Plans tab UI module.
- **Contains:** Planning day pager, cards, settings, bulk edit. Symbols: DefaultPlanCategorySearchDelegate.
- **Symbols:** `DefaultPlanCategorySearchDelegate`
- **Does:** Optimistic plan interactions; delegates persistence to Brain.
- **Connected to:** Shell tab index 1; uses canonical `PlanTimeTaskCard` / `AppButton`.

RU:

- **Назначение:** UI-модуль фичи.
- **Что внутри:** Экраны/листы/шиты без прямого HTTP.
- **Что делает:** Optimistic UI через Brain; соблюдает `UX_CONTRACT`.
- **Как связано с общей логикой:** Подключается из shell или соседних shared-компонентов.


### `lib/features/planning/settings/default_plan_timezone_search.dart`

EN:

- **Purpose:** Plans tab UI module.
- **Contains:** Planning day pager, cards, settings, bulk edit. Symbols: (library).
- **Does:** Optimistic plan interactions; delegates persistence to Brain.
- **Connected to:** Shell tab index 1; uses canonical `PlanTimeTaskCard` / `AppButton`.

RU:

- **Назначение:** UI-модуль фичи.
- **Что внутри:** Экраны/листы/шиты без прямого HTTP.
- **Что делает:** Optimistic UI через Brain; соблюдает `UX_CONTRACT`.
- **Как связано с общей логикой:** Подключается из shell или соседних shared-компонентов.


### `lib/features/planning/settings/plan_record_link_settings.dart`

EN:

- **Purpose:** Plans tab UI module.
- **Contains:** Planning day pager, cards, settings, bulk edit. Symbols: PlanRecordLinkSuggestionSettingsBlock, PlanRecordLinkSuggestionSettingsBlockState.
- **Symbols:** `PlanRecordLinkSuggestionSettingsBlock, PlanRecordLinkSuggestionSettingsBlockState`
- **Does:** Optimistic plan interactions; delegates persistence to Brain.
- **Connected to:** Shell tab index 1; uses canonical `PlanTimeTaskCard` / `AppButton`.

RU:

- **Назначение:** UI-модуль фичи.
- **Что внутри:** Экраны/листы/шиты без прямого HTTP.
- **Что делает:** Optimistic UI через Brain; соблюдает `UX_CONTRACT`.
- **Как связано с общей логикой:** Подключается из shell или соседних shared-компонентов.


### `lib/features/planning/settings/planning_no_tags_settings.dart`

EN:

- **Purpose:** Plans tab UI module.
- **Contains:** Planning day pager, cards, settings, bulk edit. Symbols: PlanningNoTagsSettingsBlock, PlanningNoTagsSettingsBlockState.
- **Symbols:** `PlanningNoTagsSettingsBlock, PlanningNoTagsSettingsBlockState`
- **Does:** Optimistic plan interactions; delegates persistence to Brain.
- **Connected to:** Shell tab index 1; uses canonical `PlanTimeTaskCard` / `AppButton`.

RU:

- **Назначение:** UI-модуль фичи.
- **Что внутри:** Экраны/листы/шиты без прямого HTTP.
- **Что делает:** Optimistic UI через Brain; соблюдает `UX_CONTRACT`.
- **Как связано с общей логикой:** Подключается из shell или соседних shared-компонентов.


### `lib/features/planning/settings/planning_timeline_bounds_sheet.dart`

EN:

- **Purpose:** Plans tab UI module.
- **Contains:** Planning day pager, cards, settings, bulk edit. Symbols: PlanningTimelineBoundsSheet, PlanningTimelineBoundsSheetState.
- **Symbols:** `PlanningTimelineBoundsSheet, PlanningTimelineBoundsSheetState`
- **Does:** Optimistic plan interactions; delegates persistence to Brain.
- **Connected to:** Shell tab index 1; uses canonical `PlanTimeTaskCard` / `AppButton`.

RU:

- **Назначение:** UI-модуль фичи.
- **Что внутри:** Экраны/листы/шиты без прямого HTTP.
- **Что делает:** Optimistic UI через Brain; соблюдает `UX_CONTRACT`.
- **Как связано с общей логикой:** Подключается из shell или соседних shared-компонентов.


### `lib/features/planning/smart_plan_sheet.dart`

EN:

- **Purpose:** Plans tab UI module.
- **Contains:** Planning day pager, cards, settings, bulk edit. Symbols: SmartPlanCommit, SmartPlanSheet, _SmartPlanSheetState.
- **Symbols:** `SmartPlanCommit, SmartPlanSheet, _SmartPlanSheetState`
- **Does:** Optimistic plan interactions; delegates persistence to Brain.
- **Connected to:** Shell tab index 1; uses canonical `PlanTimeTaskCard` / `AppButton`.

RU:

- **Назначение:** UI-модуль фичи.
- **Что внутри:** Экраны/листы/шиты без прямого HTTP.
- **Что делает:** Optimistic UI через Brain; соблюдает `UX_CONTRACT`.
- **Как связано с общей логикой:** Подключается из shell или соседних shared-компонентов.


### `lib/features/planning/time_view/planning_time_view.dart`

EN:

- **Purpose:** Planning Time View UI submodule.
- **Contains:** Canvas, drag/resize, card layer, settings. Symbols: PlanningTimeViewPlanningTimeView.
- **Symbols:** `PlanningTimeViewPlanningTimeView`
- **Does:** Renders proportional day timeline and handles Time View gestures.
- **Connected to:** Composed by `planning_page.dart` / `PlanningTimeViewHost`.

RU:

- **Назначение:** UI-модуль фичи.
- **Что внутри:** Экраны/листы/шиты без прямого HTTP.
- **Что делает:** Optimistic UI через Brain; соблюдает `UX_CONTRACT`.
- **Как связано с общей логикой:** Подключается из shell или соседних shared-компонентов.


### `lib/features/planning/time_view/planning_time_view_coordinator.dart`

EN:

- **Purpose:** Planning Time View UI submodule.
- **Contains:** Canvas, drag/resize, card layer, settings. Symbols: PlanningTimeViewCoordinator.
- **Symbols:** `PlanningTimeViewCoordinator`
- **Does:** Renders proportional day timeline and handles Time View gestures.
- **Connected to:** Composed by `planning_page.dart` / `PlanningTimeViewHost`.

RU:

- **Назначение:** UI-модуль фичи.
- **Что внутри:** Экраны/листы/шиты без прямого HTTP.
- **Что делает:** Optimistic UI через Brain; соблюдает `UX_CONTRACT`.
- **Как связано с общей логикой:** Подключается из shell или соседних shared-компонентов.


### `lib/features/planning/time_view/planning_time_view_host.dart`

EN:

- **Purpose:** Planning Time View UI submodule.
- **Contains:** Canvas, drag/resize, card layer, settings. Symbols: (library).
- **Does:** Renders proportional day timeline and handles Time View gestures.
- **Connected to:** Composed by `planning_page.dart` / `PlanningTimeViewHost`.

RU:

- **Назначение:** UI-модуль фичи.
- **Что внутри:** Экраны/листы/шиты без прямого HTTP.
- **Что делает:** Optimistic UI через Brain; соблюдает `UX_CONTRACT`.
- **Как связано с общей логикой:** Подключается из shell или соседних shared-компонентов.


### `lib/features/planning/time_view/time_view_canvas.dart`

EN:

- **Purpose:** Planning Time View UI submodule.
- **Contains:** Canvas, drag/resize, card layer, settings. Symbols: PlanningTimeViewTimeViewCanvas.
- **Symbols:** `PlanningTimeViewTimeViewCanvas`
- **Does:** Renders proportional day timeline and handles Time View gestures.
- **Connected to:** Composed by `planning_page.dart` / `PlanningTimeViewHost`.

RU:

- **Назначение:** UI-модуль фичи.
- **Что внутри:** Экраны/листы/шиты без прямого HTTP.
- **Что делает:** Optimistic UI через Brain; соблюдает `UX_CONTRACT`.
- **Как связано с общей логикой:** Подключается из shell или соседних shared-компонентов.


### `lib/features/planning/time_view/time_view_card_layer.dart`

EN:

- **Purpose:** Planning Time View UI submodule.
- **Contains:** Canvas, drag/resize, card layer, settings. Symbols: PlanningTimeViewTimeViewCardLayer.
- **Symbols:** `PlanningTimeViewTimeViewCardLayer`
- **Does:** Renders proportional day timeline and handles Time View gestures.
- **Connected to:** Composed by `planning_page.dart` / `PlanningTimeViewHost`.

RU:

- **Назначение:** UI-модуль фичи.
- **Что внутри:** Экраны/листы/шиты без прямого HTTP.
- **Что делает:** Optimistic UI через Brain; соблюдает `UX_CONTRACT`.
- **Как связано с общей логикой:** Подключается из shell или соседних shared-компонентов.


### `lib/features/planning/time_view/time_view_drag_controller.dart`

EN:

- **Purpose:** Planning Time View UI submodule.
- **Contains:** Canvas, drag/resize, card layer, settings. Symbols: PlanningTimeViewTimeViewDragController.
- **Symbols:** `PlanningTimeViewTimeViewDragController`
- **Does:** Renders proportional day timeline and handles Time View gestures.
- **Connected to:** Composed by `planning_page.dart` / `PlanningTimeViewHost`.

RU:

- **Назначение:** UI-модуль фичи.
- **Что внутри:** Экраны/листы/шиты без прямого HTTP.
- **Что делает:** Optimistic UI через Brain; соблюдает `UX_CONTRACT`.
- **Как связано с общей логикой:** Подключается из shell или соседних shared-компонентов.


### `lib/features/planning/time_view/time_view_drag_state.dart`

EN:

- **Purpose:** Planning Time View UI submodule.
- **Contains:** Canvas, drag/resize, card layer, settings. Symbols: TimelineResizeEdge, TimelinePointerGesturePhase.
- **Symbols:** `TimelineResizeEdge, TimelinePointerGesturePhase`
- **Does:** Renders proportional day timeline and handles Time View gestures.
- **Connected to:** Composed by `planning_page.dart` / `PlanningTimeViewHost`.

RU:

- **Назначение:** UI-модуль фичи.
- **Что внутри:** Экраны/листы/шиты без прямого HTTP.
- **Что делает:** Optimistic UI через Brain; соблюдает `UX_CONTRACT`.
- **Как связано с общей логикой:** Подключается из shell или соседних shared-компонентов.


### `lib/features/planning/time_view/time_view_drop_preview.dart`

EN:

- **Purpose:** Planning Time View UI submodule.
- **Contains:** Canvas, drag/resize, card layer, settings. Symbols: PlanningTimeViewTimeViewDropPreview.
- **Symbols:** `PlanningTimeViewTimeViewDropPreview`
- **Does:** Renders proportional day timeline and handles Time View gestures.
- **Connected to:** Composed by `planning_page.dart` / `PlanningTimeViewHost`.

RU:

- **Назначение:** UI-модуль фичи.
- **Что внутри:** Экраны/листы/шиты без прямого HTTP.
- **Что делает:** Optimistic UI через Brain; соблюдает `UX_CONTRACT`.
- **Как связано с общей логикой:** Подключается из shell или соседних shared-компонентов.


### `lib/features/planning/time_view/time_view_fixed_time_settings.dart`

EN:

- **Purpose:** Planning Time View UI submodule.
- **Contains:** Canvas, drag/resize, card layer, settings. Symbols: TimeViewFixedTagsSettingsBlock, TimeViewFixedTagsSettingsBlockState.
- **Symbols:** `TimeViewFixedTagsSettingsBlock, TimeViewFixedTagsSettingsBlockState`
- **Does:** Renders proportional day timeline and handles Time View gestures.
- **Connected to:** Composed by `planning_page.dart` / `PlanningTimeViewHost`.

RU:

- **Назначение:** UI-модуль фичи.
- **Что внутри:** Экраны/листы/шиты без прямого HTTP.
- **Что делает:** Optimistic UI через Brain; соблюдает `UX_CONTRACT`.
- **Как связано с общей логикой:** Подключается из shell или соседних shared-компонентов.


### `lib/features/planning/time_view/time_view_hour_grid.dart`

EN:

- **Purpose:** Planning Time View UI submodule.
- **Contains:** Canvas, drag/resize, card layer, settings. Symbols: PlanningTimeViewTimeViewHourGrid.
- **Symbols:** `PlanningTimeViewTimeViewHourGrid`
- **Does:** Renders proportional day timeline and handles Time View gestures.
- **Connected to:** Composed by `planning_page.dart` / `PlanningTimeViewHost`.

RU:

- **Назначение:** UI-модуль фичи.
- **Что внутри:** Экраны/листы/шиты без прямого HTTP.
- **Что делает:** Optimistic UI через Brain; соблюдает `UX_CONTRACT`.
- **Как связано с общей логикой:** Подключается из shell или соседних shared-компонентов.


### `lib/features/planning/time_view/time_view_interaction_block.dart`

EN:

- **Purpose:** Planning Time View UI submodule.
- **Contains:** Canvas, drag/resize, card layer, settings. Symbols: TimelinePlanInteractionBlock, TimelinePlanInteractionBlockState.
- **Symbols:** `TimelinePlanInteractionBlock, TimelinePlanInteractionBlockState`
- **Does:** Renders proportional day timeline and handles Time View gestures.
- **Connected to:** Composed by `planning_page.dart` / `PlanningTimeViewHost`.

RU:

- **Назначение:** UI-модуль фичи.
- **Что внутри:** Экраны/листы/шиты без прямого HTTP.
- **Что делает:** Optimistic UI через Brain; соблюдает `UX_CONTRACT`.
- **Как связано с общей логикой:** Подключается из shell или соседних shared-компонентов.


### `lib/features/planning/time_view/time_view_resize_controller.dart`

EN:

- **Purpose:** Planning Time View UI submodule.
- **Contains:** Canvas, drag/resize, card layer, settings. Symbols: PlanningTimeViewTimeViewResizeController.
- **Symbols:** `PlanningTimeViewTimeViewResizeController`
- **Does:** Renders proportional day timeline and handles Time View gestures.
- **Connected to:** Composed by `planning_page.dart` / `PlanningTimeViewHost`.

RU:

- **Назначение:** UI-модуль фичи.
- **Что внутри:** Экраны/листы/шиты без прямого HTTP.
- **Что делает:** Optimistic UI через Brain; соблюдает `UX_CONTRACT`.
- **Как связано с общей логикой:** Подключается из shell или соседних shared-компонентов.


### `lib/features/planning/time_view/time_view_search_delegate.dart`

EN:

- **Purpose:** Planning Time View UI submodule.
- **Contains:** Canvas, drag/resize, card layer, settings. Symbols: PlanningTimeViewTimeViewSearchDelegate.
- **Symbols:** `PlanningTimeViewTimeViewSearchDelegate`
- **Does:** Renders proportional day timeline and handles Time View gestures.
- **Connected to:** Composed by `planning_page.dart` / `PlanningTimeViewHost`.

RU:

- **Назначение:** UI-модуль фичи.
- **Что внутри:** Экраны/листы/шиты без прямого HTTP.
- **Что делает:** Optimistic UI через Brain; соблюдает `UX_CONTRACT`.
- **Как связано с общей логикой:** Подключается из shell или соседних shared-компонентов.


### `lib/features/planning/time_view/time_view_settings_sheet.dart`

EN:

- **Purpose:** Planning Time View UI submodule.
- **Contains:** Canvas, drag/resize, card layer, settings. Symbols: PlanningTimeViewTimeViewSettingsSheet.
- **Symbols:** `PlanningTimeViewTimeViewSettingsSheet`
- **Does:** Renders proportional day timeline and handles Time View gestures.
- **Connected to:** Composed by `planning_page.dart` / `PlanningTimeViewHost`.

RU:

- **Назначение:** UI-модуль фичи.
- **Что внутри:** Экраны/листы/шиты без прямого HTTP.
- **Что делает:** Optimistic UI через Brain; соблюдает `UX_CONTRACT`.
- **Как связано с общей логикой:** Подключается из shell или соседних shared-компонентов.


### `lib/features/planning/widgets/plan_card_reorder_settle.dart`

EN:

- **Purpose:** Plans tab UI module.
- **Contains:** Planning day pager, cards, settings, bulk edit. Symbols: PlanCardReorderSettle, PlanCardReorderSettleState.
- **Symbols:** `PlanCardReorderSettle, PlanCardReorderSettleState`
- **Does:** Optimistic plan interactions; delegates persistence to Brain.
- **Connected to:** Shell tab index 1; uses canonical `PlanTimeTaskCard` / `AppButton`.

RU:

- **Назначение:** UI-модуль фичи.
- **Что внутри:** Экраны/листы/шиты без прямого HTTP.
- **Что делает:** Optimistic UI через Brain; соблюдает `UX_CONTRACT`.
- **Как связано с общей логикой:** Подключается из shell или соседних shared-компонентов.


### `lib/features/planning/widgets/planning_bulk_bar.dart`

EN:

- **Purpose:** Plans tab UI module.
- **Contains:** Planning day pager, cards, settings, bulk edit. Symbols: PlanningBulkBottomBar.
- **Symbols:** `PlanningBulkBottomBar`
- **Does:** Optimistic plan interactions; delegates persistence to Brain.
- **Connected to:** Shell tab index 1; uses canonical `PlanTimeTaskCard` / `AppButton`.

RU:

- **Назначение:** UI-модуль фичи.
- **Что внутри:** Экраны/листы/шиты без прямого HTTP.
- **Что делает:** Optimistic UI через Brain; соблюдает `UX_CONTRACT`.
- **Как связано с общей логикой:** Подключается из shell или соседних shared-компонентов.


### `lib/features/planning/widgets/planning_day_card_list_keep_alive.dart`

EN:

- **Purpose:** Plans tab UI module.
- **Contains:** Planning day pager, cards, settings, bulk edit. Symbols: PlanningDayCardListKeepAliveState.
- **Symbols:** `PlanningDayCardListKeepAliveState`
- **Does:** Optimistic plan interactions; delegates persistence to Brain.
- **Connected to:** Shell tab index 1; uses canonical `PlanTimeTaskCard` / `AppButton`.

RU:

- **Назначение:** UI-модуль фичи.
- **Что внутри:** Экраны/листы/шиты без прямого HTTP.
- **Что делает:** Optimistic UI через Brain; соблюдает `UX_CONTRACT`.
- **Как связано с общей логикой:** Подключается из shell или соседних shared-компонентов.


### `lib/features/planning/widgets/planning_empty_states.dart`

EN:

- **Purpose:** Plans tab UI module.
- **Contains:** Planning day pager, cards, settings, bulk edit. Symbols: PlanningDayEmptyState, PlanningFrozenListEmptyState.
- **Symbols:** `PlanningDayEmptyState, PlanningFrozenListEmptyState`
- **Does:** Optimistic plan interactions; delegates persistence to Brain.
- **Connected to:** Shell tab index 1; uses canonical `PlanTimeTaskCard` / `AppButton`.

RU:

- **Назначение:** UI-модуль фичи.
- **Что внутри:** Экраны/листы/шиты без прямого HTTP.
- **Что делает:** Optimistic UI через Brain; соблюдает `UX_CONTRACT`.
- **Как связано с общей логикой:** Подключается из shell или соседних shared-компонентов.


### `lib/features/planning/widgets/planning_filter_controls.dart`

EN:

- **Purpose:** Plans tab UI module.
- **Contains:** Planning day pager, cards, settings, bulk edit. Symbols: PlanningSortModeBar.
- **Symbols:** `PlanningSortModeBar`
- **Does:** Optimistic plan interactions; delegates persistence to Brain.
- **Connected to:** Shell tab index 1; uses canonical `PlanTimeTaskCard` / `AppButton`.

RU:

- **Назначение:** UI-модуль фичи.
- **Что внутри:** Экраны/листы/шиты без прямого HTTP.
- **Что делает:** Optimistic UI через Brain; соблюдает `UX_CONTRACT`.
- **Как связано с общей логикой:** Подключается из shell или соседних shared-компонентов.


### `lib/features/planning/widgets/planning_list_helpers.dart`

EN:

- **Purpose:** Plans tab UI module.
- **Contains:** Planning day pager, cards, settings, bulk edit. Symbols: (library).
- **Does:** Optimistic plan interactions; delegates persistence to Brain.
- **Connected to:** Shell tab index 1; uses canonical `PlanTimeTaskCard` / `AppButton`.

RU:

- **Назначение:** UI-модуль фичи.
- **Что внутри:** Экраны/листы/шиты без прямого HTTP.
- **Что делает:** Optimistic UI через Brain; соблюдает `UX_CONTRACT`.
- **Как связано с общей логикой:** Подключается из shell или соседних shared-компонентов.


### `lib/features/planning/widgets/planning_menu_overlay.dart`

EN:

- **Purpose:** Plans tab UI module.
- **Contains:** Planning day pager, cards, settings, bulk edit. Symbols: SemicirclePlanningMenuOverlay, SemicirclePlanningMenuOverlayState.
- **Symbols:** `SemicirclePlanningMenuOverlay, SemicirclePlanningMenuOverlayState`
- **Does:** Optimistic plan interactions; delegates persistence to Brain.
- **Connected to:** Shell tab index 1; uses canonical `PlanTimeTaskCard` / `AppButton`.

RU:

- **Назначение:** UI-модуль фичи.
- **Что внутри:** Экраны/листы/шиты без прямого HTTP.
- **Что делает:** Optimistic UI через Brain; соблюдает `UX_CONTRACT`.
- **Как связано с общей логикой:** Подключается из shell или соседних shared-компонентов.


### `lib/features/planning/widgets/planning_quick_add_strip.dart`

EN:

- **Purpose:** Plans tab UI module.
- **Contains:** Planning day pager, cards, settings, bulk edit. Symbols: PlanningQuickAddTagStrip.
- **Symbols:** `PlanningQuickAddTagStrip`
- **Does:** Optimistic plan interactions; delegates persistence to Brain.
- **Connected to:** Shell tab index 1; uses canonical `PlanTimeTaskCard` / `AppButton`.

RU:

- **Назначение:** UI-модуль фичи.
- **Что внутри:** Экраны/листы/шиты без прямого HTTP.
- **Что делает:** Optimistic UI через Brain; соблюдает `UX_CONTRACT`.
- **Как связано с общей логикой:** Подключается из shell или соседних shared-компонентов.


### `lib/features/profile/desktop_voice_attempt_dialog.dart`

EN:

- **Purpose:** Profile and tag settings UI.
- **Contains:** Account, timezone, tag manager, desktop voice settings. Symbols: DesktopVoiceAttemptCopyFn, DesktopVoiceAttemptDialogTestHooks, _DesktopVoiceAttemptDialog.
- **Symbols:** `DesktopVoiceAttemptCopyFn, DesktopVoiceAttemptDialogTestHooks, _DesktopVoiceAttemptDialog, _DesktopVoiceAttemptDialogState, _AttemptView`
- **Does:** Edits profile prefs and tag catalog through Brain profile modules.
- **Connected to:** More menu → Profile; admin Component Lab gated by `isAdmin`.

RU:

- **Назначение:** UI-модуль фичи.
- **Что внутри:** Экраны/листы/шиты без прямого HTTP.
- **Что делает:** Optimistic UI через Brain; соблюдает `UX_CONTRACT`.
- **Как связано с общей логикой:** Подключается из shell или соседних shared-компонентов.


### `lib/features/profile/desktop_voice_settings_desktop.dart`

EN:

- **Purpose:** Profile and tag settings UI.
- **Contains:** Account, timezone, tag manager, desktop voice settings. Symbols: DesktopVoiceSettingsDesktopGrid, _DesktopVoiceSettingsDesktopGridState.
- **Symbols:** `DesktopVoiceSettingsDesktopGrid, _DesktopVoiceSettingsDesktopGridState`
- **Does:** Edits profile prefs and tag catalog through Brain profile modules.
- **Connected to:** More menu → Profile; admin Component Lab gated by `isAdmin`.

RU:

- **Назначение:** UI-модуль фичи.
- **Что внутри:** Экраны/листы/шиты без прямого HTTP.
- **Что делает:** Optimistic UI через Brain; соблюдает `UX_CONTRACT`.
- **Как связано с общей логикой:** Подключается из shell или соседних shared-компонентов.


### `lib/features/profile/desktop_voice_settings_section.dart`

EN:

- **Purpose:** Profile and tag settings UI.
- **Contains:** Account, timezone, tag manager, desktop voice settings. Symbols: DesktopVoiceSettingsSection, _DesktopVoiceSettingsSectionState.
- **Symbols:** `DesktopVoiceSettingsSection, _DesktopVoiceSettingsSectionState`
- **Does:** Edits profile prefs and tag catalog through Brain profile modules.
- **Connected to:** More menu → Profile; admin Component Lab gated by `isAdmin`.

RU:

- **Назначение:** UI-модуль фичи.
- **Что внутри:** Экраны/листы/шиты без прямого HTTP.
- **Что делает:** Optimistic UI через Brain; соблюдает `UX_CONTRACT`.
- **Как связано с общей логикой:** Подключается из shell или соседних shared-компонентов.


### `lib/features/profile/profile_view.dart`

EN:

- **Purpose:** Profile and tag settings UI.
- **Contains:** Account, timezone, tag manager, desktop voice settings. Symbols: ProfilePage, _ProfilePageState.
- **Symbols:** `ProfilePage, _ProfilePageState`
- **Does:** Edits profile prefs and tag catalog through Brain profile modules.
- **Connected to:** More menu → Profile; admin Component Lab gated by `isAdmin`.

RU:

- **Назначение:** UI-модуль фичи.
- **Что внутри:** Экраны/листы/шиты без прямого HTTP.
- **Что делает:** Optimistic UI через Brain; соблюдает `UX_CONTRACT`.
- **Как связано с общей логикой:** Подключается из shell или соседних shared-компонентов.


### `lib/features/profile/settings/account_settings_section.dart`

EN:

- **Purpose:** Profile and tag settings UI.
- **Contains:** Account, timezone, tag manager, desktop voice settings. Symbols: AccountSecuritySection.
- **Symbols:** `AccountSecuritySection`
- **Does:** Edits profile prefs and tag catalog through Brain profile modules.
- **Connected to:** More menu → Profile; admin Component Lab gated by `isAdmin`.

RU:

- **Назначение:** UI-модуль фичи.
- **Что внутри:** Экраны/листы/шиты без прямого HTTP.
- **Что делает:** Optimistic UI через Brain; соблюдает `UX_CONTRACT`.
- **Как связано с общей логикой:** Подключается из shell или соседних shared-компонентов.


### `lib/features/profile/settings/notification_settings_section.dart`

EN:

- **Purpose:** Profile and tag settings UI.
- **Contains:** Account, timezone, tag manager, desktop voice settings. Symbols: ProfileNotificationsSection, ProfileNotificationsSectionState.
- **Symbols:** `ProfileNotificationsSection, ProfileNotificationsSectionState`
- **Does:** Edits profile prefs and tag catalog through Brain profile modules.
- **Connected to:** More menu → Profile; admin Component Lab gated by `isAdmin`.

RU:

- **Назначение:** UI-модуль фичи.
- **Что внутри:** Экраны/листы/шиты без прямого HTTP.
- **Что делает:** Optimistic UI через Brain; соблюдает `UX_CONTRACT`.
- **Как связано с общей логикой:** Подключается из shell или соседних shared-компонентов.


### `lib/features/profile/settings/security_settings_section.dart`

EN:

- **Purpose:** Profile and tag settings UI.
- **Contains:** Account, timezone, tag manager, desktop voice settings. Symbols: SecuritySection, SecuritySectionState.
- **Symbols:** `SecuritySection, SecuritySectionState`
- **Does:** Edits profile prefs and tag catalog through Brain profile modules.
- **Connected to:** More menu → Profile; admin Component Lab gated by `isAdmin`.

RU:

- **Назначение:** UI-модуль фичи.
- **Что внутри:** Экраны/листы/шиты без прямого HTTP.
- **Что делает:** Optimistic UI через Brain; соблюдает `UX_CONTRACT`.
- **Как связано с общей логикой:** Подключается из shell или соседних shared-компонентов.


### `lib/features/profile/settings/settings_page.dart`

EN:

- **Purpose:** Profile and tag settings UI.
- **Contains:** Account, timezone, tag manager, desktop voice settings. Symbols: (library).
- **Does:** Edits profile prefs and tag catalog through Brain profile modules.
- **Connected to:** More menu → Profile; admin Component Lab gated by `isAdmin`.

RU:

- **Назначение:** UI-модуль фичи.
- **Что внутри:** Экраны/листы/шиты без прямого HTTP.
- **Что делает:** Optimistic UI через Brain; соблюдает `UX_CONTRACT`.
- **Как связано с общей логикой:** Подключается из shell или соседних shared-компонентов.


### `lib/features/profile/tag_default_duration_settings_view.dart`

EN:

- **Purpose:** Profile and tag settings UI.
- **Contains:** Account, timezone, tag manager, desktop voice settings. Symbols: TagDefaultDurationSettingsView, _TagDefaultDurationSettingsViewState.
- **Symbols:** `TagDefaultDurationSettingsView, _TagDefaultDurationSettingsViewState`
- **Does:** Edits profile prefs and tag catalog through Brain profile modules.
- **Connected to:** More menu → Profile; admin Component Lab gated by `isAdmin`.

RU:

- **Назначение:** UI-модуль фичи.
- **Что внутри:** Экраны/листы/шиты без прямого HTTP.
- **Что делает:** Optimistic UI через Brain; соблюдает `UX_CONTRACT`.
- **Как связано с общей логикой:** Подключается из shell или соседних shared-компонентов.


### `lib/features/profile/tag_manager_page.dart`

EN:

- **Purpose:** Profile and tag settings UI.
- **Contains:** Account, timezone, tag manager, desktop voice settings. Symbols: TagManagerPage, _TagManagerPageState.
- **Symbols:** `TagManagerPage, _TagManagerPageState`
- **Does:** Edits profile prefs and tag catalog through Brain profile modules.
- **Connected to:** More menu → Profile; admin Component Lab gated by `isAdmin`.

RU:

- **Назначение:** UI-модуль фичи.
- **Что внутри:** Экраны/листы/шиты без прямого HTTP.
- **Что делает:** Optimistic UI через Brain; соблюдает `UX_CONTRACT`.
- **Как связано с общей логикой:** Подключается из shell или соседних shared-компонентов.


### `lib/features/profile/tag_settings_hub.dart`

EN:

- **Purpose:** Profile and tag settings UI.
- **Contains:** Account, timezone, tag manager, desktop voice settings. Symbols: TagSettingsHub, _TagSettingsHubState.
- **Symbols:** `TagSettingsHub, _TagSettingsHubState`
- **Does:** Edits profile prefs and tag catalog through Brain profile modules.
- **Connected to:** More menu → Profile; admin Component Lab gated by `isAdmin`.

RU:

- **Назначение:** UI-модуль фичи.
- **Что внутри:** Экраны/листы/шиты без прямого HTTP.
- **Что делает:** Optimistic UI через Brain; соблюдает `UX_CONTRACT`.
- **Как связано с общей логикой:** Подключается из shell или соседних shared-компонентов.


### `lib/features/profile/tag_settings_view.dart`

EN:

- **Purpose:** Profile and tag settings UI.
- **Contains:** Account, timezone, tag manager, desktop voice settings. Symbols: TagSettingsView, _TagSettingsViewState.
- **Symbols:** `TagSettingsView, _TagSettingsViewState`
- **Does:** Edits profile prefs and tag catalog through Brain profile modules.
- **Connected to:** More menu → Profile; admin Component Lab gated by `isAdmin`.

RU:

- **Назначение:** UI-модуль фичи.
- **Что внутри:** Экраны/листы/шиты без прямого HTTP.
- **Что делает:** Optimistic UI через Brain; соблюдает `UX_CONTRACT`.
- **Как связано с общей логикой:** Подключается из shell или соседних shared-компонентов.


### `lib/features/profile/timezone_settings.dart`

EN:

- **Purpose:** Profile and tag settings UI.
- **Contains:** Account, timezone, tag manager, desktop voice settings. Symbols: TimezoneOption.
- **Symbols:** `TimezoneOption`
- **Does:** Edits profile prefs and tag catalog through Brain profile modules.
- **Connected to:** More menu → Profile; admin Component Lab gated by `isAdmin`.

RU:

- **Назначение:** UI-модуль фичи.
- **Что внутри:** Экраны/листы/шиты без прямого HTTP.
- **Что делает:** Optimistic UI через Brain; соблюдает `UX_CONTRACT`.
- **Как связано с общей логикой:** Подключается из shell или соседних shared-компонентов.


### `lib/features/shared/activity_detail_sheet.dart`

EN:

- **Purpose:** Cross-feature shared UI.
- **Contains:** Edit sheets, voice widgets, offline banner. Symbols: ActivityDetailKind, ActivityDetailSheet.
- **Symbols:** `ActivityDetailKind, ActivityDetailSheet`
- **Does:** Routes plan/record edits; hosts Omni-Picker and autosave gate.
- **Connected to:** Imported by Timeline, Planning, Lists, shell edit hosts.

RU:

- **Назначение:** UI-модуль фичи.
- **Что внутри:** Экраны/листы/шиты без прямого HTTP.
- **Что делает:** Optimistic UI через Brain; соблюдает `UX_CONTRACT`.
- **Как связано с общей логикой:** Подключается из shell или соседних shared-компонентов.


### `lib/features/shared/desktop_voice_capsule.dart`

EN:

- **Purpose:** Cross-feature shared UI.
- **Contains:** Edit sheets, voice widgets, offline banner. Symbols: DesktopVoiceCapsule.
- **Symbols:** `DesktopVoiceCapsule`
- **Does:** Routes plan/record edits; hosts Omni-Picker and autosave gate.
- **Connected to:** Imported by Timeline, Planning, Lists, shell edit hosts.

RU:

- **Назначение:** UI-модуль фичи.
- **Что внутри:** Экраны/листы/шиты без прямого HTTP.
- **Что делает:** Optimistic UI через Brain; соблюдает `UX_CONTRACT`.
- **Как связано с общей логикой:** Подключается из shell или соседних shared-компонентов.


### `lib/features/shared/desktop_voice_command_panel.dart`

EN:

- **Purpose:** Cross-feature shared UI.
- **Contains:** Edit sheets, voice widgets, offline banner. Symbols: _DesktopVoicePanelPhase, DesktopVoiceCommandPanel, _DesktopVoiceCommandPanelState.
- **Symbols:** `_DesktopVoicePanelPhase, DesktopVoiceCommandPanel, _DesktopVoiceCommandPanelState`
- **Does:** Routes plan/record edits; hosts Omni-Picker and autosave gate.
- **Connected to:** Imported by Timeline, Planning, Lists, shell edit hosts.

RU:

- **Назначение:** UI-модуль фичи.
- **Что внутри:** Экраны/листы/шиты без прямого HTTP.
- **Что делает:** Optimistic UI через Brain; соблюдает `UX_CONTRACT`.
- **Как связано с общей логикой:** Подключается из shell или соседних shared-компонентов.


### `lib/features/shared/desktop_voice_widget.dart`

EN:

- **Purpose:** Cross-feature shared UI.
- **Contains:** Edit sheets, voice widgets, offline banner. Symbols: DesktopVoiceOverlayPhase, DesktopVoiceOverlay, _DesktopVoiceOverlayState.
- **Symbols:** `DesktopVoiceOverlayPhase, DesktopVoiceOverlay, _DesktopVoiceOverlayState`
- **Does:** Routes plan/record edits; hosts Omni-Picker and autosave gate.
- **Connected to:** Imported by Timeline, Planning, Lists, shell edit hosts.

RU:

- **Назначение:** UI-модуль фичи.
- **Что внутри:** Экраны/листы/шиты без прямого HTTP.
- **Что делает:** Optimistic UI через Brain; соблюдает `UX_CONTRACT`.
- **Как связано с общей логикой:** Подключается из shell или соседних shared-компонентов.


### `lib/features/shared/edit_sheet/checklist_helpers.dart`

EN:

- **Purpose:** Cross-feature shared UI.
- **Contains:** Edit sheets, voice widgets, offline banner. Symbols: (library).
- **Does:** Routes plan/record edits; hosts Omni-Picker and autosave gate.
- **Connected to:** Imported by Timeline, Planning, Lists, shell edit hosts.

RU:

- **Назначение:** UI-модуль фичи.
- **Что внутри:** Экраны/листы/шиты без прямого HTTP.
- **Что делает:** Optimistic UI через Brain; соблюдает `UX_CONTRACT`.
- **Как связано с общей логикой:** Подключается из shell или соседних shared-компонентов.


### `lib/features/shared/edit_sheet/parallel_record_panels.dart`

EN:

- **Purpose:** Cross-feature shared UI.
- **Contains:** Edit sheets, voice widgets, offline banner. Symbols: BacklogSubItemsPanel, BacklogSubItemsPanelState.
- **Symbols:** `BacklogSubItemsPanel, BacklogSubItemsPanelState`
- **Does:** Routes plan/record edits; hosts Omni-Picker and autosave gate.
- **Connected to:** Imported by Timeline, Planning, Lists, shell edit hosts.

RU:

- **Назначение:** UI-модуль фичи.
- **Что внутри:** Экраны/листы/шиты без прямого HTTP.
- **Что делает:** Optimistic UI через Brain; соблюдает `UX_CONTRACT`.
- **Как связано с общей логикой:** Подключается из shell или соседних shared-компонентов.


### `lib/features/shared/edit_sheet/plan_repeat_helpers.dart`

EN:

- **Purpose:** Cross-feature shared UI.
- **Contains:** Edit sheets, voice widgets, offline banner. Symbols: (library).
- **Does:** Routes plan/record edits; hosts Omni-Picker and autosave gate.
- **Connected to:** Imported by Timeline, Planning, Lists, shell edit hosts.

RU:

- **Назначение:** UI-модуль фичи.
- **Что внутри:** Экраны/листы/шиты без прямого HTTP.
- **Что делает:** Optimistic UI через Brain; соблюдает `UX_CONTRACT`.
- **Как связано с общей логикой:** Подключается из shell или соседних shared-компонентов.


### `lib/features/shared/edit_sheet/quill_link_launcher.dart`

EN:

- **Purpose:** Cross-feature shared UI.
- **Contains:** Edit sheets, voice widgets, offline banner. Symbols: (library).
- **Does:** Routes plan/record edits; hosts Omni-Picker and autosave gate.
- **Connected to:** Imported by Timeline, Planning, Lists, shell edit hosts.

RU:

- **Назначение:** UI-модуль фичи.
- **Что внутри:** Экраны/листы/шиты без прямого HTTP.
- **Что делает:** Optimistic UI через Brain; соблюдает `UX_CONTRACT`.
- **Как связано с общей логикой:** Подключается из shell или соседних shared-компонентов.


### `lib/features/shared/edit_sheet/quill_toolbar_config.dart`

EN:

- **Purpose:** Cross-feature shared UI.
- **Contains:** Edit sheets, voice widgets, offline banner. Symbols: (library).
- **Does:** Routes plan/record edits; hosts Omni-Picker and autosave gate.
- **Connected to:** Imported by Timeline, Planning, Lists, shell edit hosts.

RU:

- **Назначение:** UI-модуль фичи.
- **Что внутри:** Экраны/листы/шиты без прямого HTTP.
- **Что делает:** Optimistic UI через Brain; соблюдает `UX_CONTRACT`.
- **Как связано с общей логикой:** Подключается из shell или соседних shared-компонентов.


### `lib/features/shared/edit_sheet/sheet_autosave_gate.dart`

EN:

- **Purpose:** Cross-feature shared UI.
- **Contains:** Edit sheets, voice widgets, offline banner. Symbols: EditSheetAutosaveGate.
- **Symbols:** `EditSheetAutosaveGate`
- **Does:** Routes plan/record edits; hosts Omni-Picker and autosave gate.
- **Connected to:** Imported by Timeline, Planning, Lists, shell edit hosts.

RU:

- **Назначение:** UI-модуль фичи.
- **Что внутри:** Экраны/листы/шиты без прямого HTTP.
- **Что делает:** Optimistic UI через Brain; соблюдает `UX_CONTRACT`.
- **Как связано с общей логикой:** Подключается из shell или соседних shared-компонентов.


### `lib/features/shared/edit_sheet/sheet_time_helpers.dart`

EN:

- **Purpose:** Cross-feature shared UI.
- **Contains:** Edit sheets, voice widgets, offline banner. Symbols: (library).
- **Does:** Routes plan/record edits; hosts Omni-Picker and autosave gate.
- **Connected to:** Imported by Timeline, Planning, Lists, shell edit hosts.

RU:

- **Назначение:** UI-модуль фичи.
- **Что внутри:** Экраны/листы/шиты без прямого HTTP.
- **Что делает:** Optimistic UI через Brain; соблюдает `UX_CONTRACT`.
- **Как связано с общей логикой:** Подключается из shell или соседних shared-компонентов.


### `lib/features/shared/edit_sheet/sheet_time_picker.dart`

EN:

- **Purpose:** Cross-feature shared UI.
- **Contains:** Edit sheets, voice widgets, offline banner. Symbols: AppEditSheetTimeButton.
- **Symbols:** `AppEditSheetTimeButton`
- **Does:** Routes plan/record edits; hosts Omni-Picker and autosave gate.
- **Connected to:** Imported by Timeline, Planning, Lists, shell edit hosts.

RU:

- **Назначение:** UI-модуль фичи.
- **Что внутри:** Экраны/листы/шиты без прямого HTTP.
- **Что делает:** Optimistic UI через Brain; соблюдает `UX_CONTRACT`.
- **Как связано с общей логикой:** Подключается из shell или соседних shared-компонентов.


### `lib/features/shared/empty_state_placeholder.dart`

EN:

- **Purpose:** Cross-feature shared UI.
- **Contains:** Edit sheets, voice widgets, offline banner. Symbols: (library).
- **Does:** Routes plan/record edits; hosts Omni-Picker and autosave gate.
- **Connected to:** Imported by Timeline, Planning, Lists, shell edit hosts.

RU:

- **Назначение:** UI-модуль фичи.
- **Что внутри:** Экраны/листы/шиты без прямого HTTP.
- **Что делает:** Optimistic UI через Brain; соблюдает `UX_CONTRACT`.
- **Как связано с общей логикой:** Подключается из shell или соседних shared-компонентов.


### `lib/features/shared/offline_sync_status_bar.dart`

EN:

- **Purpose:** Cross-feature shared UI.
- **Contains:** Edit sheets, voice widgets, offline banner. Symbols: OfflineSyncStatusBar, OfflineSyncStatusBarState.
- **Symbols:** `OfflineSyncStatusBar, OfflineSyncStatusBarState`
- **Does:** Routes plan/record edits; hosts Omni-Picker and autosave gate.
- **Connected to:** Imported by Timeline, Planning, Lists, shell edit hosts.

RU:

- **Назначение:** UI-модуль фичи.
- **Что внутри:** Экраны/листы/шиты без прямого HTTP.
- **Что делает:** Optimistic UI через Brain; соблюдает `UX_CONTRACT`.
- **Как связано с общей логикой:** Подключается из shell или соседних shared-компонентов.


### `lib/features/shared/planning_task_edit_sheet.dart`

EN:

- **Purpose:** Cross-feature shared UI.
- **Contains:** Edit sheets, voice widgets, offline banner. Symbols: PlanningTaskEditSheetState.
- **Symbols:** `PlanningTaskEditSheetState`
- **Does:** Routes plan/record edits; hosts Omni-Picker and autosave gate.
- **Connected to:** Imported by Timeline, Planning, Lists, shell edit hosts.

RU:

- **Назначение:** UI-модуль фичи.
- **Что внутри:** Экраны/листы/шиты без прямого HTTP.
- **Что делает:** Optimistic UI через Brain; соблюдает `UX_CONTRACT`.
- **Как связано с общей логикой:** Подключается из shell или соседних shared-компонентов.


### `lib/features/shared/profile_hydration_status_bar.dart`

EN:

- **Purpose:** Cross-feature shared UI.
- **Contains:** Edit sheets, voice widgets, offline banner. Symbols: (library).
- **Does:** Routes plan/record edits; hosts Omni-Picker and autosave gate.
- **Connected to:** Imported by Timeline, Planning, Lists, shell edit hosts.

RU:

- **Назначение:** UI-модуль фичи.
- **Что внутри:** Экраны/листы/шиты без прямого HTTP.
- **Что делает:** Optimistic UI через Brain; соблюдает `UX_CONTRACT`.
- **Как связано с общей логикой:** Подключается из shell или соседних shared-компонентов.


### `lib/features/shared/shared_widgets.dart`

EN:

- **Purpose:** Cross-feature shared UI.
- **Contains:** Edit sheets, voice widgets, offline banner. Symbols: (library).
- **Does:** Routes plan/record edits; hosts Omni-Picker and autosave gate.
- **Connected to:** Imported by Timeline, Planning, Lists, shell edit hosts.

RU:

- **Назначение:** UI-модуль фичи.
- **Что внутри:** Экраны/листы/шиты без прямого HTTP.
- **Что делает:** Optimistic UI через Brain; соблюдает `UX_CONTRACT`.
- **Как связано с общей логикой:** Подключается из shell или соседних shared-компонентов.


### `lib/features/shared/timeline_record_edit_sheet.dart`

EN:

- **Purpose:** Cross-feature shared UI.
- **Contains:** Edit sheets, voice widgets, offline banner. Symbols: TimelineRecordSheetContentState.
- **Symbols:** `TimelineRecordSheetContentState`
- **Does:** Routes plan/record edits; hosts Omni-Picker and autosave gate.
- **Connected to:** Imported by Timeline, Planning, Lists, shell edit hosts.

RU:

- **Назначение:** UI-модуль фичи.
- **Что внутри:** Экраны/листы/шиты без прямого HTTP.
- **Что делает:** Optimistic UI через Brain; соблюдает `UX_CONTRACT`.
- **Как связано с общей логикой:** Подключается из shell или соседних shared-компонентов.


### `lib/features/shared/voice_capture_config.dart`

EN:

- **Purpose:** Cross-feature shared UI.
- **Contains:** Edit sheets, voice widgets, offline banner. Symbols: VoiceCaptureConfig.
- **Symbols:** `VoiceCaptureConfig`
- **Does:** Routes plan/record edits; hosts Omni-Picker and autosave gate.
- **Connected to:** Imported by Timeline, Planning, Lists, shell edit hosts.

RU:

- **Назначение:** UI-модуль фичи.
- **Что внутри:** Экраны/листы/шиты без прямого HTTP.
- **Что делает:** Optimistic UI через Brain; соблюдает `UX_CONTRACT`.
- **Как связано с общей логикой:** Подключается из shell или соседних shared-компонентов.


### `lib/features/shared/voice_input_sheet.dart`

EN:

- **Purpose:** Cross-feature shared UI.
- **Contains:** Edit sheets, voice widgets, offline banner. Symbols: VoiceInputSheet, _VoiceInputSheetState.
- **Symbols:** `VoiceInputSheet, _VoiceInputSheetState`
- **Does:** Routes plan/record edits; hosts Omni-Picker and autosave gate.
- **Connected to:** Imported by Timeline, Planning, Lists, shell edit hosts.

RU:

- **Назначение:** UI-модуль фичи.
- **Что внутри:** Экраны/листы/шиты без прямого HTTP.
- **Что делает:** Optimistic UI через Brain; соблюдает `UX_CONTRACT`.
- **Как связано с общей логикой:** Подключается из shell или соседних shared-компонентов.


### `lib/features/stats/plan_vs_fact_tab.dart`

EN:

- **Purpose:** Project file.
- **Contains:** Supporting source or config. Symbols: PlanVsFactTab, _PlanVsFactTabState.
- **Symbols:** `PlanVsFactTab, _PlanVsFactTabState`
- **Does:** See path prefix for ownership.
- **Connected to:** Cross-check `docs/APP_STRUCTURE.md` layer rules.

RU:

- **Назначение:** UI-модуль фичи.
- **Что внутри:** Экраны/листы/шиты без прямого HTTP.
- **Что делает:** Optimistic UI через Brain; соблюдает `UX_CONTRACT`.
- **Как связано с общей логикой:** Подключается из shell или соседних shared-компонентов.


### `lib/features/stats/stats_view.dart`

EN:

- **Purpose:** Project file.
- **Contains:** Supporting source or config. Symbols: StatsView, _StatsViewState.
- **Symbols:** `StatsView, _StatsViewState`
- **Does:** See path prefix for ownership.
- **Connected to:** Cross-check `docs/APP_STRUCTURE.md` layer rules.

RU:

- **Назначение:** UI-модуль фичи.
- **Что внутри:** Экраны/листы/шиты без прямого HTTP.
- **Что делает:** Optimistic UI через Brain; соблюдает `UX_CONTRACT`.
- **Как связано с общей логикой:** Подключается из shell или соседних shared-компонентов.


### `lib/features/timeline/timeline_day_page.dart`

EN:

- **Purpose:** Timeline tab UI module.
- **Contains:** Day pager, record cards, header controls. Symbols: TimelineDayCardList, TimelineDayCardListState, TimelineLazyRecordList.
- **Symbols:** `TimelineDayCardList, TimelineDayCardListState, TimelineLazyRecordList`
- **Does:** Displays running/stopped records for profile wall days.
- **Connected to:** Shell tab index 0; record edit via `ActivityDetailSheet`.

RU:

- **Назначение:** UI-модуль фичи.
- **Что внутри:** Экраны/листы/шиты без прямого HTTP.
- **Что делает:** Optimistic UI через Brain; соблюдает `UX_CONTRACT`.
- **Как связано с общей логикой:** Подключается из shell или соседних shared-компонентов.


### `lib/features/timeline/timeline_header_controls.dart`

EN:

- **Purpose:** Timeline tab UI module.
- **Contains:** Day pager, record cards, header controls. Symbols: TimelineHeaderControls.
- **Symbols:** `TimelineHeaderControls`
- **Does:** Displays running/stopped records for profile wall days.
- **Connected to:** Shell tab index 0; record edit via `ActivityDetailSheet`.

RU:

- **Назначение:** UI-модуль фичи.
- **Что внутри:** Экраны/листы/шиты без прямого HTTP.
- **Что делает:** Optimistic UI через Brain; соблюдает `UX_CONTRACT`.
- **Как связано с общей логикой:** Подключается из shell или соседних shared-компонентов.


### `lib/features/timeline/timeline_helpers.dart`

EN:

- **Purpose:** Timeline tab UI module.
- **Contains:** Day pager, record cards, header controls. Symbols: (library).
- **Does:** Displays running/stopped records for profile wall days.
- **Connected to:** Shell tab index 0; record edit via `ActivityDetailSheet`.

RU:

- **Назначение:** UI-модуль фичи.
- **Что внутри:** Экраны/листы/шиты без прямого HTTP.
- **Что делает:** Optimistic UI через Brain; соблюдает `UX_CONTRACT`.
- **Как связано с общей логикой:** Подключается из shell или соседних shared-компонентов.


### `lib/features/timeline/timeline_record_card.dart`

EN:

- **Purpose:** Timeline tab UI module.
- **Contains:** Day pager, record cards, header controls. Symbols: TimelineRecordCard, TimelineRecordCardState.
- **Symbols:** `TimelineRecordCard, TimelineRecordCardState`
- **Does:** Displays running/stopped records for profile wall days.
- **Connected to:** Shell tab index 0; record edit via `ActivityDetailSheet`.

RU:

- **Назначение:** UI-модуль фичи.
- **Что внутри:** Экраны/листы/шиты без прямого HTTP.
- **Что делает:** Optimistic UI через Brain; соблюдает `UX_CONTRACT`.
- **Как связано с общей логикой:** Подключается из shell или соседних shared-компонентов.


### `lib/features/timeline/timeline_view.dart`

EN:

- **Purpose:** Timeline tab UI module.
- **Contains:** Day pager, record cards, header controls. Symbols: TimelineSwipeWrapper.
- **Symbols:** `TimelineSwipeWrapper`
- **Does:** Displays running/stopped records for profile wall days.
- **Connected to:** Shell tab index 0; record edit via `ActivityDetailSheet`.

RU:

- **Назначение:** UI-модуль фичи.
- **Что внутри:** Экраны/листы/шиты без прямого HTTP.
- **Что делает:** Optimistic UI через Brain; соблюдает `UX_CONTRACT`.
- **Как связано с общей логикой:** Подключается из shell или соседних shared-компонентов.


### `lib/features/wear/wear_main_wrapper.dart`

EN:

- **Purpose:** Project file.
- **Contains:** Supporting source or config. Symbols: WearMainWrapper.
- **Symbols:** `WearMainWrapper`
- **Does:** See path prefix for ownership.
- **Connected to:** Cross-check `docs/APP_STRUCTURE.md` layer rules.

RU:

- **Назначение:** UI-модуль фичи.
- **Что внутри:** Экраны/листы/шиты без прямого HTTP.
- **Что делает:** Optimistic UI через Brain; соблюдает `UX_CONTRACT`.
- **Как связано с общей логикой:** Подключается из shell или соседних shared-компонентов.


### `lib/features/wear/wear_platform.dart`

EN:

- **Purpose:** Project file.
- **Contains:** Supporting source or config. Symbols: WearPlatform.
- **Symbols:** `WearPlatform`
- **Does:** See path prefix for ownership.
- **Connected to:** Cross-check `docs/APP_STRUCTURE.md` layer rules.

RU:

- **Назначение:** UI-модуль фичи.
- **Что внутри:** Экраны/листы/шиты без прямого HTTP.
- **Что делает:** Optimistic UI через Brain; соблюдает `UX_CONTRACT`.
- **Как связано с общей логикой:** Подключается из shell или соседних shared-компонентов.


### `lib/features/wear/wear_runtime.dart`

EN:

- **Purpose:** Project file.
- **Contains:** Supporting source or config. Symbols: (library).
- **Does:** See path prefix for ownership.
- **Connected to:** Cross-check `docs/APP_STRUCTURE.md` layer rules.

RU:

- **Назначение:** UI-модуль фичи.
- **Что внутри:** Экраны/листы/шиты без прямого HTTP.
- **Что делает:** Optimistic UI через Brain; соблюдает `UX_CONTRACT`.
- **Как связано с общей логикой:** Подключается из shell или соседних shared-компонентов.


### `lib/features/wear/wear_timer_screen.dart`

EN:

- **Purpose:** Project file.
- **Contains:** Supporting source or config. Symbols: WearTimerScreen, _WearTimerScreenState.
- **Symbols:** `WearTimerScreen, _WearTimerScreenState`
- **Does:** See path prefix for ownership.
- **Connected to:** Cross-check `docs/APP_STRUCTURE.md` layer rules.

RU:

- **Назначение:** UI-модуль фичи.
- **Что внутри:** Экраны/листы/шиты без прямого HTTP.
- **Что делает:** Optimistic UI через Brain; соблюдает `UX_CONTRACT`.
- **Как связано с общей логикой:** Подключается из shell или соседних shared-компонентов.


### `lib/l10n/app_locales.dart`

EN:

- **Purpose:** Localization catalog.
- **Contains:** Dictionary keys and locale maps. Symbols: (library).
- **Does:** Provides `t()` lookup for UI strings.
- **Connected to:** Features import `dictionary.dart`; EN/RU are canonical SSOT.

RU:

- **Назначение:** Файл проекта.
- **Что внутри:** См. англ. блок Purpose/Contains.
- **Что делает:** Смотри префикс пути для владения.
- **Как связано с общей логикой:** Правила слоёв — `docs/APP_STRUCTURE.md`.


### `lib/l10n/category_db_display.dart`

EN:

- **Purpose:** Localization catalog.
- **Contains:** Dictionary keys and locale maps. Symbols: (library).
- **Does:** Provides `t()` lookup for UI strings.
- **Connected to:** Features import `dictionary.dart`; EN/RU are canonical SSOT.

RU:

- **Назначение:** Файл проекта.
- **Что внутри:** См. англ. блок Purpose/Contains.
- **Что делает:** Смотри префикс пути для владения.
- **Как связано с общей логикой:** Правила слоёв — `docs/APP_STRUCTURE.md`.


### `lib/l10n/dictionary.dart`

EN:

- **Purpose:** Localization catalog.
- **Contains:** Dictionary keys and locale maps. Symbols: (library).
- **Does:** Provides `t()` lookup for UI strings.
- **Connected to:** Features import `dictionary.dart`; EN/RU are canonical SSOT.

RU:

- **Назначение:** Файл проекта.
- **Что внутри:** См. англ. блок Purpose/Contains.
- **Что делает:** Смотри префикс пути для владения.
- **Как связано с общей логикой:** Правила слоёв — `docs/APP_STRUCTURE.md`.


### `lib/l10n/langs/ar.dart`

EN:

- **Purpose:** Localization catalog.
- **Contains:** Dictionary keys and locale maps. Symbols: (library).
- **Does:** Provides `t()` lookup for UI strings.
- **Connected to:** Features import `dictionary.dart`; EN/RU are canonical SSOT.

RU:

- **Назначение:** Файл проекта.
- **Что внутри:** См. англ. блок Purpose/Contains.
- **Что делает:** Смотри префикс пути для владения.
- **Как связано с общей логикой:** Правила слоёв — `docs/APP_STRUCTURE.md`.


### `lib/l10n/langs/de.dart`

EN:

- **Purpose:** Localization catalog.
- **Contains:** Dictionary keys and locale maps. Symbols: (library).
- **Does:** Provides `t()` lookup for UI strings.
- **Connected to:** Features import `dictionary.dart`; EN/RU are canonical SSOT.

RU:

- **Назначение:** Файл проекта.
- **Что внутри:** См. англ. блок Purpose/Contains.
- **Что делает:** Смотри префикс пути для владения.
- **Как связано с общей логикой:** Правила слоёв — `docs/APP_STRUCTURE.md`.


### `lib/l10n/langs/en.dart`

EN:

- **Purpose:** Localization catalog.
- **Contains:** Dictionary keys and locale maps. Symbols: (library).
- **Does:** Provides `t()` lookup for UI strings.
- **Connected to:** Features import `dictionary.dart`; EN/RU are canonical SSOT.

RU:

- **Назначение:** Файл проекта.
- **Что внутри:** См. англ. блок Purpose/Contains.
- **Что делает:** Смотри префикс пути для владения.
- **Как связано с общей логикой:** Правила слоёв — `docs/APP_STRUCTURE.md`.


### `lib/l10n/langs/es.dart`

EN:

- **Purpose:** Localization catalog.
- **Contains:** Dictionary keys and locale maps. Symbols: (library).
- **Does:** Provides `t()` lookup for UI strings.
- **Connected to:** Features import `dictionary.dart`; EN/RU are canonical SSOT.

RU:

- **Назначение:** Файл проекта.
- **Что внутри:** См. англ. блок Purpose/Contains.
- **Что делает:** Смотри префикс пути для владения.
- **Как связано с общей логикой:** Правила слоёв — `docs/APP_STRUCTURE.md`.


### `lib/l10n/langs/fr.dart`

EN:

- **Purpose:** Localization catalog.
- **Contains:** Dictionary keys and locale maps. Symbols: (library).
- **Does:** Provides `t()` lookup for UI strings.
- **Connected to:** Features import `dictionary.dart`; EN/RU are canonical SSOT.

RU:

- **Назначение:** Файл проекта.
- **Что внутри:** См. англ. блок Purpose/Contains.
- **Что делает:** Смотри префикс пути для владения.
- **Как связано с общей логикой:** Правила слоёв — `docs/APP_STRUCTURE.md`.


### `lib/l10n/langs/it.dart`

EN:

- **Purpose:** Localization catalog.
- **Contains:** Dictionary keys and locale maps. Symbols: (library).
- **Does:** Provides `t()` lookup for UI strings.
- **Connected to:** Features import `dictionary.dart`; EN/RU are canonical SSOT.

RU:

- **Назначение:** Файл проекта.
- **Что внутри:** См. англ. блок Purpose/Contains.
- **Что делает:** Смотри префикс пути для владения.
- **Как связано с общей логикой:** Правила слоёв — `docs/APP_STRUCTURE.md`.


### `lib/l10n/langs/ko.dart`

EN:

- **Purpose:** Localization catalog.
- **Contains:** Dictionary keys and locale maps. Symbols: (library).
- **Does:** Provides `t()` lookup for UI strings.
- **Connected to:** Features import `dictionary.dart`; EN/RU are canonical SSOT.

RU:

- **Назначение:** Файл проекта.
- **Что внутри:** См. англ. блок Purpose/Contains.
- **Что делает:** Смотри префикс пути для владения.
- **Как связано с общей логикой:** Правила слоёв — `docs/APP_STRUCTURE.md`.


### `lib/l10n/langs/ru.dart`

EN:

- **Purpose:** Localization catalog.
- **Contains:** Dictionary keys and locale maps. Symbols: (library).
- **Does:** Provides `t()` lookup for UI strings.
- **Connected to:** Features import `dictionary.dart`; EN/RU are canonical SSOT.

RU:

- **Назначение:** Файл проекта.
- **Что внутри:** См. англ. блок Purpose/Contains.
- **Что делает:** Смотри префикс пути для владения.
- **Как связано с общей логикой:** Правила слоёв — `docs/APP_STRUCTURE.md`.


### `lib/l10n/langs/zh.dart`

EN:

- **Purpose:** Localization catalog.
- **Contains:** Dictionary keys and locale maps. Symbols: (library).
- **Does:** Provides `t()` lookup for UI strings.
- **Connected to:** Features import `dictionary.dart`; EN/RU are canonical SSOT.

RU:

- **Назначение:** Файл проекта.
- **Что внутри:** См. англ. блок Purpose/Contains.
- **Что делает:** Смотри префикс пути для владения.
- **Как связано с общей логикой:** Правила слоёв — `docs/APP_STRUCTURE.md`.


### `lib/main.dart`

EN:

- **Purpose:** Project file.
- **Contains:** Supporting source or config. Symbols: main.
- **Symbols:** `main`
- **Does:** See path prefix for ownership.
- **Connected to:** Cross-check `docs/APP_STRUCTURE.md` layer rules.

RU:

- **Назначение:** Файл проекта.
- **Что внутри:** См. англ. блок Purpose/Contains.
- **Что делает:** Смотри префикс пути для владения.
- **Как связано с общей логикой:** Правила слоёв — `docs/APP_STRUCTURE.md`.


### `lib/services/notification_service.dart`

EN:

- **Purpose:** Device bridge service.
- **Contains:** Notifications/alarms without UI. Symbols: _AlarmCandidate, NotificationService.
- **Symbols:** `_AlarmCandidate, NotificationService`
- **Does:** Schedules plan alarms; no PocketBase HTTP.
- **Connected to:** Brain requests reschedule via notification service.

RU:

- **Назначение:** Файл проекта.
- **Что внутри:** См. англ. блок Purpose/Contains.
- **Что делает:** Смотри префикс пути для владения.
- **Как связано с общей логикой:** Правила слоёв — `docs/APP_STRUCTURE.md`.


### `lib/shell/life_os_dashboard.dart`

EN:

- **Purpose:** Application shell module.
- **Contains:** Tab host, voice routing, edit modals, side nav. Symbols: ShellDashboardBase.
- **Symbols:** `ShellDashboardBase`
- **Does:** Wires bottom nav / desktop rail to feature pages.
- **Connected to:** `app_shell.dart` re-exports `life_os_dashboard.dart`.

RU:

- **Назначение:** Оболочка приложения (shell).
- **Что внутри:** Вкладки, voice routing, edit hosts.
- **Что делает:** Связывает main → features.
- **Как связано с общей логикой:** Re-export через `app_shell.dart`.


### `lib/shell/profile_hydration_status_bar.dart`

EN:

- **Purpose:** Application shell module.
- **Contains:** Tab host, voice routing, edit modals, side nav. Symbols: ProfileHydrationStatusBar.
- **Symbols:** `ProfileHydrationStatusBar`
- **Does:** Wires bottom nav / desktop rail to feature pages.
- **Connected to:** `app_shell.dart` re-exports `life_os_dashboard.dart`.

RU:

- **Назначение:** Оболочка приложения (shell).
- **Что внутри:** Вкладки, voice routing, edit hosts.
- **Что делает:** Связывает main → features.
- **Как связано с общей логикой:** Re-export через `app_shell.dart`.


### `lib/shell/settings_page.dart`

EN:

- **Purpose:** Application shell module.
- **Contains:** Tab host, voice routing, edit modals, side nav. Symbols: SettingsPage, SettingsPageState.
- **Symbols:** `SettingsPage, SettingsPageState`
- **Does:** Wires bottom nav / desktop rail to feature pages.
- **Connected to:** `app_shell.dart` re-exports `life_os_dashboard.dart`.

RU:

- **Назначение:** Оболочка приложения (shell).
- **Что внутри:** Вкладки, voice routing, edit hosts.
- **Что делает:** Связывает main → features.
- **Как связано с общей логикой:** Re-export через `app_shell.dart`.


### `lib/shell/shell_core.dart`

EN:

- **Purpose:** Application shell module.
- **Contains:** Tab host, voice routing, edit modals, side nav. Symbols: ShellCoreLogic.
- **Symbols:** `ShellCoreLogic`
- **Does:** Wires bottom nav / desktop rail to feature pages.
- **Connected to:** `app_shell.dart` re-exports `life_os_dashboard.dart`.

RU:

- **Назначение:** Оболочка приложения (shell).
- **Что внутри:** Вкладки, voice routing, edit hosts.
- **Что делает:** Связывает main → features.
- **Как связано с общей логикой:** Re-export через `app_shell.dart`.


### `lib/shell/shell_edit_hosts.dart`

EN:

- **Purpose:** Application shell module.
- **Contains:** Tab host, voice routing, edit modals, side nav. Symbols: ShellEditHosts.
- **Symbols:** `ShellEditHosts`
- **Does:** Wires bottom nav / desktop rail to feature pages.
- **Connected to:** `app_shell.dart` re-exports `life_os_dashboard.dart`.

RU:

- **Назначение:** Оболочка приложения (shell).
- **Что внутри:** Вкладки, voice routing, edit hosts.
- **Что делает:** Связывает main → features.
- **Как связано с общей логикой:** Re-export через `app_shell.dart`.


### `lib/shell/shell_more_menu.dart`

EN:

- **Purpose:** Application shell module.
- **Contains:** Tab host, voice routing, edit modals, side nav. Symbols: ShellMoreMenu.
- **Symbols:** `ShellMoreMenu`
- **Does:** Wires bottom nav / desktop rail to feature pages.
- **Connected to:** `app_shell.dart` re-exports `life_os_dashboard.dart`.

RU:

- **Назначение:** Оболочка приложения (shell).
- **Что внутри:** Вкладки, voice routing, edit hosts.
- **Что делает:** Связывает main → features.
- **Как связано с общей логикой:** Re-export через `app_shell.dart`.


### `lib/shell/shell_offline_banner.dart`

EN:

- **Purpose:** Application shell module.
- **Contains:** Tab host, voice routing, edit modals, side nav. Symbols: ShellTopStatusBars.
- **Symbols:** `ShellTopStatusBars`
- **Does:** Wires bottom nav / desktop rail to feature pages.
- **Connected to:** `app_shell.dart` re-exports `life_os_dashboard.dart`.

RU:

- **Назначение:** Оболочка приложения (shell).
- **Что внутри:** Вкладки, voice routing, edit hosts.
- **Что делает:** Связывает main → features.
- **Как связано с общей логикой:** Re-export через `app_shell.dart`.


### `lib/shell/shell_shared.dart`

EN:

- **Purpose:** Application shell module.
- **Contains:** Tab host, voice routing, edit modals, side nav. Symbols: (library).
- **Does:** Wires bottom nav / desktop rail to feature pages.
- **Connected to:** `app_shell.dart` re-exports `life_os_dashboard.dart`.

RU:

- **Назначение:** Оболочка приложения (shell).
- **Что внутри:** Вкладки, voice routing, edit hosts.
- **Что делает:** Связывает main → features.
- **Как связано с общей логикой:** Re-export через `app_shell.dart`.


### `lib/shell/shell_side_navigation.dart`

EN:

- **Purpose:** Application shell module.
- **Contains:** Tab host, voice routing, edit modals, side nav. Symbols: ShellSideNavigation, ShellSideNavItem.
- **Symbols:** `ShellSideNavigation, ShellSideNavItem`
- **Does:** Wires bottom nav / desktop rail to feature pages.
- **Connected to:** `app_shell.dart` re-exports `life_os_dashboard.dart`.

RU:

- **Назначение:** Оболочка приложения (shell).
- **Что внутри:** Вкладки, voice routing, edit hosts.
- **Что делает:** Связывает main → features.
- **Как связано с общей логикой:** Re-export через `app_shell.dart`.


### `lib/shell/shell_tab_host.dart`

EN:

- **Purpose:** Application shell module.
- **Contains:** Tab host, voice routing, edit modals, side nav. Symbols: ShellTabHost.
- **Symbols:** `ShellTabHost`
- **Does:** Wires bottom nav / desktop rail to feature pages.
- **Connected to:** `app_shell.dart` re-exports `life_os_dashboard.dart`.

RU:

- **Назначение:** Оболочка приложения (shell).
- **Что внутри:** Вкладки, voice routing, edit hosts.
- **Что делает:** Связывает main → features.
- **Как связано с общей логикой:** Re-export через `app_shell.dart`.


### `lib/shell/shell_voice_routing.dart`

EN:

- **Purpose:** Application shell module.
- **Contains:** Tab host, voice routing, edit modals, side nav. Symbols: ShellVoiceRouting.
- **Symbols:** `ShellVoiceRouting`
- **Does:** Wires bottom nav / desktop rail to feature pages.
- **Connected to:** `app_shell.dart` re-exports `life_os_dashboard.dart`.

RU:

- **Назначение:** Оболочка приложения (shell).
- **Что внутри:** Вкладки, voice routing, edit hosts.
- **Что делает:** Связывает main → features.
- **Как связано с общей логикой:** Re-export через `app_shell.dart`.


## lib/ — non-Dart

### `lib/core/env/env.dart.example`

EN:

- **Purpose:** Core foundation utility or widget.
- **Contains:** Theme, time, diagnostics, navigation helpers. Symbols: (library).
- **Does:** Shared non-feature code; must not import `features/` or Brain HTTP.
- **Connected to:** Used across UI modules and sometimes Brain (time helpers).

RU:

- **Назначение:** Базовый слой (core).
- **Что внутри:** Тема, виджеты DS, time/voice утилиты.
- **Что делает:** Не импортирует `features/` и Brain HTTP.
- **Как связано с общей логикой:** Переиспользуется UI и частично Brain.


### `lib/l10n/langs/HELP HOW TO UPDATE the languages`

EN:

- **Purpose:** Localization catalog.
- **Contains:** Dictionary keys and locale maps. Symbols: (library).
- **Does:** Provides `t()` lookup for UI strings.
- **Connected to:** Features import `dictionary.dart`; EN/RU are canonical SSOT.

RU:

- **Назначение:** Файл проекта.
- **Что внутри:** См. англ. блок Purpose/Contains.
- **Что делает:** Смотри префикс пути для владения.
- **Как связано с общей логикой:** Правила слоёв — `docs/APP_STRUCTURE.md`.


## test/

### `test/app_hotkey_keycaps_test.dart`

EN:

- **Purpose:** Automated test.
- **Contains:** Widget/unit/regression coverage. Symbols: main.
- **Symbols:** `main`
- **Does:** Guards domain/UI contracts (`flutter test`).
- **Connected to:** Maps to nearby production module by name.

RU:

- **Назначение:** Автотест.
- **Что внутри:** Покрытие регрессий домена/UI.
- **Что делает:** Запуск: `flutter test`.
- **Как связано с общей логикой:** Соответствует модулю по имени файла.


### `test/app_timezone_icon_test.dart`

EN:

- **Purpose:** Automated test.
- **Contains:** Widget/unit/regression coverage. Symbols: main.
- **Symbols:** `main`
- **Does:** Guards domain/UI contracts (`flutter test`).
- **Connected to:** Maps to nearby production module by name.

RU:

- **Назначение:** Автотест.
- **Что внутри:** Покрытие регрессий домена/UI.
- **Что делает:** Запуск: `flutter test`.
- **Как связано с общей логикой:** Соответствует модулю по имени файла.


### `test/desktop_voice_attempt_dialog_test.dart`

EN:

- **Purpose:** Automated test.
- **Contains:** Widget/unit/regression coverage. Symbols: main.
- **Symbols:** `main`
- **Does:** Guards domain/UI contracts (`flutter test`).
- **Connected to:** Maps to nearby production module by name.

RU:

- **Назначение:** Автотест.
- **Что внутри:** Покрытие регрессий домена/UI.
- **Что делает:** Запуск: `flutter test`.
- **Как связано с общей логикой:** Соответствует модулю по имени файла.


### `test/desktop_voice_attempt_log_test.dart`

EN:

- **Purpose:** Automated test.
- **Contains:** Widget/unit/regression coverage. Symbols: main.
- **Symbols:** `main`
- **Does:** Guards domain/UI contracts (`flutter test`).
- **Connected to:** Maps to nearby production module by name.

RU:

- **Назначение:** Автотест.
- **Что внутри:** Покрытие регрессий домена/UI.
- **Что делает:** Запуск: `flutter test`.
- **Как связано с общей логикой:** Соответствует модулю по имени файла.


### `test/desktop_voice_command_acceptance_test.dart`

EN:

- **Purpose:** Automated test.
- **Contains:** Widget/unit/regression coverage. Symbols: main.
- **Symbols:** `main`
- **Does:** Guards domain/UI contracts (`flutter test`).
- **Connected to:** Maps to nearby production module by name.

RU:

- **Назначение:** Автотест.
- **Что внутри:** Покрытие регрессий домена/UI.
- **Что делает:** Запуск: `flutter test`.
- **Как связано с общей логикой:** Соответствует модулю по имени файла.


### `test/desktop_voice_helper_failure_test.dart`

EN:

- **Purpose:** Automated test.
- **Contains:** Widget/unit/regression coverage. Symbols: main.
- **Symbols:** `main`
- **Does:** Guards domain/UI contracts (`flutter test`).
- **Connected to:** Maps to nearby production module by name.

RU:

- **Назначение:** Автотест.
- **Что внутри:** Покрытие регрессий домена/UI.
- **Что делает:** Запуск: `flutter test`.
- **Как связано с общей логикой:** Соответствует модулю по имени файла.


### `test/desktop_voice_hotkey_self_acceptance_test.dart`

EN:

- **Purpose:** Automated test.
- **Contains:** Widget/unit/regression coverage. Symbols: main.
- **Symbols:** `main`
- **Does:** Guards domain/UI contracts (`flutter test`).
- **Connected to:** Maps to nearby production module by name.

RU:

- **Назначение:** Автотест.
- **Что внутри:** Покрытие регрессий домена/UI.
- **Что делает:** Запуск: `flutter test`.
- **Как связано с общей логикой:** Соответствует модулю по имени файла.


### `test/desktop_voice_hotkey_state_machine_test.dart`

EN:

- **Purpose:** Automated test.
- **Contains:** Widget/unit/regression coverage. Symbols: main.
- **Symbols:** `main`
- **Does:** Guards domain/UI contracts (`flutter test`).
- **Connected to:** Maps to nearby production module by name.

RU:

- **Назначение:** Автотест.
- **Что внутри:** Покрытие регрессий домена/UI.
- **Что делает:** Запуск: `flutter test`.
- **Как связано с общей логикой:** Соответствует модулю по имени файла.


### `test/desktop_voice_laredo_multi_scope_test.dart`

EN:

- **Purpose:** Automated test.
- **Contains:** Widget/unit/regression coverage. Symbols: main.
- **Symbols:** `main`
- **Does:** Guards domain/UI contracts (`flutter test`).
- **Connected to:** Maps to nearby production module by name.

RU:

- **Назначение:** Автотест.
- **Что внутри:** Покрытие регрессий домена/UI.
- **Что делает:** Запуск: `flutter test`.
- **Как связано с общей логикой:** Соответствует модулю по имени файла.


### `test/desktop_voice_mic_peak_visibility_test.dart`

EN:

- **Purpose:** Automated test.
- **Contains:** Widget/unit/regression coverage. Symbols: main.
- **Symbols:** `main`
- **Does:** Guards domain/UI contracts (`flutter test`).
- **Connected to:** Maps to nearby production module by name.

RU:

- **Назначение:** Автотест.
- **Что внутри:** Покрытие регрессий домена/UI.
- **Что делает:** Запуск: `flutter test`.
- **Как связано с общей логикой:** Соответствует модулю по имени файла.


### `test/desktop_voice_no_preparing_ui_test.dart`

EN:

- **Purpose:** Automated test.
- **Contains:** Widget/unit/regression coverage. Symbols: main.
- **Symbols:** `main`
- **Does:** Guards domain/UI contracts (`flutter test`).
- **Connected to:** Maps to nearby production module by name.

RU:

- **Назначение:** Автотест.
- **Что внутри:** Покрытие регрессий домена/UI.
- **Что делает:** Запуск: `flutter test`.
- **Как связано с общей логикой:** Соответствует модулю по имени файла.


### `test/desktop_voice_overlay_state_test.dart`

EN:

- **Purpose:** Automated test.
- **Contains:** Widget/unit/regression coverage. Symbols: main.
- **Symbols:** `main`
- **Does:** Guards domain/UI contracts (`flutter test`).
- **Connected to:** Maps to nearby production module by name.

RU:

- **Назначение:** Автотест.
- **Что внутри:** Покрытие регрессий домена/UI.
- **Что делает:** Запуск: `flutter test`.
- **Как связано с общей логикой:** Соответствует модулю по имени файла.


### `test/desktop_voice_production_submit_test.dart`

EN:

- **Purpose:** Automated test.
- **Contains:** Widget/unit/regression coverage. Symbols: main.
- **Symbols:** `main`
- **Does:** Guards domain/UI contracts (`flutter test`).
- **Connected to:** Maps to nearby production module by name.

RU:

- **Назначение:** Автотест.
- **Что внутри:** Покрытие регрессий домена/UI.
- **Что делает:** Запуск: `flutter test`.
- **Как связано с общей логикой:** Соответствует модулю по имени файла.


### `test/desktop_voice_settings_mic_layout_test.dart`

EN:

- **Purpose:** Automated test.
- **Contains:** Widget/unit/regression coverage. Symbols: _TestMicrophoneCard, main.
- **Symbols:** `_TestMicrophoneCard, main`
- **Does:** Guards domain/UI contracts (`flutter test`).
- **Connected to:** Maps to nearby production module by name.

RU:

- **Назначение:** Автотест.
- **Что внутри:** Покрытие регрессий домена/UI.
- **Что делает:** Запуск: `flutter test`.
- **Как связано с общей логикой:** Соответствует модулю по имени файла.


### `test/desktop_voice_user_error_test.dart`

EN:

- **Purpose:** Automated test.
- **Contains:** Widget/unit/regression coverage. Symbols: main.
- **Symbols:** `main`
- **Does:** Guards domain/UI contracts (`flutter test`).
- **Connected to:** Maps to nearby production module by name.

RU:

- **Назначение:** Автотест.
- **Что внутри:** Покрытие регрессий домена/UI.
- **Что делает:** Запуск: `flutter test`.
- **Как связано с общей логикой:** Соответствует модулю по имени файла.


### `test/desktop_voice_widget_e2e_test.dart`

EN:

- **Purpose:** Automated test.
- **Contains:** Widget/unit/regression coverage. Symbols: _E2EOutcome, main.
- **Symbols:** `_E2EOutcome, main`
- **Does:** Guards domain/UI contracts (`flutter test`).
- **Connected to:** Maps to nearby production module by name.

RU:

- **Назначение:** Автотест.
- **Что внутри:** Покрытие регрессий домена/UI.
- **Что делает:** Запуск: `flutter test`.
- **Как связано с общей логикой:** Соответствует модулю по имени файла.


### `test/edit_sheet_autosave_test.dart`

EN:

- **Purpose:** Automated test.
- **Contains:** Widget/unit/regression coverage. Symbols: main.
- **Symbols:** `main`
- **Does:** Guards domain/UI contracts (`flutter test`).
- **Connected to:** Maps to nearby production module by name.

RU:

- **Назначение:** Автотест.
- **Что внутри:** Покрытие регрессий домена/UI.
- **Что делает:** Запуск: `flutter test`.
- **Как связано с общей логикой:** Соответствует модулю по имени файла.


### `test/outbox_coalesce_regression_test.dart`

EN:

- **Purpose:** Automated test.
- **Contains:** Widget/unit/regression coverage. Symbols: main.
- **Symbols:** `main`
- **Does:** Guards domain/UI contracts (`flutter test`).
- **Connected to:** Maps to nearby production module by name.

RU:

- **Назначение:** Автотест.
- **Что внутри:** Покрытие регрессий домена/UI.
- **Что делает:** Запуск: `flutter test`.
- **Как связано с общей логикой:** Соответствует модулю по имени файла.


### `test/perf_date_swipe_diag_test.dart`

EN:

- **Purpose:** Automated test.
- **Contains:** Widget/unit/regression coverage. Symbols: main.
- **Symbols:** `main`
- **Does:** Guards domain/UI contracts (`flutter test`).
- **Connected to:** Maps to nearby production module by name.

RU:

- **Назначение:** Автотест.
- **Что внутри:** Покрытие регрессий домена/UI.
- **Что делает:** Запуск: `flutter test`.
- **Как связано с общей логикой:** Соответствует модулю по имени файла.


### `test/perf_shell_date_settle_test.dart`

EN:

- **Purpose:** Automated test.
- **Contains:** Widget/unit/regression coverage. Symbols: _ShellDateHarness, _ShellDateHarnessState, main.
- **Symbols:** `_ShellDateHarness, _ShellDateHarnessState, main`
- **Does:** Guards domain/UI contracts (`flutter test`).
- **Connected to:** Maps to nearby production module by name.

RU:

- **Назначение:** Автотест.
- **Что внутри:** Покрытие регрессий домена/UI.
- **Что делает:** Запуск: `flutter test`.
- **Как связано с общей логикой:** Соответствует модулю по имени файла.


### `test/plan_recurrence_scope_test.dart`

EN:

- **Purpose:** Automated test.
- **Contains:** Widget/unit/regression coverage. Symbols: main.
- **Symbols:** `main`
- **Does:** Guards domain/UI contracts (`flutter test`).
- **Connected to:** Maps to nearby production module by name.

RU:

- **Назначение:** Автотест.
- **Что внутри:** Покрытие регрессий домена/UI.
- **Что делает:** Запуск: `flutter test`.
- **Как связано с общей логикой:** Соответствует модулю по имени файла.


### `test/plan_time_bulk_drag_test.dart`

EN:

- **Purpose:** Automated test.
- **Contains:** Widget/unit/regression coverage. Symbols: main.
- **Symbols:** `main`
- **Does:** Guards domain/UI contracts (`flutter test`).
- **Connected to:** Maps to nearby production module by name.

RU:

- **Назначение:** Автотест.
- **Что внутри:** Покрытие регрессий домена/UI.
- **Что делает:** Запуск: `flutter test`.
- **Как связано с общей логикой:** Соответствует модулю по имени файла.


### `test/plan_time_drag_gesture_contract_test.dart`

EN:

- **Purpose:** Automated test.
- **Contains:** Widget/unit/regression coverage. Symbols: main.
- **Symbols:** `main`
- **Does:** Guards domain/UI contracts (`flutter test`).
- **Connected to:** Maps to nearby production module by name.

RU:

- **Назначение:** Автотест.
- **Что внутри:** Покрытие регрессий домена/UI.
- **Что делает:** Запуск: `flutter test`.
- **Как связано с общей логикой:** Соответствует модулю по имени файла.


### `test/plan_time_duration_fidelity_test.dart`

EN:

- **Purpose:** Automated test.
- **Contains:** Widget/unit/regression coverage. Symbols: main.
- **Symbols:** `main`
- **Does:** Guards domain/UI contracts (`flutter test`).
- **Connected to:** Maps to nearby production module by name.

RU:

- **Назначение:** Автотест.
- **Что внутри:** Покрытие регрессий домена/UI.
- **Что делает:** Запуск: `flutter test`.
- **Как связано с общей логикой:** Соответствует модулю по имени файла.


### `test/plan_time_fixed_time_policy_test.dart`

EN:

- **Purpose:** Automated test.
- **Contains:** Widget/unit/regression coverage. Symbols: main.
- **Symbols:** `main`
- **Does:** Guards domain/UI contracts (`flutter test`).
- **Connected to:** Maps to nearby production module by name.

RU:

- **Назначение:** Автотест.
- **Что внутри:** Покрытие регрессий домена/UI.
- **Что делает:** Запуск: `flutter test`.
- **Как связано с общей логикой:** Соответствует модулю по имени файла.


### `test/plan_time_sequential_cascade_test.dart`

EN:

- **Purpose:** Automated test.
- **Contains:** Widget/unit/regression coverage. Symbols: main.
- **Symbols:** `main`
- **Does:** Guards domain/UI contracts (`flutter test`).
- **Connected to:** Maps to nearby production module by name.

RU:

- **Назначение:** Автотест.
- **Что внутри:** Покрытие регрессий домена/UI.
- **Что делает:** Запуск: `flutter test`.
- **Как связано с общей логикой:** Соответствует модулю по имени файла.


### `test/plan_time_target_drop_test.dart`

EN:

- **Purpose:** Automated test.
- **Contains:** Widget/unit/regression coverage. Symbols: main.
- **Symbols:** `main`
- **Does:** Guards domain/UI contracts (`flutter test`).
- **Connected to:** Maps to nearby production module by name.

RU:

- **Назначение:** Автотест.
- **Что внутри:** Покрытие регрессий домена/UI.
- **Что делает:** Запуск: `flutter test`.
- **Как связано с общей логикой:** Соответствует модулю по имени файла.


### `test/plan_time_timezone_projection_test.dart`

EN:

- **Purpose:** Automated test.
- **Contains:** Widget/unit/regression coverage. Symbols: main.
- **Symbols:** `main`
- **Does:** Guards domain/UI contracts (`flutter test`).
- **Connected to:** Maps to nearby production module by name.

RU:

- **Назначение:** Автотест.
- **Что внутри:** Покрытие регрессий домена/UI.
- **Что делает:** Запуск: `flutter test`.
- **Как связано с общей логикой:** Соответствует модулю по имени файла.


### `test/plan_time_view_layout_test.dart`

EN:

- **Purpose:** Automated test.
- **Contains:** Widget/unit/regression coverage. Symbols: main.
- **Symbols:** `main`
- **Does:** Guards domain/UI contracts (`flutter test`).
- **Connected to:** Maps to nearby production module by name.

RU:

- **Назначение:** Автотест.
- **Что внутри:** Покрытие регрессий домена/UI.
- **Что делает:** Запуск: `flutter test`.
- **Как связано с общей логикой:** Соответствует модулю по имени файла.


### `test/plan_time_visible_window_test.dart`

EN:

- **Purpose:** Automated test.
- **Contains:** Widget/unit/regression coverage. Symbols: main.
- **Symbols:** `main`
- **Does:** Guards domain/UI contracts (`flutter test`).
- **Connected to:** Maps to nearby production module by name.

RU:

- **Назначение:** Автотест.
- **Что внутри:** Покрытие регрессий домена/UI.
- **Что делает:** Запуск: `flutter test`.
- **Как связано с общей логикой:** Соответствует модулю по имени файла.


### `test/planning_duplicate_plan_guard_test.dart`

EN:

- **Purpose:** Automated test.
- **Contains:** Widget/unit/regression coverage. Symbols: main.
- **Symbols:** `main`
- **Does:** Guards domain/UI contracts (`flutter test`).
- **Connected to:** Maps to nearby production module by name.

RU:

- **Назначение:** Автотест.
- **Что внутри:** Покрытие регрессий домена/UI.
- **Что делает:** Запуск: `flutter test`.
- **Как связано с общей логикой:** Соответствует модулю по имени файла.


### `test/planning_realtime_stream_lifecycle_test.dart`

EN:

- **Purpose:** Automated test.
- **Contains:** Widget/unit/regression coverage. Symbols: main.
- **Symbols:** `main`
- **Does:** Guards domain/UI contracts (`flutter test`).
- **Connected to:** Maps to nearby production module by name.

RU:

- **Назначение:** Автотест.
- **Что внутри:** Покрытие регрессий домена/UI.
- **Что делает:** Запуск: `flutter test`.
- **Как связано с общей логикой:** Соответствует модулю по имени файла.


### `test/profile_timezone_catalog_test.dart`

EN:

- **Purpose:** Automated test.
- **Contains:** Widget/unit/regression coverage. Symbols: main.
- **Symbols:** `main`
- **Does:** Guards domain/UI contracts (`flutter test`).
- **Connected to:** Maps to nearby production module by name.

RU:

- **Назначение:** Автотест.
- **Что внутри:** Покрытие регрессий домена/UI.
- **Что делает:** Запуск: `flutter test`.
- **Как связано с общей логикой:** Соответствует модулю по имени файла.


### `test/smart_input_parser_test.dart`

EN:

- **Purpose:** Automated test.
- **Contains:** Widget/unit/regression coverage. Symbols: main.
- **Symbols:** `main`
- **Does:** Guards domain/UI contracts (`flutter test`).
- **Connected to:** Maps to nearby production module by name.

RU:

- **Назначение:** Автотест.
- **Что внутри:** Покрытие регрессий домена/UI.
- **Что делает:** Запуск: `flutter test`.
- **Как связано с общей логикой:** Соответствует модулю по имени файла.


### `test/timezone_quick_picker_test.dart`

EN:

- **Purpose:** Automated test.
- **Contains:** Widget/unit/regression coverage. Symbols: main.
- **Symbols:** `main`
- **Does:** Guards domain/UI contracts (`flutter test`).
- **Connected to:** Maps to nearby production module by name.

RU:

- **Назначение:** Автотест.
- **Что внутри:** Покрытие регрессий домена/UI.
- **Что делает:** Запуск: `flutter test`.
- **Как связано с общей логикой:** Соответствует модулю по имени файла.


### `test/voice_command_parser_test.dart`

EN:

- **Purpose:** Automated test.
- **Contains:** Widget/unit/regression coverage. Symbols: main.
- **Symbols:** `main`
- **Does:** Guards domain/UI contracts (`flutter test`).
- **Connected to:** Maps to nearby production module by name.

RU:

- **Назначение:** Автотест.
- **Что внутри:** Покрытие регрессий домена/UI.
- **Что делает:** Запуск: `flutter test`.
- **Как связано с общей логикой:** Соответствует модулю по имени файла.


### `test/widget_test.dart`

EN:

- **Purpose:** Automated test.
- **Contains:** Widget/unit/regression coverage. Symbols: main.
- **Symbols:** `main`
- **Does:** Guards domain/UI contracts (`flutter test`).
- **Connected to:** Maps to nearby production module by name.

RU:

- **Назначение:** Автотест.
- **Что внутри:** Покрытие регрессий домена/UI.
- **Что делает:** Запуск: `flutter test`.
- **Как связано с общей логикой:** Соответствует модулю по имени файла.


## scripts/

### `scripts/audit/architecture_guard.ps1`

EN:

- **Purpose:** Developer/CI script.
- **Contains:** Audit, deploy, locale sync, or one-off maintenance. Symbols: (library).
- **Does:** Not shipped in app binary.
- **Connected to:** Documented in `docs/DEPLOY.md` or audit reports when workflow-critical.

RU:

- **Назначение:** Скрипт разработки/CI.
- **Что внутри:** Аудит, деплой, синхронизация.
- **Что делает:** Не входит в релизный бинарник.
- **Как связано с общей логикой:** См. `docs/DEPLOY.md` для deploy-пути.


### `scripts/manual/check_no_preparing_ui.ps1`

EN:

- **Purpose:** Developer/CI script.
- **Contains:** Audit, deploy, locale sync, or one-off maintenance. Symbols: (library).
- **Does:** Not shipped in app binary.
- **Connected to:** Documented in `docs/DEPLOY.md` or audit reports when workflow-critical.

RU:

- **Назначение:** Скрипт разработки/CI.
- **Что внутри:** Аудит, деплой, синхронизация.
- **Что делает:** Не входит в релизный бинарник.
- **Как связано с общей логикой:** См. `docs/DEPLOY.md` для deploy-пути.


### `scripts/manual/export_price_reporter_timesheet.dart`

EN:

- **Purpose:** Developer/CI script.
- **Contains:** Audit, deploy, locale sync, or one-off maintenance. Symbols: (library).
- **Does:** Not shipped in app binary.
- **Connected to:** Documented in `docs/DEPLOY.md` or audit reports when workflow-critical.

RU:

- **Назначение:** Скрипт разработки/CI.
- **Что внутри:** Аудит, деплой, синхронизация.
- **Что делает:** Не входит в релизный бинарник.
- **Как связано с общей логикой:** См. `docs/DEPLOY.md` для deploy-пути.


### `scripts/manual/generate_app_structure_detailed.py`

EN:

- **Purpose:** Developer/CI script.
- **Contains:** Audit, deploy, locale sync, or one-off maintenance. Symbols: (library).
- **Does:** Not shipped in app binary.
- **Connected to:** Documented in `docs/DEPLOY.md` or audit reports when workflow-critical.

RU:

- **Назначение:** Скрипт разработки/CI.
- **Что внутри:** Аудит, деплой, синхронизация.
- **Что делает:** Не входит в релизный бинарник.
- **Как связано с общей логикой:** См. `docs/DEPLOY.md` для deploy-пути.


### `scripts/manual/run_desktop_voice_acceptance.ps1`

EN:

- **Purpose:** Developer/CI script.
- **Contains:** Audit, deploy, locale sync, or one-off maintenance. Symbols: (library).
- **Does:** Not shipped in app binary.
- **Connected to:** Documented in `docs/DEPLOY.md` or audit reports when workflow-critical.

RU:

- **Назначение:** Скрипт разработки/CI.
- **Что внутри:** Аудит, деплой, синхронизация.
- **Что делает:** Не входит в релизный бинарник.
- **Как связано с общей логикой:** См. `docs/DEPLOY.md` для deploy-пути.


### `scripts/manual/smoke_desktop_hotkey.ps1`

EN:

- **Purpose:** Developer/CI script.
- **Contains:** Audit, deploy, locale sync, or one-off maintenance. Symbols: (library).
- **Does:** Not shipped in app binary.
- **Connected to:** Documented in `docs/DEPLOY.md` or audit reports when workflow-critical.

RU:

- **Назначение:** Скрипт разработки/CI.
- **Что внутри:** Аудит, деплой, синхронизация.
- **Что делает:** Не входит в релизный бинарник.
- **Как связано с общей логикой:** См. `docs/DEPLOY.md` для deploy-пути.


### `scripts/manual/smoke_desktop_voice_helper_failure.ps1`

EN:

- **Purpose:** Developer/CI script.
- **Contains:** Audit, deploy, locale sync, or one-off maintenance. Symbols: (library).
- **Does:** Not shipped in app binary.
- **Connected to:** Documented in `docs/DEPLOY.md` or audit reports when workflow-critical.

RU:

- **Назначение:** Скрипт разработки/CI.
- **Что внутри:** Аудит, деплой, синхронизация.
- **Что делает:** Не входит в релизный бинарник.
- **Как связано с общей логикой:** См. `docs/DEPLOY.md` для deploy-пути.


### `scripts/manual/smoke_desktop_voice_helper_selftest.ps1`

EN:

- **Purpose:** Developer/CI script.
- **Contains:** Audit, deploy, locale sync, or one-off maintenance. Symbols: (library).
- **Does:** Not shipped in app binary.
- **Connected to:** Documented in `docs/DEPLOY.md` or audit reports when workflow-critical.

RU:

- **Назначение:** Скрипт разработки/CI.
- **Что внутри:** Аудит, деплой, синхронизация.
- **Что делает:** Не входит в релизный бинарник.
- **Как связано с общей логикой:** См. `docs/DEPLOY.md` для deploy-пути.


### `scripts/manual/smoke_desktop_voice_recording_first.ps1`

EN:

- **Purpose:** Developer/CI script.
- **Contains:** Audit, deploy, locale sync, or one-off maintenance. Symbols: (library).
- **Does:** Not shipped in app binary.
- **Connected to:** Documented in `docs/DEPLOY.md` or audit reports when workflow-critical.

RU:

- **Назначение:** Скрипт разработки/CI.
- **Что внутри:** Аудит, деплой, синхронизация.
- **Что делает:** Не входит в релизный бинарник.
- **Как связано с общей логикой:** См. `docs/DEPLOY.md` для deploy-пути.


### `scripts/manual/structure_scan.ps1`

EN:

- **Purpose:** Developer/CI script.
- **Contains:** Audit, deploy, locale sync, or one-off maintenance. Symbols: (library).
- **Does:** Not shipped in app binary.
- **Connected to:** Documented in `docs/DEPLOY.md` or audit reports when workflow-critical.

RU:

- **Назначение:** Скрипт разработки/CI.
- **Что внутри:** Аудит, деплой, синхронизация.
- **Что делает:** Не входит в релизный бинарник.
- **Как связано с общей логикой:** См. `docs/DEPLOY.md` для deploy-пути.


### `scripts/manual/td`

EN:

- **Purpose:** Developer/CI script.
- **Contains:** Audit, deploy, locale sync, or one-off maintenance. Symbols: (library).
- **Does:** Not shipped in app binary.
- **Connected to:** Documented in `docs/DEPLOY.md` or audit reports when workflow-critical.

RU:

- **Назначение:** Скрипт разработки/CI.
- **Что внутри:** Аудит, деплой, синхронизация.
- **Что делает:** Не входит в релизный бинарник.
- **Как связано с общей логикой:** См. `docs/DEPLOY.md` для deploy-пути.


### `scripts/manual/td.ps1`

EN:

- **Purpose:** Developer/CI script.
- **Contains:** Audit, deploy, locale sync, or one-off maintenance. Symbols: (library).
- **Does:** Not shipped in app binary.
- **Connected to:** Documented in `docs/DEPLOY.md` or audit reports when workflow-critical.

RU:

- **Назначение:** Скрипт разработки/CI.
- **Что внутри:** Аудит, деплой, синхронизация.
- **Что делает:** Не входит в релизный бинарник.
- **Как связано с общей логикой:** См. `docs/DEPLOY.md` для deploy-пути.


### `scripts/sync_locales.dart`

EN:

- **Purpose:** Developer/CI script.
- **Contains:** Audit, deploy, locale sync, or one-off maintenance. Symbols: main.
- **Symbols:** `main`
- **Does:** Not shipped in app binary.
- **Connected to:** Documented in `docs/DEPLOY.md` or audit reports when workflow-critical.

RU:

- **Назначение:** Скрипт разработки/CI.
- **Что внутри:** Аудит, деплой, синхронизация.
- **Что делает:** Не входит в релизный бинарник.
- **Как связано с общей логикой:** См. `docs/DEPLOY.md` для deploy-пути.


## docs/

### `docs/APP_STRUCTURE.md`

EN:

- **Purpose:** Project documentation.
- **Contains:** Architecture, data map, reports. Symbols: (library).
- **Does:** Governing specs for Brain/UI behavior.
- **Connected to:** Must stay aligned with code tree (this file).

RU:

- **Назначение:** Документация проекта.
- **Что внутри:** Контракты архитектуры и DATA_MAP.
- **Что делает:** Источник правды для полей PB.
- **Как связано с общей логикой:** Должна совпадать с деревом файлов.


### `docs/APP_STRUCTURE_DETAILED.md`

EN:

- **Purpose:** Project documentation.
- **Contains:** Architecture, data map, reports. Symbols: (library).
- **Does:** Governing specs for Brain/UI behavior.
- **Connected to:** Must stay aligned with code tree (this file).

RU:

- **Назначение:** Документация проекта.
- **Что внутри:** Контракты архитектуры и DATA_MAP.
- **Что делает:** Источник правды для полей PB.
- **Как связано с общей логикой:** Должна совпадать с деревом файлов.


### `docs/ARCHITECTURE.md`

EN:

- **Purpose:** Project documentation.
- **Contains:** Architecture, data map, reports. Symbols: (library).
- **Does:** Governing specs for Brain/UI behavior.
- **Connected to:** Must stay aligned with code tree (this file).

RU:

- **Назначение:** Документация проекта.
- **Что внутри:** Контракты архитектуры и DATA_MAP.
- **Что делает:** Источник правды для полей PB.
- **Как связано с общей логикой:** Должна совпадать с деревом файлов.


### `docs/DATA_MAP.md`

EN:

- **Purpose:** Project documentation.
- **Contains:** Architecture, data map, reports. Symbols: (library).
- **Does:** Governing specs for Brain/UI behavior.
- **Connected to:** Must stay aligned with code tree (this file).

RU:

- **Назначение:** Документация проекта.
- **Что внутри:** Контракты архитектуры и DATA_MAP.
- **Что делает:** Источник правды для полей PB.
- **Как связано с общей логикой:** Должна совпадать с деревом файлов.


### `docs/DEPLOY.md`

EN:

- **Purpose:** Project documentation.
- **Contains:** Architecture, data map, reports. Symbols: (library).
- **Does:** Governing specs for Brain/UI behavior.
- **Connected to:** Must stay aligned with code tree (this file).

RU:

- **Назначение:** Документация проекта.
- **Что внутри:** Контракты архитектуры и DATA_MAP.
- **Что делает:** Источник правды для полей PB.
- **Как связано с общей логикой:** Должна совпадать с деревом файлов.


### `docs/DESIGN_SYSTEM.md`

EN:

- **Purpose:** Project documentation.
- **Contains:** Architecture, data map, reports. Symbols: (library).
- **Does:** Governing specs for Brain/UI behavior.
- **Connected to:** Must stay aligned with code tree (this file).

RU:

- **Назначение:** Документация проекта.
- **Что внутри:** Контракты архитектуры и DATA_MAP.
- **Что делает:** Источник правды для полей PB.
- **Как связано с общей логикой:** Должна совпадать с деревом файлов.


### `docs/POCKETBASE_MANIFEST.md`

EN:

- **Purpose:** Project documentation.
- **Contains:** Architecture, data map, reports. Symbols: (library).
- **Does:** Governing specs for Brain/UI behavior.
- **Connected to:** Must stay aligned with code tree (this file).

RU:

- **Назначение:** Документация проекта.
- **Что внутри:** Контракты архитектуры и DATA_MAP.
- **Что делает:** Источник правды для полей PB.
- **Как связано с общей логикой:** Должна совпадать с деревом файлов.


### `docs/PROJECT_KNOWLEDGE_PACK.md`

EN:

- **Purpose:** Project documentation.
- **Contains:** Architecture, data map, reports. Symbols: (library).
- **Does:** Governing specs for Brain/UI behavior.
- **Connected to:** Must stay aligned with code tree (this file).

RU:

- **Назначение:** Документация проекта.
- **Что внутри:** Контракты архитектуры и DATA_MAP.
- **Что делает:** Источник правды для полей PB.
- **Как связано с общей логикой:** Должна совпадать с деревом файлов.


### `docs/ROADMAP.md`

EN:

- **Purpose:** Project documentation.
- **Contains:** Architecture, data map, reports. Symbols: (library).
- **Does:** Governing specs for Brain/UI behavior.
- **Connected to:** Must stay aligned with code tree (this file).

RU:

- **Назначение:** Документация проекта.
- **Что внутри:** Контракты архитектуры и DATA_MAP.
- **Что делает:** Источник правды для полей PB.
- **Как связано с общей логикой:** Должна совпадать с деревом файлов.


### `docs/UX_CONTRACT.md`

EN:

- **Purpose:** Project documentation.
- **Contains:** Architecture, data map, reports. Symbols: (library).
- **Does:** Governing specs for Brain/UI behavior.
- **Connected to:** Must stay aligned with code tree (this file).

RU:

- **Назначение:** Документация проекта.
- **Что внутри:** Контракты архитектуры и DATA_MAP.
- **Что делает:** Источник правды для полей PB.
- **Как связано с общей логикой:** Должна совпадать с деревом файлов.


### `docs/reports/DESIGN_SYSTEM_INVENTORY.md`

EN:

- **Purpose:** Project documentation.
- **Contains:** Architecture, data map, reports. Symbols: (library).
- **Does:** Governing specs for Brain/UI behavior.
- **Connected to:** Must stay aligned with code tree (this file).

RU:

- **Назначение:** Документация проекта.
- **Что внутри:** Контракты архитектуры и DATA_MAP.
- **Что делает:** Источник правды для полей PB.
- **Как связано с общей логикой:** Должна совпадать с деревом файлов.


### `docs/reports/FINAL_STRUCTURE_PARITY_AND_DOC_CLEANUP_2026-07-03.md`

EN:

- **Purpose:** Project documentation.
- **Contains:** Architecture, data map, reports. Symbols: (library).
- **Does:** Governing specs for Brain/UI behavior.
- **Connected to:** Must stay aligned with code tree (this file).

RU:

- **Назначение:** Документация проекта.
- **Что внутри:** Контракты архитектуры и DATA_MAP.
- **Что делает:** Источник правды для полей PB.
- **Как связано с общей логикой:** Должна совпадать с деревом файлов.


### `docs/website/CONTENT_LIBRARY.md`

EN:

- **Purpose:** Project documentation.
- **Contains:** Architecture, data map, reports. Symbols: (library).
- **Does:** Governing specs for Brain/UI behavior.
- **Connected to:** Must stay aligned with code tree (this file).

RU:

- **Назначение:** Документация проекта.
- **Что внутри:** Контракты архитектуры и DATA_MAP.
- **Что делает:** Источник правды для полей PB.
- **Как связано с общей логикой:** Должна совпадать с деревом файлов.


### `docs/website/FEATURE_MATRIX.md`

EN:

- **Purpose:** Project documentation.
- **Contains:** Architecture, data map, reports. Symbols: (library).
- **Does:** Governing specs for Brain/UI behavior.
- **Connected to:** Must stay aligned with code tree (this file).

RU:

- **Назначение:** Документация проекта.
- **Что внутри:** Контракты архитектуры и DATA_MAP.
- **Что делает:** Источник правды для полей PB.
- **Как связано с общей логикой:** Должна совпадать с деревом файлов.


### `docs/website/HOMEPAGE_WIREFRAME_V1.md`

EN:

- **Purpose:** Project documentation.
- **Contains:** Architecture, data map, reports. Symbols: (library).
- **Does:** Governing specs for Brain/UI behavior.
- **Connected to:** Must stay aligned with code tree (this file).

RU:

- **Назначение:** Документация проекта.
- **Что внутри:** Контракты архитектуры и DATA_MAP.
- **Что делает:** Источник правды для полей PB.
- **Как связано с общей логикой:** Должна совпадать с деревом файлов.


### `docs/website/INTERNAL_NOTES_NOT_FOR_SITE.md`

EN:

- **Purpose:** Project documentation.
- **Contains:** Architecture, data map, reports. Symbols: (library).
- **Does:** Governing specs for Brain/UI behavior.
- **Connected to:** Must stay aligned with code tree (this file).

RU:

- **Назначение:** Документация проекта.
- **Что внутри:** Контракты архитектуры и DATA_MAP.
- **Что делает:** Источник правды для полей PB.
- **Как связано с общей логикой:** Должна совпадать с деревом файлов.


### `docs/website/POSITIONING_V1.md`

EN:

- **Purpose:** Project documentation.
- **Contains:** Architecture, data map, reports. Symbols: (library).
- **Does:** Governing specs for Brain/UI behavior.
- **Connected to:** Must stay aligned with code tree (this file).

RU:

- **Назначение:** Документация проекта.
- **Что внутри:** Контракты архитектуры и DATA_MAP.
- **Что делает:** Источник правды для полей PB.
- **Как связано с общей логикой:** Должна совпадать с деревом файлов.


### `docs/website/PRODUCT_INVENTORY.md`

EN:

- **Purpose:** Project documentation.
- **Contains:** Architecture, data map, reports. Symbols: (library).
- **Does:** Governing specs for Brain/UI behavior.
- **Connected to:** Must stay aligned with code tree (this file).

RU:

- **Назначение:** Документация проекта.
- **Что внутри:** Контракты архитектуры и DATA_MAP.
- **Что делает:** Источник правды для полей PB.
- **Как связано с общей логикой:** Должна совпадать с деревом файлов.


### `docs/website/PUBLIC_COPY_DRAFTS.md`

EN:

- **Purpose:** Project documentation.
- **Contains:** Architecture, data map, reports. Symbols: (library).
- **Does:** Governing specs for Brain/UI behavior.
- **Connected to:** Must stay aligned with code tree (this file).

RU:

- **Назначение:** Документация проекта.
- **Что внутри:** Контракты архитектуры и DATA_MAP.
- **Что делает:** Источник правды для полей PB.
- **Как связано с общей логикой:** Должна совпадать с деревом файлов.


### `docs/website/SCREENSHOT_SHOTLIST.md`

EN:

- **Purpose:** Project documentation.
- **Contains:** Architecture, data map, reports. Symbols: (library).
- **Does:** Governing specs for Brain/UI behavior.
- **Connected to:** Must stay aligned with code tree (this file).

RU:

- **Назначение:** Документация проекта.
- **Что внутри:** Контракты архитектуры и DATA_MAP.
- **Что делает:** Источник правды для полей PB.
- **Как связано с общей логикой:** Должна совпадать с деревом файлов.


### `docs/website/WEBSITE_CLAIMS_REVIEW.md`

EN:

- **Purpose:** Project documentation.
- **Contains:** Architecture, data map, reports. Symbols: (library).
- **Does:** Governing specs for Brain/UI behavior.
- **Connected to:** Must stay aligned with code tree (this file).

RU:

- **Назначение:** Документация проекта.
- **Что внутри:** Контракты архитектуры и DATA_MAP.
- **Что делает:** Источник правды для полей PB.
- **Как связано с общей логикой:** Должна совпадать с деревом файлов.


### `docs/website/WEBSITE_IMPLEMENTATION_OPTIONS.md`

EN:

- **Purpose:** Project documentation.
- **Contains:** Architecture, data map, reports. Symbols: (library).
- **Does:** Governing specs for Brain/UI behavior.
- **Connected to:** Must stay aligned with code tree (this file).

RU:

- **Назначение:** Документация проекта.
- **Что внутри:** Контракты архитектуры и DATA_MAP.
- **Что делает:** Источник правды для полей PB.
- **Как связано с общей логикой:** Должна совпадать с деревом файлов.


### `docs/website/WEBSITE_PAGE_STRUCTURE.md`

EN:

- **Purpose:** Project documentation.
- **Contains:** Architecture, data map, reports. Symbols: (library).
- **Does:** Governing specs for Brain/UI behavior.
- **Connected to:** Must stay aligned with code tree (this file).

RU:

- **Назначение:** Документация проекта.
- **Что внутри:** Контракты архитектуры и DATA_MAP.
- **Что делает:** Источник правды для полей PB.
- **Как связано с общей логикой:** Должна совпадать с деревом файлов.


### `docs/website/WEBSITE_TZ.md`

EN:

- **Purpose:** Project documentation.
- **Contains:** Architecture, data map, reports. Symbols: (library).
- **Does:** Governing specs for Brain/UI behavior.
- **Connected to:** Must stay aligned with code tree (this file).

RU:

- **Назначение:** Документация проекта.
- **Что внутри:** Контракты архитектуры и DATA_MAP.
- **Что делает:** Источник правды для полей PB.
- **Как связано с общей логикой:** Должна совпадать с деревом файлов.


### `docs/website/WEBSITE_V1_SCOPE.md`

EN:

- **Purpose:** Project documentation.
- **Contains:** Architecture, data map, reports. Symbols: (library).
- **Does:** Governing specs for Brain/UI behavior.
- **Connected to:** Must stay aligned with code tree (this file).

RU:

- **Назначение:** Документация проекта.
- **Что внутри:** Контракты архитектуры и DATA_MAP.
- **Что делает:** Источник правды для полей PB.
- **Как связано с общей логикой:** Должна совпадать с деревом файлов.


## web/

### `web/favicon.png`

EN:

- **Purpose:** Platform embedder/config.
- **Contains:** Native runner, manifest, or web shell. Symbols: (library).
- **Does:** Flutter tooling generates most files; app logic stays in `lib/`.
- **Connected to:** Build targets for mobile/desktop/web CI.

RU:

- **Назначение:** Файл проекта.
- **Что внутри:** См. англ. блок Purpose/Contains.
- **Что делает:** Смотри префикс пути для владения.
- **Как связано с общей логикой:** Правила слоёв — `docs/APP_STRUCTURE.md`.


### `web/icons/Icon-192.png`

EN:

- **Purpose:** Platform embedder/config.
- **Contains:** Native runner, manifest, or web shell. Symbols: (library).
- **Does:** Flutter tooling generates most files; app logic stays in `lib/`.
- **Connected to:** Build targets for mobile/desktop/web CI.

RU:

- **Назначение:** Файл проекта.
- **Что внутри:** См. англ. блок Purpose/Contains.
- **Что делает:** Смотри префикс пути для владения.
- **Как связано с общей логикой:** Правила слоёв — `docs/APP_STRUCTURE.md`.


### `web/icons/Icon-512.png`

EN:

- **Purpose:** Platform embedder/config.
- **Contains:** Native runner, manifest, or web shell. Symbols: (library).
- **Does:** Flutter tooling generates most files; app logic stays in `lib/`.
- **Connected to:** Build targets for mobile/desktop/web CI.

RU:

- **Назначение:** Файл проекта.
- **Что внутри:** См. англ. блок Purpose/Contains.
- **Что делает:** Смотри префикс пути для владения.
- **Как связано с общей логикой:** Правила слоёв — `docs/APP_STRUCTURE.md`.


### `web/icons/Icon-maskable-192.png`

EN:

- **Purpose:** Platform embedder/config.
- **Contains:** Native runner, manifest, or web shell. Symbols: (library).
- **Does:** Flutter tooling generates most files; app logic stays in `lib/`.
- **Connected to:** Build targets for mobile/desktop/web CI.

RU:

- **Назначение:** Файл проекта.
- **Что внутри:** См. англ. блок Purpose/Contains.
- **Что делает:** Смотри префикс пути для владения.
- **Как связано с общей логикой:** Правила слоёв — `docs/APP_STRUCTURE.md`.


### `web/icons/Icon-maskable-512.png`

EN:

- **Purpose:** Platform embedder/config.
- **Contains:** Native runner, manifest, or web shell. Symbols: (library).
- **Does:** Flutter tooling generates most files; app logic stays in `lib/`.
- **Connected to:** Build targets for mobile/desktop/web CI.

RU:

- **Назначение:** Файл проекта.
- **Что внутри:** См. англ. блок Purpose/Contains.
- **Что делает:** Смотри префикс пути для владения.
- **Как связано с общей логикой:** Правила слоёв — `docs/APP_STRUCTURE.md`.


### `web/index.html`

EN:

- **Purpose:** Platform embedder/config.
- **Contains:** Native runner, manifest, or web shell. Symbols: (library).
- **Does:** Flutter tooling generates most files; app logic stays in `lib/`.
- **Connected to:** Build targets for mobile/desktop/web CI.

RU:

- **Назначение:** Файл проекта.
- **Что внутри:** См. англ. блок Purpose/Contains.
- **Что делает:** Смотри префикс пути для владения.
- **Как связано с общей логикой:** Правила слоёв — `docs/APP_STRUCTURE.md`.


### `web/manifest.json`

EN:

- **Purpose:** Platform embedder/config.
- **Contains:** Native runner, manifest, or web shell. Symbols: (library).
- **Does:** Flutter tooling generates most files; app logic stays in `lib/`.
- **Connected to:** Build targets for mobile/desktop/web CI.

RU:

- **Назначение:** Файл проекта.
- **Что внутри:** См. англ. блок Purpose/Contains.
- **Что делает:** Смотри префикс пути для владения.
- **Как связано с общей логикой:** Правила слоёв — `docs/APP_STRUCTURE.md`.


## android/ (selected)

### `android/.gitignore`

EN:

- **Purpose:** Platform embedder/config.
- **Contains:** Native runner, manifest, or web shell. Symbols: (library).
- **Does:** Flutter tooling generates most files; app logic stays in `lib/`.
- **Connected to:** Build targets for mobile/desktop/web CI.

RU:

- **Назначение:** Файл проекта.
- **Что внутри:** См. англ. блок Purpose/Contains.
- **Что делает:** Смотри префикс пути для владения.
- **Как связано с общей логикой:** Правила слоёв — `docs/APP_STRUCTURE.md`.


### `android/app/build.gradle.kts`

EN:

- **Purpose:** Platform embedder/config.
- **Contains:** Native runner, manifest, or web shell. Symbols: (library).
- **Does:** Flutter tooling generates most files; app logic stays in `lib/`.
- **Connected to:** Build targets for mobile/desktop/web CI.

RU:

- **Назначение:** Файл проекта.
- **Что внутри:** См. англ. блок Purpose/Contains.
- **Что делает:** Смотри префикс пути для владения.
- **Как связано с общей логикой:** Правила слоёв — `docs/APP_STRUCTURE.md`.


### `android/app/google-services.json`

EN:

- **Purpose:** Platform embedder/config.
- **Contains:** Native runner, manifest, or web shell. Symbols: (library).
- **Does:** Flutter tooling generates most files; app logic stays in `lib/`.
- **Connected to:** Build targets for mobile/desktop/web CI.

RU:

- **Назначение:** Файл проекта.
- **Что внутри:** См. англ. блок Purpose/Contains.
- **Что делает:** Смотри префикс пути для владения.
- **Как связано с общей логикой:** Правила слоёв — `docs/APP_STRUCTURE.md`.


### `android/app/proguard-rules.pro`

EN:

- **Purpose:** Platform embedder/config.
- **Contains:** Native runner, manifest, or web shell. Symbols: (library).
- **Does:** Flutter tooling generates most files; app logic stays in `lib/`.
- **Connected to:** Build targets for mobile/desktop/web CI.

RU:

- **Назначение:** Файл проекта.
- **Что внутри:** См. англ. блок Purpose/Contains.
- **Что делает:** Смотри префикс пути для владения.
- **Как связано с общей логикой:** Правила слоёв — `docs/APP_STRUCTURE.md`.


### `android/build.gradle.kts`

EN:

- **Purpose:** Platform embedder/config.
- **Contains:** Native runner, manifest, or web shell. Symbols: (library).
- **Does:** Flutter tooling generates most files; app logic stays in `lib/`.
- **Connected to:** Build targets for mobile/desktop/web CI.

RU:

- **Назначение:** Файл проекта.
- **Что внутри:** См. англ. блок Purpose/Contains.
- **Что делает:** Смотри префикс пути для владения.
- **Как связано с общей логикой:** Правила слоёв — `docs/APP_STRUCTURE.md`.


### `android/gradle.properties`

EN:

- **Purpose:** Platform embedder/config.
- **Contains:** Native runner, manifest, or web shell. Symbols: (library).
- **Does:** Flutter tooling generates most files; app logic stays in `lib/`.
- **Connected to:** Build targets for mobile/desktop/web CI.

RU:

- **Назначение:** Файл проекта.
- **Что внутри:** См. англ. блок Purpose/Contains.
- **Что делает:** Смотри префикс пути для владения.
- **Как связано с общей логикой:** Правила слоёв — `docs/APP_STRUCTURE.md`.


### `android/gradle/wrapper/gradle-wrapper.properties`

EN:

- **Purpose:** Platform embedder/config.
- **Contains:** Native runner, manifest, or web shell. Symbols: (library).
- **Does:** Flutter tooling generates most files; app logic stays in `lib/`.
- **Connected to:** Build targets for mobile/desktop/web CI.

RU:

- **Назначение:** Файл проекта.
- **Что внутри:** См. англ. блок Purpose/Contains.
- **Что делает:** Смотри префикс пути для владения.
- **Как связано с общей логикой:** Правила слоёв — `docs/APP_STRUCTURE.md`.


### `android/settings.gradle.kts`

EN:

- **Purpose:** Platform embedder/config.
- **Contains:** Native runner, manifest, or web shell. Symbols: (library).
- **Does:** Flutter tooling generates most files; app logic stays in `lib/`.
- **Connected to:** Build targets for mobile/desktop/web CI.

RU:

- **Назначение:** Файл проекта.
- **Что внутри:** См. англ. блок Purpose/Contains.
- **Что делает:** Смотри префикс пути для владения.
- **Как связано с общей логикой:** Правила слоёв — `docs/APP_STRUCTURE.md`.


## ios/ (selected)

### `ios/.gitignore`

EN:

- **Purpose:** Platform embedder/config.
- **Contains:** Native runner, manifest, or web shell. Symbols: (library).
- **Does:** Flutter tooling generates most files; app logic stays in `lib/`.
- **Connected to:** Build targets for mobile/desktop/web CI.

RU:

- **Назначение:** Файл проекта.
- **Что внутри:** См. англ. блок Purpose/Contains.
- **Что делает:** Смотри префикс пути для владения.
- **Как связано с общей логикой:** Правила слоёв — `docs/APP_STRUCTURE.md`.


### `ios/Flutter/AppFrameworkInfo.plist`

EN:

- **Purpose:** Platform embedder/config.
- **Contains:** Native runner, manifest, or web shell. Symbols: (library).
- **Does:** Flutter tooling generates most files; app logic stays in `lib/`.
- **Connected to:** Build targets for mobile/desktop/web CI.

RU:

- **Назначение:** Файл проекта.
- **Что внутри:** См. англ. блок Purpose/Contains.
- **Что делает:** Смотри префикс пути для владения.
- **Как связано с общей логикой:** Правила слоёв — `docs/APP_STRUCTURE.md`.


### `ios/Flutter/Debug.xcconfig`

EN:

- **Purpose:** Platform embedder/config.
- **Contains:** Native runner, manifest, or web shell. Symbols: (library).
- **Does:** Flutter tooling generates most files; app logic stays in `lib/`.
- **Connected to:** Build targets for mobile/desktop/web CI.

RU:

- **Назначение:** Файл проекта.
- **Что внутри:** См. англ. блок Purpose/Contains.
- **Что делает:** Смотри префикс пути для владения.
- **Как связано с общей логикой:** Правила слоёв — `docs/APP_STRUCTURE.md`.


### `ios/Flutter/Release.xcconfig`

EN:

- **Purpose:** Platform embedder/config.
- **Contains:** Native runner, manifest, or web shell. Symbols: (library).
- **Does:** Flutter tooling generates most files; app logic stays in `lib/`.
- **Connected to:** Build targets for mobile/desktop/web CI.

RU:

- **Назначение:** Файл проекта.
- **Что внутри:** См. англ. блок Purpose/Contains.
- **Что делает:** Смотри префикс пути для владения.
- **Как связано с общей логикой:** Правила слоёв — `docs/APP_STRUCTURE.md`.


### `ios/Runner.xcodeproj/project.pbxproj`

EN:

- **Purpose:** Platform embedder/config.
- **Contains:** Native runner, manifest, or web shell. Symbols: (library).
- **Does:** Flutter tooling generates most files; app logic stays in `lib/`.
- **Connected to:** Build targets for mobile/desktop/web CI.

RU:

- **Назначение:** Файл проекта.
- **Что внутри:** См. англ. блок Purpose/Contains.
- **Что делает:** Смотри префикс пути для владения.
- **Как связано с общей логикой:** Правила слоёв — `docs/APP_STRUCTURE.md`.


### `ios/Runner.xcodeproj/project.xcworkspace/contents.xcworkspacedata`

EN:

- **Purpose:** Platform embedder/config.
- **Contains:** Native runner, manifest, or web shell. Symbols: (library).
- **Does:** Flutter tooling generates most files; app logic stays in `lib/`.
- **Connected to:** Build targets for mobile/desktop/web CI.

RU:

- **Назначение:** Файл проекта.
- **Что внутри:** См. англ. блок Purpose/Contains.
- **Что делает:** Смотри префикс пути для владения.
- **Как связано с общей логикой:** Правила слоёв — `docs/APP_STRUCTURE.md`.


### `ios/Runner.xcworkspace/contents.xcworkspacedata`

EN:

- **Purpose:** Platform embedder/config.
- **Contains:** Native runner, manifest, or web shell. Symbols: (library).
- **Does:** Flutter tooling generates most files; app logic stays in `lib/`.
- **Connected to:** Build targets for mobile/desktop/web CI.

RU:

- **Назначение:** Файл проекта.
- **Что внутри:** См. англ. блок Purpose/Contains.
- **Что делает:** Смотри префикс пути для владения.
- **Как связано с общей логикой:** Правила слоёв — `docs/APP_STRUCTURE.md`.


### `ios/Runner.xcworkspace/xcshareddata/IDEWorkspaceChecks.plist`

EN:

- **Purpose:** Platform embedder/config.
- **Contains:** Native runner, manifest, or web shell. Symbols: (library).
- **Does:** Flutter tooling generates most files; app logic stays in `lib/`.
- **Connected to:** Build targets for mobile/desktop/web CI.

RU:

- **Назначение:** Файл проекта.
- **Что внутри:** См. англ. блок Purpose/Contains.
- **Что делает:** Смотри префикс пути для владения.
- **Как связано с общей логикой:** Правила слоёв — `docs/APP_STRUCTURE.md`.


### `ios/Runner.xcworkspace/xcshareddata/WorkspaceSettings.xcsettings`

EN:

- **Purpose:** Platform embedder/config.
- **Contains:** Native runner, manifest, or web shell. Symbols: (library).
- **Does:** Flutter tooling generates most files; app logic stays in `lib/`.
- **Connected to:** Build targets for mobile/desktop/web CI.

RU:

- **Назначение:** Файл проекта.
- **Что внутри:** См. англ. блок Purpose/Contains.
- **Что делает:** Смотри префикс пути для владения.
- **Как связано с общей логикой:** Правила слоёв — `docs/APP_STRUCTURE.md`.


### `ios/Runner/AppDelegate.swift`

EN:

- **Purpose:** Platform embedder/config.
- **Contains:** Native runner, manifest, or web shell. Symbols: (library).
- **Does:** Flutter tooling generates most files; app logic stays in `lib/`.
- **Connected to:** Build targets for mobile/desktop/web CI.

RU:

- **Назначение:** Файл проекта.
- **Что внутри:** См. англ. блок Purpose/Contains.
- **Что делает:** Смотри префикс пути для владения.
- **Как связано с общей логикой:** Правила слоёв — `docs/APP_STRUCTURE.md`.


### `ios/Runner/Base.lproj/LaunchScreen.storyboard`

EN:

- **Purpose:** Platform embedder/config.
- **Contains:** Native runner, manifest, or web shell. Symbols: (library).
- **Does:** Flutter tooling generates most files; app logic stays in `lib/`.
- **Connected to:** Build targets for mobile/desktop/web CI.

RU:

- **Назначение:** Файл проекта.
- **Что внутри:** См. англ. блок Purpose/Contains.
- **Что делает:** Смотри префикс пути для владения.
- **Как связано с общей логикой:** Правила слоёв — `docs/APP_STRUCTURE.md`.


### `ios/Runner/Base.lproj/Main.storyboard`

EN:

- **Purpose:** Platform embedder/config.
- **Contains:** Native runner, manifest, or web shell. Symbols: (library).
- **Does:** Flutter tooling generates most files; app logic stays in `lib/`.
- **Connected to:** Build targets for mobile/desktop/web CI.

RU:

- **Назначение:** Файл проекта.
- **Что внутри:** См. англ. блок Purpose/Contains.
- **Что делает:** Смотри префикс пути для владения.
- **Как связано с общей логикой:** Правила слоёв — `docs/APP_STRUCTURE.md`.


### `ios/Runner/Info.plist`

EN:

- **Purpose:** Platform embedder/config.
- **Contains:** Native runner, manifest, or web shell. Symbols: (library).
- **Does:** Flutter tooling generates most files; app logic stays in `lib/`.
- **Connected to:** Build targets for mobile/desktop/web CI.

RU:

- **Назначение:** Файл проекта.
- **Что внутри:** См. англ. блок Purpose/Contains.
- **Что делает:** Смотри префикс пути для владения.
- **Как связано с общей логикой:** Правила слоёв — `docs/APP_STRUCTURE.md`.


### `ios/Runner/Runner-Bridging-Header.h`

EN:

- **Purpose:** Platform embedder/config.
- **Contains:** Native runner, manifest, or web shell. Symbols: (library).
- **Does:** Flutter tooling generates most files; app logic stays in `lib/`.
- **Connected to:** Build targets for mobile/desktop/web CI.

RU:

- **Назначение:** Файл проекта.
- **Что внутри:** См. англ. блок Purpose/Contains.
- **Что делает:** Смотри префикс пути для владения.
- **Как связано с общей логикой:** Правила слоёв — `docs/APP_STRUCTURE.md`.


### `ios/Runner/SceneDelegate.swift`

EN:

- **Purpose:** Platform embedder/config.
- **Contains:** Native runner, manifest, or web shell. Symbols: (library).
- **Does:** Flutter tooling generates most files; app logic stays in `lib/`.
- **Connected to:** Build targets for mobile/desktop/web CI.

RU:

- **Назначение:** Файл проекта.
- **Что внутри:** См. англ. блок Purpose/Contains.
- **Что делает:** Смотри префикс пути для владения.
- **Как связано с общей логикой:** Правила слоёв — `docs/APP_STRUCTURE.md`.


### `ios/RunnerTests/RunnerTests.swift`

EN:

- **Purpose:** Platform embedder/config.
- **Contains:** Native runner, manifest, or web shell. Symbols: (library).
- **Does:** Flutter tooling generates most files; app logic stays in `lib/`.
- **Connected to:** Build targets for mobile/desktop/web CI.

RU:

- **Назначение:** Файл проекта.
- **Что внутри:** См. англ. блок Purpose/Contains.
- **Что делает:** Смотри префикс пути для владения.
- **Как связано с общей логикой:** Правила слоёв — `docs/APP_STRUCTURE.md`.


## windows/ (selected)

### `windows/.gitignore`

EN:

- **Purpose:** Platform embedder/config.
- **Contains:** Native runner, manifest, or web shell. Symbols: (library).
- **Does:** Flutter tooling generates most files; app logic stays in `lib/`.
- **Connected to:** Build targets for mobile/desktop/web CI.

RU:

- **Назначение:** Файл проекта.
- **Что внутри:** См. англ. блок Purpose/Contains.
- **Что делает:** Смотри префикс пути для владения.
- **Как связано с общей логикой:** Правила слоёв — `docs/APP_STRUCTURE.md`.


### `windows/CMakeLists.txt`

EN:

- **Purpose:** Platform embedder/config.
- **Contains:** Native runner, manifest, or web shell. Symbols: (library).
- **Does:** Flutter tooling generates most files; app logic stays in `lib/`.
- **Connected to:** Build targets for mobile/desktop/web CI.

RU:

- **Назначение:** Файл проекта.
- **Что внутри:** См. англ. блок Purpose/Contains.
- **Что делает:** Смотри префикс пути для владения.
- **Как связано с общей логикой:** Правила слоёв — `docs/APP_STRUCTURE.md`.


### `windows/flutter/CMakeLists.txt`

EN:

- **Purpose:** Platform embedder/config.
- **Contains:** Native runner, manifest, or web shell. Symbols: (library).
- **Does:** Flutter tooling generates most files; app logic stays in `lib/`.
- **Connected to:** Build targets for mobile/desktop/web CI.

RU:

- **Назначение:** Файл проекта.
- **Что внутри:** См. англ. блок Purpose/Contains.
- **Что делает:** Смотри префикс пути для владения.
- **Как связано с общей логикой:** Правила слоёв — `docs/APP_STRUCTURE.md`.


### `windows/flutter/generated_plugin_registrant.cc`

EN:

- **Purpose:** Platform embedder/config.
- **Contains:** Native runner, manifest, or web shell. Symbols: (library).
- **Does:** Flutter tooling generates most files; app logic stays in `lib/`.
- **Connected to:** Build targets for mobile/desktop/web CI.

RU:

- **Назначение:** Файл проекта.
- **Что внутри:** См. англ. блок Purpose/Contains.
- **Что делает:** Смотри префикс пути для владения.
- **Как связано с общей логикой:** Правила слоёв — `docs/APP_STRUCTURE.md`.


### `windows/flutter/generated_plugin_registrant.h`

EN:

- **Purpose:** Platform embedder/config.
- **Contains:** Native runner, manifest, or web shell. Symbols: (library).
- **Does:** Flutter tooling generates most files; app logic stays in `lib/`.
- **Connected to:** Build targets for mobile/desktop/web CI.

RU:

- **Назначение:** Файл проекта.
- **Что внутри:** См. англ. блок Purpose/Contains.
- **Что делает:** Смотри префикс пути для владения.
- **Как связано с общей логикой:** Правила слоёв — `docs/APP_STRUCTURE.md`.


### `windows/flutter/generated_plugins.cmake`

EN:

- **Purpose:** Platform embedder/config.
- **Contains:** Native runner, manifest, or web shell. Symbols: (library).
- **Does:** Flutter tooling generates most files; app logic stays in `lib/`.
- **Connected to:** Build targets for mobile/desktop/web CI.

RU:

- **Назначение:** Файл проекта.
- **Что внутри:** См. англ. блок Purpose/Contains.
- **Что делает:** Смотри префикс пути для владения.
- **Как связано с общей логикой:** Правила слоёв — `docs/APP_STRUCTURE.md`.


### `windows/runner/CMakeLists.txt`

EN:

- **Purpose:** Platform embedder/config.
- **Contains:** Native runner, manifest, or web shell. Symbols: (library).
- **Does:** Flutter tooling generates most files; app logic stays in `lib/`.
- **Connected to:** Build targets for mobile/desktop/web CI.

RU:

- **Назначение:** Файл проекта.
- **Что внутри:** См. англ. блок Purpose/Contains.
- **Что делает:** Смотри префикс пути для владения.
- **Как связано с общей логикой:** Правила слоёв — `docs/APP_STRUCTURE.md`.


### `windows/runner/Runner.rc`

EN:

- **Purpose:** Platform embedder/config.
- **Contains:** Native runner, manifest, or web shell. Symbols: (library).
- **Does:** Flutter tooling generates most files; app logic stays in `lib/`.
- **Connected to:** Build targets for mobile/desktop/web CI.

RU:

- **Назначение:** Файл проекта.
- **Что внутри:** См. англ. блок Purpose/Contains.
- **Что делает:** Смотри префикс пути для владения.
- **Как связано с общей логикой:** Правила слоёв — `docs/APP_STRUCTURE.md`.


### `windows/runner/desktop_voice_native_overlay.cpp`

EN:

- **Purpose:** Platform embedder/config.
- **Contains:** Native runner, manifest, or web shell. Symbols: (library).
- **Does:** Flutter tooling generates most files; app logic stays in `lib/`.
- **Connected to:** Build targets for mobile/desktop/web CI.

RU:

- **Назначение:** Файл проекта.
- **Что внутри:** См. англ. блок Purpose/Contains.
- **Что делает:** Смотри префикс пути для владения.
- **Как связано с общей логикой:** Правила слоёв — `docs/APP_STRUCTURE.md`.


### `windows/runner/desktop_voice_native_overlay.h`

EN:

- **Purpose:** Platform embedder/config.
- **Contains:** Native runner, manifest, or web shell. Symbols: (library).
- **Does:** Flutter tooling generates most files; app logic stays in `lib/`.
- **Connected to:** Build targets for mobile/desktop/web CI.

RU:

- **Назначение:** Файл проекта.
- **Что внутри:** См. англ. блок Purpose/Contains.
- **Что делает:** Смотри префикс пути для владения.
- **Как связано с общей логикой:** Правила слоёв — `docs/APP_STRUCTURE.md`.


### `windows/runner/flutter_window.cpp`

EN:

- **Purpose:** Platform embedder/config.
- **Contains:** Native runner, manifest, or web shell. Symbols: (library).
- **Does:** Flutter tooling generates most files; app logic stays in `lib/`.
- **Connected to:** Build targets for mobile/desktop/web CI.

RU:

- **Назначение:** Файл проекта.
- **Что внутри:** См. англ. блок Purpose/Contains.
- **Что делает:** Смотри префикс пути для владения.
- **Как связано с общей логикой:** Правила слоёв — `docs/APP_STRUCTURE.md`.


### `windows/runner/flutter_window.h`

EN:

- **Purpose:** Platform embedder/config.
- **Contains:** Native runner, manifest, or web shell. Symbols: (library).
- **Does:** Flutter tooling generates most files; app logic stays in `lib/`.
- **Connected to:** Build targets for mobile/desktop/web CI.

RU:

- **Назначение:** Файл проекта.
- **Что внутри:** См. англ. блок Purpose/Contains.
- **Что делает:** Смотри префикс пути для владения.
- **Как связано с общей логикой:** Правила слоёв — `docs/APP_STRUCTURE.md`.


### `windows/runner/main.cpp`

EN:

- **Purpose:** Platform embedder/config.
- **Contains:** Native runner, manifest, or web shell. Symbols: (library).
- **Does:** Flutter tooling generates most files; app logic stays in `lib/`.
- **Connected to:** Build targets for mobile/desktop/web CI.

RU:

- **Назначение:** Файл проекта.
- **Что внутри:** См. англ. блок Purpose/Contains.
- **Что делает:** Смотри префикс пути для владения.
- **Как связано с общей логикой:** Правила слоёв — `docs/APP_STRUCTURE.md`.


### `windows/runner/resource.h`

EN:

- **Purpose:** Platform embedder/config.
- **Contains:** Native runner, manifest, or web shell. Symbols: (library).
- **Does:** Flutter tooling generates most files; app logic stays in `lib/`.
- **Connected to:** Build targets for mobile/desktop/web CI.

RU:

- **Назначение:** Файл проекта.
- **Что внутри:** См. англ. блок Purpose/Contains.
- **Что делает:** Смотри префикс пути для владения.
- **Как связано с общей логикой:** Правила слоёв — `docs/APP_STRUCTURE.md`.


### `windows/runner/resources/app_icon.ico`

EN:

- **Purpose:** Platform embedder/config.
- **Contains:** Native runner, manifest, or web shell. Symbols: (library).
- **Does:** Flutter tooling generates most files; app logic stays in `lib/`.
- **Connected to:** Build targets for mobile/desktop/web CI.

RU:

- **Назначение:** Файл проекта.
- **Что внутри:** См. англ. блок Purpose/Contains.
- **Что делает:** Смотри префикс пути для владения.
- **Как связано с общей логикой:** Правила слоёв — `docs/APP_STRUCTURE.md`.


### `windows/runner/runner.exe.manifest`

EN:

- **Purpose:** Platform embedder/config.
- **Contains:** Native runner, manifest, or web shell. Symbols: (library).
- **Does:** Flutter tooling generates most files; app logic stays in `lib/`.
- **Connected to:** Build targets for mobile/desktop/web CI.

RU:

- **Назначение:** Файл проекта.
- **Что внутри:** См. англ. блок Purpose/Contains.
- **Что делает:** Смотри префикс пути для владения.
- **Как связано с общей логикой:** Правила слоёв — `docs/APP_STRUCTURE.md`.


### `windows/runner/utils.cpp`

EN:

- **Purpose:** Platform embedder/config.
- **Contains:** Native runner, manifest, or web shell. Symbols: (library).
- **Does:** Flutter tooling generates most files; app logic stays in `lib/`.
- **Connected to:** Build targets for mobile/desktop/web CI.

RU:

- **Назначение:** Файл проекта.
- **Что внутри:** См. англ. блок Purpose/Contains.
- **Что делает:** Смотри префикс пути для владения.
- **Как связано с общей логикой:** Правила слоёв — `docs/APP_STRUCTURE.md`.


### `windows/runner/utils.h`

EN:

- **Purpose:** Platform embedder/config.
- **Contains:** Native runner, manifest, or web shell. Symbols: (library).
- **Does:** Flutter tooling generates most files; app logic stays in `lib/`.
- **Connected to:** Build targets for mobile/desktop/web CI.

RU:

- **Назначение:** Файл проекта.
- **Что внутри:** См. англ. блок Purpose/Contains.
- **Что делает:** Смотри префикс пути для владения.
- **Как связано с общей логикой:** Правила слоёв — `docs/APP_STRUCTURE.md`.


### `windows/runner/win32_window.cpp`

EN:

- **Purpose:** Platform embedder/config.
- **Contains:** Native runner, manifest, or web shell. Symbols: (library).
- **Does:** Flutter tooling generates most files; app logic stays in `lib/`.
- **Connected to:** Build targets for mobile/desktop/web CI.

RU:

- **Назначение:** Файл проекта.
- **Что внутри:** См. англ. блок Purpose/Contains.
- **Что делает:** Смотри префикс пути для владения.
- **Как связано с общей логикой:** Правила слоёв — `docs/APP_STRUCTURE.md`.


### `windows/runner/win32_window.h`

EN:

- **Purpose:** Platform embedder/config.
- **Contains:** Native runner, manifest, or web shell. Symbols: (library).
- **Does:** Flutter tooling generates most files; app logic stays in `lib/`.
- **Connected to:** Build targets for mobile/desktop/web CI.

RU:

- **Назначение:** Файл проекта.
- **Что внутри:** См. англ. блок Purpose/Contains.
- **Что делает:** Смотри префикс пути для владения.
- **Как связано с общей логикой:** Правила слоёв — `docs/APP_STRUCTURE.md`.


## linux/ (selected)

### `linux/.gitignore`

EN:

- **Purpose:** Platform embedder/config.
- **Contains:** Native runner, manifest, or web shell. Symbols: (library).
- **Does:** Flutter tooling generates most files; app logic stays in `lib/`.
- **Connected to:** Build targets for mobile/desktop/web CI.

RU:

- **Назначение:** Файл проекта.
- **Что внутри:** См. англ. блок Purpose/Contains.
- **Что делает:** Смотри префикс пути для владения.
- **Как связано с общей логикой:** Правила слоёв — `docs/APP_STRUCTURE.md`.


### `linux/CMakeLists.txt`

EN:

- **Purpose:** Platform embedder/config.
- **Contains:** Native runner, manifest, or web shell. Symbols: (library).
- **Does:** Flutter tooling generates most files; app logic stays in `lib/`.
- **Connected to:** Build targets for mobile/desktop/web CI.

RU:

- **Назначение:** Файл проекта.
- **Что внутри:** См. англ. блок Purpose/Contains.
- **Что делает:** Смотри префикс пути для владения.
- **Как связано с общей логикой:** Правила слоёв — `docs/APP_STRUCTURE.md`.


### `linux/flutter/CMakeLists.txt`

EN:

- **Purpose:** Platform embedder/config.
- **Contains:** Native runner, manifest, or web shell. Symbols: (library).
- **Does:** Flutter tooling generates most files; app logic stays in `lib/`.
- **Connected to:** Build targets for mobile/desktop/web CI.

RU:

- **Назначение:** Файл проекта.
- **Что внутри:** См. англ. блок Purpose/Contains.
- **Что делает:** Смотри префикс пути для владения.
- **Как связано с общей логикой:** Правила слоёв — `docs/APP_STRUCTURE.md`.


### `linux/flutter/generated_plugin_registrant.cc`

EN:

- **Purpose:** Platform embedder/config.
- **Contains:** Native runner, manifest, or web shell. Symbols: (library).
- **Does:** Flutter tooling generates most files; app logic stays in `lib/`.
- **Connected to:** Build targets for mobile/desktop/web CI.

RU:

- **Назначение:** Файл проекта.
- **Что внутри:** См. англ. блок Purpose/Contains.
- **Что делает:** Смотри префикс пути для владения.
- **Как связано с общей логикой:** Правила слоёв — `docs/APP_STRUCTURE.md`.


### `linux/flutter/generated_plugin_registrant.h`

EN:

- **Purpose:** Platform embedder/config.
- **Contains:** Native runner, manifest, or web shell. Symbols: (library).
- **Does:** Flutter tooling generates most files; app logic stays in `lib/`.
- **Connected to:** Build targets for mobile/desktop/web CI.

RU:

- **Назначение:** Файл проекта.
- **Что внутри:** См. англ. блок Purpose/Contains.
- **Что делает:** Смотри префикс пути для владения.
- **Как связано с общей логикой:** Правила слоёв — `docs/APP_STRUCTURE.md`.


### `linux/flutter/generated_plugins.cmake`

EN:

- **Purpose:** Platform embedder/config.
- **Contains:** Native runner, manifest, or web shell. Symbols: (library).
- **Does:** Flutter tooling generates most files; app logic stays in `lib/`.
- **Connected to:** Build targets for mobile/desktop/web CI.

RU:

- **Назначение:** Файл проекта.
- **Что внутри:** См. англ. блок Purpose/Contains.
- **Что делает:** Смотри префикс пути для владения.
- **Как связано с общей логикой:** Правила слоёв — `docs/APP_STRUCTURE.md`.


### `linux/runner/CMakeLists.txt`

EN:

- **Purpose:** Platform embedder/config.
- **Contains:** Native runner, manifest, or web shell. Symbols: (library).
- **Does:** Flutter tooling generates most files; app logic stays in `lib/`.
- **Connected to:** Build targets for mobile/desktop/web CI.

RU:

- **Назначение:** Файл проекта.
- **Что внутри:** См. англ. блок Purpose/Contains.
- **Что делает:** Смотри префикс пути для владения.
- **Как связано с общей логикой:** Правила слоёв — `docs/APP_STRUCTURE.md`.


### `linux/runner/main.cc`

EN:

- **Purpose:** Platform embedder/config.
- **Contains:** Native runner, manifest, or web shell. Symbols: (library).
- **Does:** Flutter tooling generates most files; app logic stays in `lib/`.
- **Connected to:** Build targets for mobile/desktop/web CI.

RU:

- **Назначение:** Файл проекта.
- **Что внутри:** См. англ. блок Purpose/Contains.
- **Что делает:** Смотри префикс пути для владения.
- **Как связано с общей логикой:** Правила слоёв — `docs/APP_STRUCTURE.md`.


### `linux/runner/my_application.cc`

EN:

- **Purpose:** Platform embedder/config.
- **Contains:** Native runner, manifest, or web shell. Symbols: (library).
- **Does:** Flutter tooling generates most files; app logic stays in `lib/`.
- **Connected to:** Build targets for mobile/desktop/web CI.

RU:

- **Назначение:** Файл проекта.
- **Что внутри:** См. англ. блок Purpose/Contains.
- **Что делает:** Смотри префикс пути для владения.
- **Как связано с общей логикой:** Правила слоёв — `docs/APP_STRUCTURE.md`.


### `linux/runner/my_application.h`

EN:

- **Purpose:** Platform embedder/config.
- **Contains:** Native runner, manifest, or web shell. Symbols: (library).
- **Does:** Flutter tooling generates most files; app logic stays in `lib/`.
- **Connected to:** Build targets for mobile/desktop/web CI.

RU:

- **Назначение:** Файл проекта.
- **Что внутри:** См. англ. блок Purpose/Contains.
- **Что делает:** Смотри префикс пути для владения.
- **Как связано с общей логикой:** Правила слоёв — `docs/APP_STRUCTURE.md`.


## macos/ (selected)

### `macos/.gitignore`

EN:

- **Purpose:** Platform embedder/config.
- **Contains:** Native runner, manifest, or web shell. Symbols: (library).
- **Does:** Flutter tooling generates most files; app logic stays in `lib/`.
- **Connected to:** Build targets for mobile/desktop/web CI.

RU:

- **Назначение:** Файл проекта.
- **Что внутри:** См. англ. блок Purpose/Contains.
- **Что делает:** Смотри префикс пути для владения.
- **Как связано с общей логикой:** Правила слоёв — `docs/APP_STRUCTURE.md`.


### `macos/Flutter/Flutter-Debug.xcconfig`

EN:

- **Purpose:** Platform embedder/config.
- **Contains:** Native runner, manifest, or web shell. Symbols: (library).
- **Does:** Flutter tooling generates most files; app logic stays in `lib/`.
- **Connected to:** Build targets for mobile/desktop/web CI.

RU:

- **Назначение:** Файл проекта.
- **Что внутри:** См. англ. блок Purpose/Contains.
- **Что делает:** Смотри префикс пути для владения.
- **Как связано с общей логикой:** Правила слоёв — `docs/APP_STRUCTURE.md`.


### `macos/Flutter/Flutter-Release.xcconfig`

EN:

- **Purpose:** Platform embedder/config.
- **Contains:** Native runner, manifest, or web shell. Symbols: (library).
- **Does:** Flutter tooling generates most files; app logic stays in `lib/`.
- **Connected to:** Build targets for mobile/desktop/web CI.

RU:

- **Назначение:** Файл проекта.
- **Что внутри:** См. англ. блок Purpose/Contains.
- **Что делает:** Смотри префикс пути для владения.
- **Как связано с общей логикой:** Правила слоёв — `docs/APP_STRUCTURE.md`.


### `macos/Flutter/GeneratedPluginRegistrant.swift`

EN:

- **Purpose:** Platform embedder/config.
- **Contains:** Native runner, manifest, or web shell. Symbols: (library).
- **Does:** Flutter tooling generates most files; app logic stays in `lib/`.
- **Connected to:** Build targets for mobile/desktop/web CI.

RU:

- **Назначение:** Файл проекта.
- **Что внутри:** См. англ. блок Purpose/Contains.
- **Что делает:** Смотри префикс пути для владения.
- **Как связано с общей логикой:** Правила слоёв — `docs/APP_STRUCTURE.md`.


### `macos/Runner.xcodeproj/project.pbxproj`

EN:

- **Purpose:** Platform embedder/config.
- **Contains:** Native runner, manifest, or web shell. Symbols: (library).
- **Does:** Flutter tooling generates most files; app logic stays in `lib/`.
- **Connected to:** Build targets for mobile/desktop/web CI.

RU:

- **Назначение:** Файл проекта.
- **Что внутри:** См. англ. блок Purpose/Contains.
- **Что делает:** Смотри префикс пути для владения.
- **Как связано с общей логикой:** Правила слоёв — `docs/APP_STRUCTURE.md`.


### `macos/Runner.xcworkspace/contents.xcworkspacedata`

EN:

- **Purpose:** Platform embedder/config.
- **Contains:** Native runner, manifest, or web shell. Symbols: (library).
- **Does:** Flutter tooling generates most files; app logic stays in `lib/`.
- **Connected to:** Build targets for mobile/desktop/web CI.

RU:

- **Назначение:** Файл проекта.
- **Что внутри:** См. англ. блок Purpose/Contains.
- **Что делает:** Смотри префикс пути для владения.
- **Как связано с общей логикой:** Правила слоёв — `docs/APP_STRUCTURE.md`.


### `macos/Runner.xcworkspace/xcshareddata/IDEWorkspaceChecks.plist`

EN:

- **Purpose:** Platform embedder/config.
- **Contains:** Native runner, manifest, or web shell. Symbols: (library).
- **Does:** Flutter tooling generates most files; app logic stays in `lib/`.
- **Connected to:** Build targets for mobile/desktop/web CI.

RU:

- **Назначение:** Файл проекта.
- **Что внутри:** См. англ. блок Purpose/Contains.
- **Что делает:** Смотри префикс пути для владения.
- **Как связано с общей логикой:** Правила слоёв — `docs/APP_STRUCTURE.md`.


### `macos/Runner/AppDelegate.swift`

EN:

- **Purpose:** Platform embedder/config.
- **Contains:** Native runner, manifest, or web shell. Symbols: (library).
- **Does:** Flutter tooling generates most files; app logic stays in `lib/`.
- **Connected to:** Build targets for mobile/desktop/web CI.

RU:

- **Назначение:** Файл проекта.
- **Что внутри:** См. англ. блок Purpose/Contains.
- **Что делает:** Смотри префикс пути для владения.
- **Как связано с общей логикой:** Правила слоёв — `docs/APP_STRUCTURE.md`.


### `macos/Runner/Base.lproj/MainMenu.xib`

EN:

- **Purpose:** Platform embedder/config.
- **Contains:** Native runner, manifest, or web shell. Symbols: (library).
- **Does:** Flutter tooling generates most files; app logic stays in `lib/`.
- **Connected to:** Build targets for mobile/desktop/web CI.

RU:

- **Назначение:** Файл проекта.
- **Что внутри:** См. англ. блок Purpose/Contains.
- **Что делает:** Смотри префикс пути для владения.
- **Как связано с общей логикой:** Правила слоёв — `docs/APP_STRUCTURE.md`.


### `macos/Runner/Configs/AppInfo.xcconfig`

EN:

- **Purpose:** Platform embedder/config.
- **Contains:** Native runner, manifest, or web shell. Symbols: (library).
- **Does:** Flutter tooling generates most files; app logic stays in `lib/`.
- **Connected to:** Build targets for mobile/desktop/web CI.

RU:

- **Назначение:** Файл проекта.
- **Что внутри:** См. англ. блок Purpose/Contains.
- **Что делает:** Смотри префикс пути для владения.
- **Как связано с общей логикой:** Правила слоёв — `docs/APP_STRUCTURE.md`.


### `macos/Runner/Configs/Debug.xcconfig`

EN:

- **Purpose:** Platform embedder/config.
- **Contains:** Native runner, manifest, or web shell. Symbols: (library).
- **Does:** Flutter tooling generates most files; app logic stays in `lib/`.
- **Connected to:** Build targets for mobile/desktop/web CI.

RU:

- **Назначение:** Файл проекта.
- **Что внутри:** См. англ. блок Purpose/Contains.
- **Что делает:** Смотри префикс пути для владения.
- **Как связано с общей логикой:** Правила слоёв — `docs/APP_STRUCTURE.md`.


### `macos/Runner/Configs/Release.xcconfig`

EN:

- **Purpose:** Platform embedder/config.
- **Contains:** Native runner, manifest, or web shell. Symbols: (library).
- **Does:** Flutter tooling generates most files; app logic stays in `lib/`.
- **Connected to:** Build targets for mobile/desktop/web CI.

RU:

- **Назначение:** Файл проекта.
- **Что внутри:** См. англ. блок Purpose/Contains.
- **Что делает:** Смотри префикс пути для владения.
- **Как связано с общей логикой:** Правила слоёв — `docs/APP_STRUCTURE.md`.


### `macos/Runner/Configs/Warnings.xcconfig`

EN:

- **Purpose:** Platform embedder/config.
- **Contains:** Native runner, manifest, or web shell. Symbols: (library).
- **Does:** Flutter tooling generates most files; app logic stays in `lib/`.
- **Connected to:** Build targets for mobile/desktop/web CI.

RU:

- **Назначение:** Файл проекта.
- **Что внутри:** См. англ. блок Purpose/Contains.
- **Что делает:** Смотри префикс пути для владения.
- **Как связано с общей логикой:** Правила слоёв — `docs/APP_STRUCTURE.md`.


### `macos/Runner/DebugProfile.entitlements`

EN:

- **Purpose:** Platform embedder/config.
- **Contains:** Native runner, manifest, or web shell. Symbols: (library).
- **Does:** Flutter tooling generates most files; app logic stays in `lib/`.
- **Connected to:** Build targets for mobile/desktop/web CI.

RU:

- **Назначение:** Файл проекта.
- **Что внутри:** См. англ. блок Purpose/Contains.
- **Что делает:** Смотри префикс пути для владения.
- **Как связано с общей логикой:** Правила слоёв — `docs/APP_STRUCTURE.md`.


### `macos/Runner/Info.plist`

EN:

- **Purpose:** Platform embedder/config.
- **Contains:** Native runner, manifest, or web shell. Symbols: (library).
- **Does:** Flutter tooling generates most files; app logic stays in `lib/`.
- **Connected to:** Build targets for mobile/desktop/web CI.

RU:

- **Назначение:** Файл проекта.
- **Что внутри:** См. англ. блок Purpose/Contains.
- **Что делает:** Смотри префикс пути для владения.
- **Как связано с общей логикой:** Правила слоёв — `docs/APP_STRUCTURE.md`.


### `macos/Runner/MainFlutterWindow.swift`

EN:

- **Purpose:** Platform embedder/config.
- **Contains:** Native runner, manifest, or web shell. Symbols: (library).
- **Does:** Flutter tooling generates most files; app logic stays in `lib/`.
- **Connected to:** Build targets for mobile/desktop/web CI.

RU:

- **Назначение:** Файл проекта.
- **Что внутри:** См. англ. блок Purpose/Contains.
- **Что делает:** Смотри префикс пути для владения.
- **Как связано с общей логикой:** Правила слоёв — `docs/APP_STRUCTURE.md`.


### `macos/Runner/Release.entitlements`

EN:

- **Purpose:** Platform embedder/config.
- **Contains:** Native runner, manifest, or web shell. Symbols: (library).
- **Does:** Flutter tooling generates most files; app logic stays in `lib/`.
- **Connected to:** Build targets for mobile/desktop/web CI.

RU:

- **Назначение:** Файл проекта.
- **Что внутри:** См. англ. блок Purpose/Contains.
- **Что делает:** Смотри префикс пути для владения.
- **Как связано с общей логикой:** Правила слоёв — `docs/APP_STRUCTURE.md`.


### `macos/RunnerTests/RunnerTests.swift`

EN:

- **Purpose:** Platform embedder/config.
- **Contains:** Native runner, manifest, or web shell. Symbols: (library).
- **Does:** Flutter tooling generates most files; app logic stays in `lib/`.
- **Connected to:** Build targets for mobile/desktop/web CI.

RU:

- **Назначение:** Файл проекта.
- **Что внутри:** См. англ. блок Purpose/Contains.
- **Что делает:** Смотри префикс пути для владения.
- **Как связано с общей логикой:** Правила слоёв — `docs/APP_STRUCTURE.md`.


## .github/

### `.github/copilot-instructions.md`

EN:

- **Purpose:** GitHub Actions workflow.
- **Contains:** CI deploy/build automation. Symbols: (library).
- **Does:** Pushes web to gh-pages; optional Windows installer.
- **Connected to:** Triggered from `main` or manual dispatch.

RU:

- **Назначение:** Файл проекта.
- **Что внутри:** См. англ. блок Purpose/Contains.
- **Что делает:** Смотри префикс пути для владения.
- **Как связано с общей логикой:** Правила слоёв — `docs/APP_STRUCTURE.md`.


### `.github/workflows/deploy.yml`

EN:

- **Purpose:** GitHub Actions workflow.
- **Contains:** CI deploy/build automation. Symbols: (library).
- **Does:** Pushes web to gh-pages; optional Windows installer.
- **Connected to:** Triggered from `main` or manual dispatch.

RU:

- **Назначение:** Файл проекта.
- **Что внутри:** См. англ. блок Purpose/Contains.
- **Что делает:** Смотри префикс пути для владения.
- **Как связано с общей логикой:** Правила слоёв — `docs/APP_STRUCTURE.md`.


### `.github/workflows/windows-desktop-build.yml`

EN:

- **Purpose:** GitHub Actions workflow.
- **Contains:** CI deploy/build automation. Symbols: (library).
- **Does:** Pushes web to gh-pages; optional Windows installer.
- **Connected to:** Triggered from `main` or manual dispatch.

RU:

- **Назначение:** Файл проекта.
- **Что внутри:** См. англ. блок Purpose/Contains.
- **Что делает:** Смотри префикс пути для владения.
- **Как связано с общей логикой:** Правила слоёв — `docs/APP_STRUCTURE.md`.


## pb_hooks/

### `pb_hooks/auth.request_password_reset.pb.js`

EN:

- **Purpose:** Project file.
- **Contains:** Supporting source or config. Symbols: (library).
- **Does:** See path prefix for ownership.
- **Connected to:** Cross-check `docs/APP_STRUCTURE.md` layer rules.

RU:

- **Назначение:** Файл проекта.
- **Что внутри:** См. англ. блок Purpose/Contains.
- **Что делает:** Смотри префикс пути для владения.
- **Как связано с общей логикой:** Правила слоёв — `docs/APP_STRUCTURE.md`.


### `pb_hooks/records.interval_sanitize.pb.js`

EN:

- **Purpose:** Project file.
- **Contains:** Supporting source or config. Symbols: (library).
- **Does:** See path prefix for ownership.
- **Connected to:** Cross-check `docs/APP_STRUCTURE.md` layer rules.

RU:

- **Назначение:** Файл проекта.
- **Что внутри:** См. англ. блок Purpose/Contains.
- **Что делает:** Смотри префикс пути для владения.
- **Как связано с общей логикой:** Правила слоёв — `docs/APP_STRUCTURE.md`.


## installer/

### `installer/windows/build_stt_helper_en.ps1`

EN:

- **Purpose:** Project file.
- **Contains:** Supporting source or config. Symbols: (library).
- **Does:** See path prefix for ownership.
- **Connected to:** Cross-check `docs/APP_STRUCTURE.md` layer rules.

RU:

- **Назначение:** Файл проекта.
- **Что внутри:** См. англ. блок Purpose/Contains.
- **Что делает:** Смотри префикс пути для владения.
- **Как связано с общей логикой:** Правила слоёв — `docs/APP_STRUCTURE.md`.


### `installer/windows/counter.iss`

EN:

- **Purpose:** Project file.
- **Contains:** Supporting source or config. Symbols: (library).
- **Does:** See path prefix for ownership.
- **Connected to:** Cross-check `docs/APP_STRUCTURE.md` layer rules.

RU:

- **Назначение:** Файл проекта.
- **Что внутри:** См. англ. блок Purpose/Contains.
- **Что делает:** Смотри префикс пути для владения.
- **Как связано с общей логикой:** Правила слоёв — `docs/APP_STRUCTURE.md`.


### `installer/windows/install-cpp-atl.ps1`

EN:

- **Purpose:** Project file.
- **Contains:** Supporting source or config. Symbols: (library).
- **Does:** See path prefix for ownership.
- **Connected to:** Cross-check `docs/APP_STRUCTURE.md` layer rules.

RU:

- **Назначение:** Файл проекта.
- **Что внутри:** См. англ. блок Purpose/Contains.
- **Что делает:** Смотри префикс пути для владения.
- **Как связано с общей логикой:** Правила слоёв — `docs/APP_STRUCTURE.md`.


### `installer/windows/prepare_stt_payload.ps1`

EN:

- **Purpose:** Project file.
- **Contains:** Supporting source or config. Symbols: (library).
- **Does:** See path prefix for ownership.
- **Connected to:** Cross-check `docs/APP_STRUCTURE.md` layer rules.

RU:

- **Назначение:** Файл проекта.
- **Что внутри:** См. англ. блок Purpose/Contains.
- **Что делает:** Смотри префикс пути для владения.
- **Как связано с общей логикой:** Правила слоёв — `docs/APP_STRUCTURE.md`.


### `installer/windows/scripts/win_speech_wav.ps1`

EN:

- **Purpose:** Project file.
- **Contains:** Supporting source or config. Symbols: (library).
- **Does:** See path prefix for ownership.
- **Connected to:** Cross-check `docs/APP_STRUCTURE.md` layer rules.

RU:

- **Назначение:** Файл проекта.
- **Что внутри:** См. англ. блок Purpose/Contains.
- **Что делает:** Смотри префикс пути для владения.
- **Как связано с общей логикой:** Правила слоёв — `docs/APP_STRUCTURE.md`.


### `installer/windows/stt_helper_build/counter_stt_helper.exe`

EN:

- **Purpose:** Project file.
- **Contains:** Supporting source or config. Symbols: (library).
- **Does:** See path prefix for ownership.
- **Connected to:** Cross-check `docs/APP_STRUCTURE.md` layer rules.

RU:

- **Назначение:** Файл проекта.
- **Что внутри:** См. англ. блок Purpose/Contains.
- **Что делает:** Смотри префикс пути для владения.
- **Как связано с общей логикой:** Правила слоёв — `docs/APP_STRUCTURE.md`.


## Platform boilerplate (folder-level)

EN: Deeper paths under `android/`, `ios/`, `windows/`, `linux/`, `macos/` are mostly Flutter-generated Gradle/Xcode/CMake runners, Gradle caches, and plugin registrants. They are required for builds but do not contain product business logic. Edit app identifiers, permissions, and icons at the top-level manifest/gradle files listed above.

RU: Глубокие пути в platform-папках — в основном автоген Flutter (Gradle, Xcode, CMake). Бизнес-логика только в `lib/`. Манифесты верхнего уровня — точка правок permissions/icons.
