[package]
name = "wav_stt_replay"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "wav_stt_replay"
path = "src/main.rs"

[dependencies]
hound = "3"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
transcribe-rs = { version = "0.3", features = ["onnx", "ort-directml"] }
