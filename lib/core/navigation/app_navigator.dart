import 'package:flutter/material.dart';

/// Root navigator for desktop overlays (voice widget) when main window is hidden.
final GlobalKey<NavigatorState> appRootNavigatorKey = GlobalKey<NavigatorState>();
