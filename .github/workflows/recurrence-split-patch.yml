name: Temporary recurrence future-split patch

on:
  push:
    branches:
      - fix/recurrence-dialog-alignment

permissions:
  contents: write

jobs:
  patch-recurrence:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Apply recurrence model update
        shell: bash
        run: |
          python - <<'PY'
          from pathlib import Path
          import re

          root = Path('.')

          # 1) Scope model: only single occurrence + this/future are user-supported.
          (root / 'lib/data/recurrence_edit_scope.dart').write_text(r'''/// Recurrence edit/delete scope for Planning recurring tasks.
enum RecurrenceEditScope {
  /// Only the selected occurrence (exception + optional one-off materialize).
  singleOccurrence,

  /// Selected occurrence and every later occurrence. Implemented as a series split
  /// so occurrences before the selected boundary remain historically unchanged.
  thisAndFuture,

  /// Legacy internal value. Never expose in UI and never rewrite historical occurrences.
  @Deprecated('Use thisAndFuture; recurring history is immutable.')
  entireSeries,
}

/// User-supported recurrence mutation scopes.
bool recurrenceEditScopeIsSupported(RecurrenceEditScope scope) =>
    scope != RecurrenceEditScope.entireSeries;

/// Old callers that still pass `entireSeries` get the future-safe behavior.
RecurrenceEditScope canonicalRecurrenceEditScope(RecurrenceEditScope scope) =>
    scope == RecurrenceEditScope.entireSeries
        ? RecurrenceEditScope.thisAndFuture
        : scope;

String _rruleUtcStamp(DateTime value) {
  final u = value.toUtc();
  String two(int v) => v.toString().padLeft(2, '0');
  return '${u.year.toString().padLeft(4, '0')}${two(u.month)}${two(u.day)}T${two(u.hour)}${two(u.minute)}${two(u.second)}Z';
}

/// Returns the same recurrence cadence, ending immediately before [cutoffUtc].
/// COUNT/old UNTIL are replaced by one RFC5545 UNTIL boundary.
String recurrenceRruleEndingBefore(String raw, DateTime cutoffUtc) {
  final source = raw.trim();
  if (source.isEmpty) return source;
  final prefixed = source.toUpperCase().startsWith('RRULE:');
  final body = prefixed ? source.substring(source.indexOf(':') + 1) : source;
  final parts = body
      .split(';')
      .map((e) => e.trim())
      .where((e) =>
          e.isNotEmpty &&
          !e.toUpperCase().startsWith('UNTIL=') &&
          !e.toUpperCase().startsWith('COUNT='))
      .toList();
  final end = cutoffUtc.toUtc().subtract(const Duration(seconds: 1));
  parts.add('UNTIL=${_rruleUtcStamp(end)}');
  return '${prefixed ? 'RRULE:' : ''}${parts.join(';')}';
}

/// Preserves an original COUNT rule after [consumedCount] earlier occurrences.
/// Returns null when no occurrence remains. Rules without COUNT are unchanged.
String? recurrenceRruleWithRemainingCount(String raw, int consumedCount) {
  final source = raw.trim();
  if (source.isEmpty) return source;
  final prefixed = source.toUpperCase().startsWith('RRULE:');
  final body = prefixed ? source.substring(source.indexOf(':') + 1) : source;
  final parts = body.split(';').map((e) => e.trim()).toList();
  final index = parts.indexWhere((e) => e.toUpperCase().startsWith('COUNT='));
  if (index < 0) return source;
  final count = int.tryParse(parts[index].split('=').skip(1).join('='));
  if (count == null) return source;
  final remaining = count - consumedCount;
  if (remaining <= 0) return null;
  parts[index] = 'COUNT=$remaining';
  return '${prefixed ? 'RRULE:' : ''}${parts.join(';')}';
}
''', encoding='utf-8')

          # 2) Two-option, left-aligned dialog.
          (root / 'lib/features/planning/recurrence_scope_dialog.dart').write_text(r'''import 'package:counter/data/models.dart';
import 'package:counter/data/recurrence_edit_scope.dart';
import 'package:counter/l10n/dictionary.dart';
import 'package:flutter/material.dart';

/// Keeps one explicit recurrence-scope decision per edit session and coalesces
/// concurrent autosave/Save requests onto the same dialog.
class RecurrenceEditScopeGate {
  RecurrenceEditScope? selected;
  Future<RecurrenceEditScope?>? _pending;

  bool requiresChoice(bool isRecurring) => isRecurring && selected == null;

  Future<RecurrenceEditScope?> resolve(
    BuildContext context, {
    required PlanningTask task,
    required bool isRecurring,
  }) async {
    if (!isRecurring) return RecurrenceEditScope.singleOccurrence;
    if (selected != null) return selected;
    if (_pending != null) return _pending!;
    final pending = showRecurrenceScopeDialog(
      context,
      task: task,
      isDelete: false,
    );
    _pending = pending;
    final scope = await pending;
    if (identical(_pending, pending)) _pending = null;
    if (scope != null) selected = scope;
    return scope;
  }
}

/// Returns the selected occurrence scope, or null when dismissed.
Future<RecurrenceEditScope?> showRecurrenceScopeDialog(
  BuildContext context, {
  required PlanningTask task,
  required bool isDelete,
}) async {
  final locale = currentLocale.value;
  final titleKey = isDelete
      ? 'plan_recurrence_delete_scope_title'
      : 'plan_recurrence_edit_scope_title';
  final thisKey = isDelete
      ? 'plan_recurrence_delete_this'
      : 'plan_recurrence_edit_this';
  final futureKey = isDelete
      ? 'plan_recurrence_delete_future'
      : 'plan_recurrence_edit_future';

  return showDialog<RecurrenceEditScope>(
    context: context,
    builder: (ctx) => AlertDialog(
      titlePadding: const EdgeInsets.fromLTRB(24, 24, 24, 12),
      contentPadding: const EdgeInsets.fromLTRB(24, 0, 24, 0),
      actionsPadding: const EdgeInsets.fromLTRB(24, 8, 24, 16),
      title: Align(
        alignment: AlignmentDirectional.centerStart,
        child: Text(t(locale, titleKey)),
      ),
      content: Column(
        mainAxisSize: MainAxisSize.min,
        crossAxisAlignment: CrossAxisAlignment.stretch,
        children: [
          ListTile(
            contentPadding: EdgeInsets.zero,
            title: Text(t(locale, thisKey)),
            onTap: () =>
                Navigator.of(ctx).pop(RecurrenceEditScope.singleOccurrence),
          ),
          ListTile(
            contentPadding: EdgeInsets.zero,
            title: Text(t(locale, futureKey)),
            onTap: () =>
                Navigator.of(ctx).pop(RecurrenceEditScope.thisAndFuture),
          ),
        ],
      ),
      actions: [
        TextButton(
          onPressed: () => Navigator.of(ctx).pop(),
          child: Text(t(locale, 'cancel')),
        ),
      ],
    ),
  );
}
''', encoding='utf-8')

          # 3) New focused Brain part for immutable-history series splitting.
          split_path = root / 'lib/data/plans/plan_recurrence_split_helpers.dart'
          split_path.write_text(r'''part of '../database_service.dart';

extension PlanRecurrenceSplitExtension on DatabaseService {
  /// Editor source: a virtual/materialized occurrence borrows recurrence metadata
  /// from its parent series so the Repeat field never falsely shows "Once".
  PlanningTask planningRecurrenceEditSource(PlanningTask task) {
    if (task.rrule?.trim().isNotEmpty == true) return task;
    final parent = _resolveRecurrenceSeriesPocketId(
      planRowId: task.planRowIdForBackend,
      planBusinessId: task.planRowId,
      cached: task,
    );
    if (parent == null) return task;
    return _findCachedPlanningTaskForEdit(parent) ?? task;
  }

  String? _splitRecurrenceDay({
    required String planRowId,
    String? recurrenceInstanceDateKey,
    PlanningTask? cached,
  }) {
    final resolved = _resolveRecurrenceInstanceDateKey(
      planRowId: planRowId,
      recurrenceInstanceDateKey: recurrenceInstanceDateKey,
      cached: cached,
    );
    if (resolved != null) return resolved;
    final dk = cached?.dateKey.trim() ?? '';
    return dk.length >= 10 ? dk.substring(0, 10) : null;
  }

  DateTime? _wallDayForRecurrenceKey(String key) {
    if (key.length < 10) return null;
    final y = int.tryParse(key.substring(0, 4));
    final m = int.tryParse(key.substring(5, 7));
    final d = int.tryParse(key.substring(8, 10));
    if (y == null || m == null || d == null) return null;
    final out = DateTime(y, m, d);
    return out.year == y && out.month == m && out.day == d ? out : null;
  }

  String _wallDateKey(DateTime value) =>
      '${value.year}-${_two(value.month)}-${_two(value.day)}';

  Future<PlanningTask?> _recurrenceSeriesFromServer(String seriesPb) async {
    try {
      final tagCatalog = await _fetchPlanAndListTagCatalog();
      final rec = await _pb
          .collection(PbCollections.plans)
          .getOne(seriesPb, expand: kPbPlanTagsExpand);
      final task = _planningTaskFromPocketRecord(
        rec,
        pocketTagCatalog: tagCatalog,
      );
      return task.rrule?.trim().isNotEmpty == true ? task : null;
    } catch (_) {
      return null;
    }
  }

  int _recurrenceCountBefore(
    PlanningTask parent,
    String rrule,
    DateTime splitStartUtc,
  ) {
    try {
      final instants = _planUtcInstants(parent);
      if (instants == null) return 0;
      final base = instants.startUtc.toUtc();
      if (!base.isBefore(splitStartUtc)) return 0;
      final rule = RecurrenceRule.fromString(
        _normalizeRruleStringForDecoder(rrule),
        options: const RecurrenceRuleFromStringOptions.lenient(),
      );
      return rule
          .getAllInstances(
            start: base,
            after: base.subtract(const Duration(seconds: 1)),
            includeAfter: true,
            before: splitStartUtc,
            includeBefore: false,
          )
          .length;
    } catch (_) {
      return 0;
    }
  }

  Future<String?> _createRecurringSplitRowStrict(PlanningTask task) async {
    if (!_isInitialized || !(currentProfileId?.isNotEmpty ?? false)) return null;
    final catRule = getCategoryRuleById(task.categoryId);
    if (catRule == null || task.title.trim().isEmpty) return null;
    try {
      final Object categoryField;
      final catSystem = _categoryBackendRowIdStrict(catRule);
      if (catSystem != null) {
        categoryField = catSystem;
      } else {
        final catString = _categoryStringPkForApi(catRule);
        categoryField = catString != null && catString.isNotEmpty
            ? catString
            : _recordCategoryBusinessPkForApi(task.categoryId);
      }
      final clientPlanId = DatabaseService._newClientRecordUuid();
      final body = await _buildPocketPlanCreateBody(
        task,
        titleTrimmed: task.title.trim(),
        clientPlanId: clientPlanId,
        categoryFieldForPlan: categoryField,
      );
      final record = await _pb.collection(PbCollections.plans).create(body: body);
      if (task.tags.isNotEmpty) {
        await _syncPlanTagsPocket(record.id, task.tags);
      }
      final tagCatalog = await _fetchPlanAndListTagCatalog();
      final persisted = _planningTaskFromPocketRecord(
        task.tags.isNotEmpty
            ? await _pb
                .collection(PbCollections.plans)
                .getOne(record.id, expand: kPbPlanTagsExpand)
            : record,
        pocketTagCatalog: tagCatalog,
      );
      _upsertPlanInUserCache(persisted);
      return record.id;
    } catch (e, st) {
      DatabaseService._log('RECURRENCE_SPLIT_CREATE: $e');
      DatabaseService._log(st.toString());
      return null;
    }
  }

  Future<List<PlanningTask>> _materializedChildrenFrom(
    String seriesPb,
    String splitDay,
  ) async {
    await _ensureAllPlansUserCacheFresh(force: true);
    return [
      for (final task in _allPlansUserCache)
        if ((task.parentPlanPocketId?.trim() ?? '') == seriesPb &&
            (task.recurrenceInstanceDateKey?.trim().length ?? 0) >= 10 &&
            task.recurrenceInstanceDateKey!.substring(0, 10).compareTo(splitDay) >= 0)
          task,
    ];
  }

  Future<bool> _reparentRecurrenceChildren(
    List<PlanningTask> children,
    String parentPb,
  ) async {
    final changed = <PlanningTask>[];
    try {
      for (final child in children) {
        final id = child.pocketRecordId?.trim() ?? '';
        if (!DatabaseService._isLikelyPocketBaseRowId(id)) continue;
        await _pb.collection(PbCollections.plans).update(
          id,
          body: <String, dynamic>{'parent_plan_id': parentPb},
        );
        changed.add(child);
        _upsertPlanInUserCache(child.copyWith(parentPlanPocketId: parentPb));
      }
      return true;
    } catch (e, st) {
      DatabaseService._log('RECURRENCE_SPLIT_REPARENT: $e');
      DatabaseService._log(st.toString());
      for (final child in changed) {
        final id = child.pocketRecordId?.trim() ?? '';
        final original = child.parentPlanPocketId?.trim() ?? '';
        if (!DatabaseService._isLikelyPocketBaseRowId(id) ||
            !DatabaseService._isLikelyPocketBaseRowId(original)) continue;
        try {
          await _pb.collection(PbCollections.plans).update(
            id,
            body: <String, dynamic>{'parent_plan_id': original},
          );
        } catch (_) {}
      }
      return false;
    }
  }

  Future<bool> _patchMaterializedOccurrenceStrict(
    PlanningTask child, {
    String? title,
    int? categoryId,
    bool? isDone,
    String? notesPlain,
    String? notesDeltaJson,
    List<Map<String, dynamic>>? checklist,
    int? parentPlanId,
    int? order,
    DateTime? startTime,
    DateTime? startTimeDisplay,
    DateTime? endDateTime,
    DateTime? endDateTimeDisplay,
    bool clearEnd = false,
    List<Tag>? tags,
    String? planInitialDateKey,
    bool? planIsPostponed,
    int? planReminderOffset,
  }) async {
    final id = child.pocketRecordId?.trim() ?? '';
    if (!DatabaseService._isLikelyPocketBaseRowId(id)) return false;
    final body = _scalarPatchBodyForPlanningRow(
      planBusinessId: child.planRowId,
      title: title,
      categoryId: categoryId,
      isDone: isDone,
      notesPlain: notesPlain,
      notesDeltaJson: notesDeltaJson,
      checklist: checklist,
      parentPlanId: parentPlanId,
      order: order,
      startTime: startTime,
      startTimeDisplay: startTimeDisplay,
      endDateTime: endDateTime,
      endDateTimeDisplay: endDateTimeDisplay,
      clearEnd: clearEnd,
      planInitialDateKey: planInitialDateKey,
      planIsPostponed: planIsPostponed,
    );
    try {
      if (body.isNotEmpty) {
        await _pb.collection(PbCollections.plans).update(id, body: body);
      }
      if (tags != null) await _syncPlanTagsPocket(id, tags);
      return true;
    } catch (_) {
      return false;
    }
  }

  Future<bool> _truncateRecurringSeriesStrict(
    PlanningTask parent,
    String splitDay,
    String oldRule,
  ) async {
    final id = parent.pocketRecordId?.trim() ?? '';
    if (!DatabaseService._isLikelyPocketBaseRowId(id)) return false;
    final pastExceptions = <String>[
      for (final e in parent.exceptionDates)
        if (e.trim().length >= 10 &&
            e.trim().substring(0, 10).compareTo(splitDay) < 0)
          e.trim().substring(0, 10),
    ]..sort();
    final body = _scalarPatchBodyForPlanningRow(
      planBusinessId: parent.planRowId,
      patchPlanAlarmRecurrence: true,
      planRrule: oldRule,
      planReminderOffset: parent.reminderOffset,
      planExceptionDates: pastExceptions,
    );
    try {
      await _pb.collection(PbCollections.plans).update(id, body: body);
      return true;
    } catch (_) {
      return false;
    }
  }

  Future<bool> _updateRecurringThisAndFuture(
    String planRowId, {
    String? planBusinessId,
    String? title,
    int? categoryId,
    bool? isDone,
    String? notesPlain,
    String? notesDeltaJson,
    List<Map<String, dynamic>>? checklist,
    int? parentPlanId,
    int? order,
    DateTime? startTime,
    DateTime? startTimeDisplay,
    DateTime? endDateTime,
    DateTime? endDateTimeDisplay,
    bool clearEnd = false,
    bool suppressAppSnack = false,
    List<Tag>? tags,
    String? planInitialDateKey,
    bool? planIsPostponed,
    bool patchPlanAlarmRecurrence = false,
    String? planRrule,
    int? planReminderOffset,
    String? recurrenceInstanceDateKey,
  }) async {
    final cached = _findCachedPlanningTaskForEdit(
      planRowId,
      planBusinessId: planBusinessId,
    );
    final seriesPb = _resolveRecurrenceSeriesPocketId(
      planRowId: planRowId,
      planBusinessId: planBusinessId,
      cached: cached,
    );
    final splitDay = _splitRecurrenceDay(
      planRowId: planRowId,
      recurrenceInstanceDateKey: recurrenceInstanceDateKey,
      cached: cached,
    );
    if (seriesPb == null || splitDay == null) {
      if (!suppressAppSnack) AppSnack.failed();
      return false;
    }
    final wallDay = _wallDayForRecurrenceKey(splitDay);
    final parent = await _recurrenceSeriesFromServer(seriesPb);
    if (wallDay == null || parent == null) {
      if (!suppressAppSnack) AppSnack.failed();
      return false;
    }
    final currentRule = parent.rrule!.trim();
    final splitStartUtc = _profileUtcFromWall(wallDay);
    final consumed = _recurrenceCountBefore(parent, currentRule, splitStartUtc);
    final oldRule = recurrenceRruleEndingBefore(currentRule, splitStartUtc);
    final requestedRule = patchPlanAlarmRecurrence
        ? (planRrule ?? '').trim()
        : currentRule;
    final sameRule = _normalizeRruleStringForDecoder(requestedRule) ==
        _normalizeRruleStringForDecoder(currentRule);
    final futureRule = requestedRule.isEmpty
        ? null
        : (sameRule
            ? recurrenceRruleWithRemainingCount(requestedRule, consumed)
            : requestedRule);

    final originalTimes = _materializedWallStartEndForDay(parent, wallDay);
    var futureStart = startTimeDisplay ?? startTime ?? cached?.startTime ?? originalTimes.$1;
    if (futureStart == null) futureStart = wallDay;
    if (_wallDateKey(futureStart).compareTo(splitDay) < 0) {
      futureStart = DateTime(
        wallDay.year,
        wallDay.month,
        wallDay.day,
        futureStart.hour,
        futureStart.minute,
        futureStart.second,
      );
    }
    DateTime? futureEnd;
    if (!clearEnd) {
      futureEnd = endDateTimeDisplay ?? endDateTime ?? cached?.endDateTime;
      if (futureEnd == null && parent.startTime != null && parent.endDateTime != null) {
        final duration = parent.endDateTime!.difference(parent.startTime!);
        if (!duration.isNegative) futureEnd = futureStart.add(duration);
      }
    }
    final futureExceptions = <String>[
      for (final e in parent.exceptionDates)
        if (e.trim().length >= 10 &&
            e.trim().substring(0, 10).compareTo(splitDay) >= 0)
          e.trim().substring(0, 10),
    ]..sort();
    final futureTask = PlanningTask(
      id: 0,
      title: (title ?? cached?.title ?? parent.title).trim(),
      categoryId: categoryId ?? cached?.categoryId ?? parent.categoryId,
      isDone: false,
      dateKey: _wallDateKey(futureStart),
      order: order ?? cached?.order ?? parent.order,
      startTime: futureStart,
      endDateTime: futureEnd,
      checklist: checklist != null
          ? _copyChecklistForMaterialize(checklist)
          : _copyChecklistForMaterialize(cached?.checklist ?? parent.checklist),
      notesPlain: notesPlain ?? cached?.notesPlain ?? parent.notesPlain,
      notesDeltaJson:
          notesDeltaJson ?? cached?.notesDeltaJson ?? parent.notesDeltaJson,
      tags: List<Tag>.from(tags ?? cached?.tags ?? parent.tags),
      initialDateKey: splitDay,
      isPostponed: _wallDateKey(futureStart).compareTo(splitDay) > 0,
      rrule: futureRule,
      exceptionDates: futureRule == null ? const <String>[] : futureExceptions,
      reminderOffset: planReminderOffset ?? cached?.reminderOffset ?? parent.reminderOffset,
    );

    final futureId = await _createRecurringSplitRowStrict(futureTask);
    if (futureId == null) {
      if (!suppressAppSnack) AppSnack.failed();
      return false;
    }
    final futureChildren = await _materializedChildrenFrom(seriesPb, splitDay);
    if (futureRule != null &&
        !await _reparentRecurrenceChildren(futureChildren, futureId)) {
      try {
        await _pb.collection(PbCollections.plans).delete(futureId);
      } catch (_) {}
      if (!suppressAppSnack) AppSnack.failed();
      return false;
    }
    if (!await _truncateRecurringSeriesStrict(parent, splitDay, oldRule)) {
      if (futureRule != null) {
        await _reparentRecurrenceChildren(futureChildren, seriesPb);
      }
      try {
        await _pb.collection(PbCollections.plans).delete(futureId);
      } catch (_) {}
      if (!suppressAppSnack) AppSnack.failed();
      return false;
    }

    if (futureRule == null) {
      for (final child in futureChildren) {
        final id = child.pocketRecordId?.trim() ?? '';
        if (!DatabaseService._isLikelyPocketBaseRowId(id)) continue;
        try {
          await _pb.collection(PbCollections.plans).delete(id);
          _removePlanFromUserCache(id);
        } catch (_) {}
      }
    } else if (cached != null && _isMaterializedRecurrenceException(cached)) {
      final selected = futureChildren.cast<PlanningTask?>().firstWhere(
            (t) => t?.pocketRecordId == cached.pocketRecordId,
            orElse: () => null,
          );
      if (selected != null) {
        await _patchMaterializedOccurrenceStrict(
          selected,
          title: title,
          categoryId: categoryId,
          isDone: isDone,
          notesPlain: notesPlain,
          notesDeltaJson: notesDeltaJson,
          checklist: checklist,
          parentPlanId: parentPlanId,
          order: order,
          startTime: startTime,
          startTimeDisplay: startTimeDisplay,
          endDateTime: endDateTime,
          endDateTimeDisplay: endDateTimeDisplay,
          clearEnd: clearEnd,
          tags: tags,
          planInitialDateKey: planInitialDateKey,
          planIsPostponed: planIsPostponed,
          planReminderOffset: planReminderOffset,
        );
      }
    }

    await _fetchAllPlanningTasksForCurrentUser();
    notifyPlanningRefresh(scheduleNetworkRefresh: false);
    _notifyTimelineAfterRecordCacheMutation();
    if (!suppressAppSnack) AppSnack.updated();
    return true;
  }

  Future<bool> _deleteRecurringThisAndFuture(
    String planRowId, {
    String? planBusinessId,
    String? recurrenceInstanceDateKey,
    bool suppressAppSnack = false,
  }) async {
    final cached = _findCachedPlanningTaskForEdit(
      planRowId,
      planBusinessId: planBusinessId,
    );
    final seriesPb = _resolveRecurrenceSeriesPocketId(
      planRowId: planRowId,
      planBusinessId: planBusinessId,
      cached: cached,
    );
    final splitDay = _splitRecurrenceDay(
      planRowId: planRowId,
      recurrenceInstanceDateKey: recurrenceInstanceDateKey,
      cached: cached,
    );
    if (seriesPb == null || splitDay == null) {
      if (!suppressAppSnack) AppSnack.failed();
      return false;
    }
    final wallDay = _wallDayForRecurrenceKey(splitDay);
    final parent = await _recurrenceSeriesFromServer(seriesPb);
    if (wallDay == null || parent == null) {
      if (!suppressAppSnack) AppSnack.failed();
      return false;
    }
    final oldRule = recurrenceRruleEndingBefore(
      parent.rrule!.trim(),
      _profileUtcFromWall(wallDay),
    );
    if (!await _truncateRecurringSeriesStrict(parent, splitDay, oldRule)) {
      if (!suppressAppSnack) AppSnack.failed();
      return false;
    }
    var ok = true;
    for (final child in await _materializedChildrenFrom(seriesPb, splitDay)) {
      final id = child.pocketRecordId?.trim() ?? '';
      if (!DatabaseService._isLikelyPocketBaseRowId(id)) continue;
      try {
        await _pb.collection(PbCollections.plans).delete(id);
        _removePlanFromUserCache(id);
      } catch (_) {
        ok = false;
      }
    }
    await _fetchAllPlanningTasksForCurrentUser();
    notifyPlanningRefresh(scheduleNetworkRefresh: false);
    _notifyTimelineAfterRecordCacheMutation();
    if (!suppressAppSnack) {
      if (ok) {
        AppSnack.updated();
      } else {
        AppSnack.failed();
      }
    }
    return ok;
  }
}
''', encoding='utf-8')

          # Register the focused Brain part.
          db = root / 'lib/data/database_service.dart'
          text = db.read_text(encoding='utf-8')
          needle = "part 'plans/plan_recurrence_helpers.dart';\n"
          replacement = needle + "part 'plans/plan_recurrence_split_helpers.dart';\n"
          if replacement not in text:
            assert needle in text
            text = text.replace(needle, replacement, 1)
          db.write_text(text, encoding='utf-8')

          # Recurring occurrence editor must display the parent recurrence rule.
          edit = root / 'lib/features/shared/planning_task_edit_sheet.dart'
          text = edit.read_text(encoding='utf-8')
          old = """    _reminderMinutes = widget.task.reminderOffset;\n    _repeatUi = planRepeatUiFromTask(widget.task);\n    _rruleCustomRaw = _repeatUi == PlanRepeatUi.custom\n        ? widget.task.rrule?.trim()\n        : null;\n    _rruleCustomController = TextEditingController(\n      text: _repeatUi == PlanRepeatUi.custom\n          ? (widget.task.rrule?.trim() ?? '')\n          : '',\n    );\n"""
          new = """    final recurrenceSource = DatabaseService.instance.planningRecurrenceEditSource(widget.task);\n    _reminderMinutes = widget.task.reminderOffset ?? recurrenceSource.reminderOffset;\n    _repeatUi = planRepeatUiFromTask(recurrenceSource);\n    _rruleCustomRaw = _repeatUi == PlanRepeatUi.custom ? recurrenceSource.rrule?.trim() : null;\n    _rruleCustomController = TextEditingController(\n      text: _repeatUi == PlanRepeatUi.custom ? (recurrenceSource.rrule?.trim() ?? '') : '',\n    );\n"""
          assert old in text, 'planning edit recurrence init block changed'
          text = text.replace(old, new, 1)
          edit.write_text(text, encoding='utf-8')

          # Replace old unsafe scope handlers with future-safe split delegation.
          helper = root / 'lib/data/plans/plan_recurrence_helpers.dart'
          text = helper.read_text(encoding='utf-8')
          start = text.index('  /// Applies [scope] for recurring plan edits')
          end = text.rfind('\n}')
          assert start > 0 and end > start
          tail = r'''  /// Applies [scope] for recurring plan edits (virtual, materialized, or series row).
  Future<bool> updatePlanningTaskWithRecurrenceScope(
    String planRowId, {
    required RecurrenceEditScope scope,
    String? planBusinessId,
    String? title,
    int? categoryId,
    bool? isDone,
    String? notesPlain,
    String? notesDeltaJson,
    List<Map<String, dynamic>>? checklist,
    int? parentPlanId,
    int? order,
    DateTime? startTime,
    DateTime? startTimeDisplay,
    DateTime? endDateTime,
    DateTime? endDateTimeDisplay,
    bool clearEnd = false,
    bool suppressAppSnack = false,
    List<Tag>? tags,
    String? planInitialDateKey,
    bool? planIsPostponed,
    bool patchPlanAlarmRecurrence = false,
    String? planRrule,
    int? planReminderOffset,
    List<String>? planExceptionDates,
    String? recurrenceInstanceDateKey,
  }) async {
    final effectiveScope = canonicalRecurrenceEditScope(scope);
    if (effectiveScope == RecurrenceEditScope.thisAndFuture) {
      return _updateRecurringThisAndFuture(
        planRowId,
        planBusinessId: planBusinessId,
        title: title,
        categoryId: categoryId,
        isDone: isDone,
        notesPlain: notesPlain,
        notesDeltaJson: notesDeltaJson,
        checklist: checklist,
        parentPlanId: parentPlanId,
        order: order,
        startTime: startTime,
        startTimeDisplay: startTimeDisplay,
        endDateTime: endDateTime,
        endDateTimeDisplay: endDateTimeDisplay,
        clearEnd: clearEnd,
        suppressAppSnack: suppressAppSnack,
        tags: tags,
        planInitialDateKey: planInitialDateKey,
        planIsPostponed: planIsPostponed,
        patchPlanAlarmRecurrence: patchPlanAlarmRecurrence,
        planRrule: planRrule,
        planReminderOffset: planReminderOffset,
        recurrenceInstanceDateKey: recurrenceInstanceDateKey,
      );
    }
    return updatePlanningTask(
      planRowId,
      planBusinessId: planBusinessId,
      title: title,
      categoryId: categoryId,
      isDone: isDone,
      notesPlain: notesPlain,
      notesDeltaJson: notesDeltaJson,
      checklist: checklist,
      parentPlanId: parentPlanId,
      order: order,
      startTime: startTime,
      startTimeDisplay: startTimeDisplay,
      endDateTime: endDateTime,
      endDateTimeDisplay: endDateTimeDisplay,
      clearEnd: clearEnd,
      suppressAppSnack: suppressAppSnack,
      tags: tags,
      planInitialDateKey: planInitialDateKey,
      planIsPostponed: planIsPostponed,
      patchPlanAlarmRecurrence: patchPlanAlarmRecurrence,
      planRrule: planRrule,
      planReminderOffset: planReminderOffset,
      planExceptionDates: planExceptionDates,
      recurrenceInstanceDateKey: recurrenceInstanceDateKey,
    );
  }

  /// Applies [scope] for recurring plan deletes.
  Future<bool> deletePlanningTaskWithRecurrenceScope(
    String planRowId, {
    required RecurrenceEditScope scope,
    String? planBusinessId,
    String? recurrenceInstanceDateKey,
    bool suppressAppSnack = false,
  }) async {
    if (!_isInitialized || !_hasAuthenticatedUserId) return false;
    if (!_isPlansTableConfigured) return false;
    final rid = planRowId.trim();
    if (rid.isEmpty) return false;
    final effectiveScope = canonicalRecurrenceEditScope(scope);
    if (effectiveScope == RecurrenceEditScope.thisAndFuture) {
      return _deleteRecurringThisAndFuture(
        rid,
        planBusinessId: planBusinessId,
        recurrenceInstanceDateKey: recurrenceInstanceDateKey,
        suppressAppSnack: suppressAppSnack,
      );
    }
    final cached = _findCachedPlanningTaskForEdit(
      rid,
      planBusinessId: planBusinessId,
    );
    final virt = _parseVirtualPlanRowId(rid);
    if (virt != null) return deletePlanningTasksBulk([rid]);
    if (_isMaterializedRecurrenceException(
      cached ??
          PlanningTask(
            id: 0,
            title: '',
            categoryId: 0,
            isDone: false,
            dateKey: '',
            order: 0,
          ),
    )) {
      return deletePlanningTasksBulk([rid]);
    }
    final instDay = _resolveRecurrenceInstanceDateKey(
      planRowId: rid,
      recurrenceInstanceDateKey: recurrenceInstanceDateKey,
      cached: cached,
    );
    if (instDay != null && cached?.rrule?.trim().isNotEmpty == true) {
      final seriesPb = cached?.pocketRecordId?.trim();
      if (seriesPb != null &&
          DatabaseService._isLikelyPocketBaseRowId(seriesPb)) {
        return _patchRecurringTemplateExceptionDates(
          parentPlanPocketId: seriesPb,
          instanceDateKey: instDay,
          addException: true,
          suppressAppSnack: suppressAppSnack,
        );
      }
    }
    return deletePlanningTasksBulk([rid]);
  }
'''
          helper.write_text(text[:start] + tail + '\n}', encoding='utf-8')

          # Localized labels: make the remaining second choice explicit.
          for path, replacements in {
            'lib/l10n/langs/ru.dart': {
              "'plan_recurrence_edit_future': 'Это и будущие события'": "'plan_recurrence_edit_future': 'Это и все следующие события'",
              "'plan_recurrence_delete_future': 'Удалить это и будущие'": "'plan_recurrence_delete_future': 'Удалить это и все следующие'",
            },
            'lib/l10n/langs/en.dart': {
              "'plan_recurrence_edit_future': 'This and future events'": "'plan_recurrence_edit_future': 'This and all following events'",
              "'plan_recurrence_delete_future': 'Delete this and future events'": "'plan_recurrence_delete_future': 'Delete this and all following events'",
            },
          }.items():
            p = root / path
            body = p.read_text(encoding='utf-8')
            for old_s, new_s in replacements.items():
              if old_s in body:
                body = body.replace(old_s, new_s, 1)
            p.write_text(body, encoding='utf-8')

          # Focused recurrence tests.
          test = root / 'test/plan_recurrence_scope_test.dart'
          text = test.read_text(encoding='utf-8')
          text = re.sub(
            r"  group\('recurrence scope model', \(\) \{.*?\n  \}\);\n\n  group\('recurrence scope APIs', \(\) \{.*?\n  \}\);",
            r'''  group('recurrence scope model', () {
    test('only occurrence and this/future are user-supported', () {
      expect(
        recurrenceEditScopeIsSupported(RecurrenceEditScope.singleOccurrence),
        isTrue,
      );
      expect(
        recurrenceEditScopeIsSupported(RecurrenceEditScope.thisAndFuture),
        isTrue,
      );
      expect(
        recurrenceEditScopeIsSupported(RecurrenceEditScope.entireSeries),
        isFalse,
      );
      expect(
        canonicalRecurrenceEditScope(RecurrenceEditScope.entireSeries),
        RecurrenceEditScope.thisAndFuture,
      );
    });
  });

  group('recurrence split RRULE helpers', () {
    test('historical rule ends immediately before split boundary', () {
      final out = recurrenceRruleEndingBefore(
        'FREQ=WEEKLY;BYDAY=MO;COUNT=20',
        DateTime.utc(2026, 9, 28),
      );
      expect(out, contains('FREQ=WEEKLY'));
      expect(out, contains('BYDAY=MO'));
      expect(out, contains('UNTIL=20260927T235959Z'));
      expect(out, isNot(contains('COUNT=')));
    });

    test('COUNT is reduced only by already consumed occurrences', () {
      expect(
        recurrenceRruleWithRemainingCount('FREQ=DAILY;COUNT=10', 3),
        'FREQ=DAILY;COUNT=7',
      );
      expect(
        recurrenceRruleWithRemainingCount('FREQ=DAILY;COUNT=3', 3),
        isNull,
      );
      expect(
        recurrenceRruleWithRemainingCount('FREQ=DAILY', 500),
        'FREQ=DAILY',
      );
    });
  });''',
            text,
            flags=re.S,
            count=1,
          )
          test.write_text(text, encoding='utf-8')

          # Governing UX contract: past is immutable; two choices only.
          ux = root / 'docs/UX_CONTRACT.md'
          text = ux.read_text(encoding='utf-8')
          old = """- **Scope dialog (EN/RU):** Editing or deleting a recurring/virtual/materialized occurrence prompts:\n  - **This event** — single occurrence only (`exception_dates` + materialized one-off on edit; exception only on delete for virtual rows).\n  - **This and following events** — disabled until safe RRULE split-series (UNTIL boundary) ships.\n  - **All events in the series** — PATCH/DELETE the stored series row only (15-char PocketBase `id`; never `virt-*`).\n- **Dedupe:** Materialized occurrence for `{seriesPb, instanceDate}` suppresses the matching virtual card. Virtual rows must never enter persistent caches or REST PATCH/DELETE paths.\n"""
          new = """- **Historical immutability:** A recurring edit/delete must never rewrite earlier occurrences. Past recurrence history remains attached to the old series definition.\n- **Scope dialog (EN/RU):** Editing or deleting a recurring/virtual/materialized occurrence has exactly two choices:\n  - **This event** — single occurrence only (`exception_dates` + materialized one-off on edit; exception only on delete for virtual rows).\n  - **This and all following events** — split at the selected occurrence. The historical series gets an `UNTIL` boundary immediately before that day; edits create a new future series from the selected occurrence, while delete stops the series at that boundary. Existing materialized future exceptions move with the future series.\n- **No “all series” mutation:** There is no user action that retroactively changes/deletes earlier occurrences. Legacy `entireSeries` callers canonicalize to the future-safe split behavior.\n- **Dedupe:** Materialized occurrence for `{seriesPb, instanceDate}` suppresses the matching virtual card. Virtual rows must never enter persistent caches or REST PATCH/DELETE paths.\n"""
          assert old in text, 'UX recurrence block changed'
          ux.write_text(text.replace(old, new, 1), encoding='utf-8')

          arch = root / 'docs/ARCHITECTURE.md'
          text = arch.read_text(encoding='utf-8')
          needle = '- **OWNERSHIP:** Every query filters by current user, e.g. `user_id = "<uuid>"` in PB filter strings.\n'
          law = '- **RECURRENCE_HISTORY_IMMUTABILITY:** Recurring-plan bulk edits/deletes never rewrite occurrences before the selected boundary. “This and following” is implemented by splitting recurrence history at that occurrence; there is no user-facing whole-series retroactive mutation.\n'
          assert needle in text
          if law not in text:
            text = text.replace(needle, needle + law, 1)
          arch.write_text(text, encoding='utf-8')

          # Canonical technical encyclopedia + human guide requested for LIFE OS.
          (root / 'docs/FUNCTIONS.md').write_text(r'''# LIFE OS Function Encyclopedia

Canonical product-behavior reference for AI developers and maintainers. Code and this document must stay synchronized. User-facing explanations live in `docs/USER_GUIDE.md`.

## planning.recurring_plans — Recurring plans

**Status:** implemented  
**Owner:** Planning / Brain  
**Persistent collection:** `plans`  
**Primary code:** `lib/data/plans/plan_recurrence_helpers.dart`, `lib/data/plans/plan_recurrence_split_helpers.dart`, `lib/data/recurrence_edit_scope.dart`, `lib/features/planning/recurrence_scope_dialog.dart`

### Storage and expansion

- One PocketBase `plans` row with `rrule` is a recurrence series.
- Ordinary occurrences are generated just-in-time for the visible window; they are not persisted as individual rows.
- Virtual IDs are `virt-{seriesPocketId}-{YYYY-MM-DD}` and must never be used as PocketBase REST IDs.
- `exception_dates` suppresses specific generated occurrences.
- A separately edited/completed occurrence is materialized as a real `plans` row linked by `parent_plan_id` + `recurrence_instance_date_key`.
- A materialized occurrence suppresses the matching virtual occurrence.

### Edit/delete scope

The product exposes exactly two scopes:

1. **This event** — one occurrence only. Edit materializes it; delete adds/keeps the occurrence exception.
2. **This and all following events** — the recurrence is split at the selected occurrence. Earlier occurrences keep the old series definition. A future edit creates a new series; a future delete ends the old series immediately before the selected occurrence.

There is no user-facing “all series” operation because it would retroactively rewrite history. The legacy `entireSeries` enum value canonicalizes to `thisAndFuture` for compatibility.

### Split invariants

- Historical series fields (title, category, notes, schedule, recurrence cadence) are never patched by a future edit; only its RRULE boundary/old exceptions are adjusted.
- The historical RRULE ends with `UNTIL` immediately before the split wall day.
- If the unchanged original RRULE used `COUNT`, the new future series receives only the remaining count.
- Existing materialized occurrences on/after the split boundary are re-parented to the new future series so their exception semantics remain valid.
- Existing materialized occurrences before the boundary remain linked to the historical series.
- Editing one occurrence never completes/deletes/renames the recurrence series.
- Completing one virtual occurrence materializes only that occurrence as done; the recurrence continues.

### Time

- Recurrence is evaluated in profile wall-clock semantics.
- Persistent timestamps remain UTC.
- Split boundaries use the selected occurrence’s profile-wall date, converted to UTC only for RFC5545 `UNTIL`.

### Documentation synchronization rule

Any change to recurrence behavior must update, in the same change set:

- `docs/FUNCTIONS.md`
- `docs/USER_GUIDE.md`
- `docs/UX_CONTRACT.md` when user-visible behavior changes
- `docs/ARCHITECTURE.md` when an invariant changes
- `docs/DATA_MAP.md` / `docs/POCKETBASE_MANIFEST.md` if persistence changes
- `docs/APP_STRUCTURE.md` / detailed structure if ownership or files change
- `CHANGELOG.md` when shipped

If code and documentation disagree, treat it as a defect and report the discrepancy rather than silently choosing one.
''', encoding='utf-8')

          (root / 'docs/USER_GUIDE.md').write_text(r'''# LIFE OS — User Guide

Human-readable instructions for LIFE OS. Technical implementation details are in `docs/FUNCTIONS.md`.

## Recurring plans

A recurring plan is one plan that LIFE OS automatically shows again according to its repeat rule — for example, a workout every Monday.

### Editing a recurring plan

When you change a recurring occurrence, LIFE OS asks where the change should apply:

- **Only this event** — changes only the occurrence you opened. The rest of the recurring plan stays unchanged.
- **This and all following events** — changes the occurrence you opened and every later occurrence. Earlier occurrences stay exactly as they were.

There is intentionally no “Entire series” option: LIFE OS does not rewrite earlier recurrence history when you change what should happen from now on.

**Example:** “Gym” repeats every Monday. You open September 28 and rename it to “Swimming”.

- **Only this event:** September 28 becomes “Swimming”; October 5 and later are still “Gym”.
- **This and all following events:** September 28, October 5, October 12 and later become “Swimming”; Mondays before September 28 remain “Gym”.

### Deleting a recurring plan

The same two choices apply:

- **Delete only this event** — skips only the selected occurrence.
- **Delete this and all following events** — removes the selected occurrence and stops the recurrence from that point onward. Earlier occurrences remain in history.

### Completing one occurrence

Marking one occurrence complete does not complete the whole recurrence. LIFE OS records that occurrence as completed and keeps generating later occurrences.

### Individually changed occurrences

An occurrence that you edited separately remains its own exception. It does not get silently overwritten by later recurrence changes that start after it unless it lies inside the selected future range, in which case it stays attached to that future recurrence as an exception.
''', encoding='utf-8')

          pack = root / 'docs/PROJECT_KNOWLEDGE_PACK.md'
          text = pack.read_text(encoding='utf-8')
          text = text.replace('**Pack count:** 14 documents (≤25 limit)', '**Pack count:** 16 documents (≤25 limit)')
          text = text.replace('## Upload these 14 files', '## Upload these 16 files')
          old_rows = """| 14 | `docs/reports/FINAL_STRUCTURE_PARITY_AND_DOC_CLEANUP_2026-07-03.md` | Structure parity proof, removed-docs log |\n"""
          new_rows = """| 14 | `docs/reports/FINAL_STRUCTURE_PARITY_AND_DOC_CLEANUP_2026-07-03.md` | Structure parity proof, removed-docs log |\n| 15 | `docs/FUNCTIONS.md` | Canonical product-function behavior for AI/developers |\n| 16 | `docs/USER_GUIDE.md` | Human-readable product instructions |\n"""
          assert old_rows in text
          text = text.replace(old_rows, new_rows, 1)
          text = text.replace('One source of truth per topic in the 14-doc pack', 'One source of truth per topic in the 16-doc pack')
          pack.write_text(text, encoding='utf-8')

          # Concise structure manifest must know about the new Brain part.
          app = root / 'docs/APP_STRUCTURE.md'
          text = app.read_text(encoding='utf-8')
          needle = '| `plans/plan_recurrence_helpers.dart` | RRULE JIT expansion, virtual/materialized occurrence identity, exception-date mutation, concrete instance materialization, and recurrence edit/delete scope *(part)* |\n'
          row = '| `plans/plan_recurrence_split_helpers.dart` | Future-safe recurring-series split, historical immutability, future exception re-parenting, and split delete semantics *(part)* |\n'
          assert needle in text
          if row not in text:
            text = text.replace(needle, needle + row, 1)
          app.write_text(text, encoding='utf-8')
          PY

      - name: Remove temporary workflow and regenerate detailed manifest
        shell: bash
        run: |
          rm .github/workflows/recurrence-split-patch.yml
          git add -A .github/workflows/recurrence-split-patch.yml
          python scripts/manual/generate_app_structure_detailed.py

      - name: Commit generated recurrence update
        shell: bash
        run: |
          git config user.name "life-os-recurrence-bot"
          git config user.email "actions@users.noreply.github.com"
          git add -A
          git commit -m "Make recurring history immutable"
          git push origin HEAD:fix/recurrence-dialog-alignment
