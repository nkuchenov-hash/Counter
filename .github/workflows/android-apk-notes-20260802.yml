name: Android APK Notes build

on:
  pull_request:
    branches:
      - main

permissions:
  contents: read

jobs:
  build-apk:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout build branch
        uses: actions/checkout@v4

      - name: Set up Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Flutter 3.41.6
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.41.6'
          channel: stable
          cache: true

      - name: Create env.dart
        run: |
          mkdir -p lib/core/env
          printf '%s\n' 'abstract final class Env {' '  static const String placeholder = '\''\'';' '}' > lib/core/env/env.dart

      - name: Install dependencies
        run: flutter pub get

      - name: Build release APK
        run: |
          BUILD_TIME=$(date -u +%Y-%m-%dT%H:%M:%SZ)
          flutter build apk --release --no-tree-shake-icons \
            --dart-define=GIT_COMMIT="$GITHUB_SHA" \
            --dart-define=BUILD_TIME="$BUILD_TIME"
          cp build/app/outputs/flutter-apk/app-release.apk LifeOS-Android-notes-20260802.apk

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: LifeOS-Android-notes-20260802
          path: LifeOS-Android-notes-20260802.apk
          if-no-files-found: error
          retention-days: 30
