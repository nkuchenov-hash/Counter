name: Apply live sync fix v2

on:
  push:
    branches:
      - fix/live-sync-push-20260805
    paths:
      - .github/workflows/apply-live-sync-fix-v2.yml

permissions:
  contents: write

jobs:
  apply-and-verify:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - name: Checkout branch
        uses: actions/checkout@v4
        with:
          ref: fix/live-sync-push-20260805
          fetch-depth: 0

      - name: Set up Flutter 3.41.6
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.41.6'
          channel: stable
          cache: true

      - name: Repair patch parser
        run: python .github/scripts/repair_live_sync_patch_parser.py

      - name: Apply surgical sync patch
        run: python .github/scripts/apply_live_sync_fix.py

      - name: Format changed Dart files
        run: |
          dart format \
            lib/data/database_service.dart \
            lib/data/db_core.dart \
            lib/data/plan_service.dart \
            lib/data/local_sync/plan_mutation_outbox.dart \
            lib/data/plans/plan_outbox_helpers.dart \
            lib/data/realtime/catalog_realtime.dart \
            lib/features/shared/planning_task_edit_sheet.dart \
            test/live_sync_contract_test.dart

      - name: Resolve packages
        run: flutter pub get

      - name: Analyze
        run: flutter analyze --no-fatal-infos --no-fatal-warnings

      - name: Run three focused sync tests
        run: flutter test test/live_sync_contract_test.dart

      - name: Build release web
        run: flutter build web --release --base-href="/Counter/" --no-tree-shake-icons --no-wasm-dry-run --pwa-strategy=none

      - name: Build Android arm64 APK
        run: flutter build apk --release --target-platform android-arm64 --split-per-abi --no-tree-shake-icons

      - name: Validate diff
        run: git diff --check

      - name: Commit verified source changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add \
            CHANGELOG.md \
            lib/data/database_service.dart \
            lib/data/db_core.dart \
            lib/data/plan_service.dart \
            lib/data/local_sync/plan_mutation_outbox.dart \
            lib/data/plans/plan_outbox_helpers.dart \
            lib/data/realtime/catalog_realtime.dart \
            lib/features/shared/planning_task_edit_sheet.dart \
            test/live_sync_contract_test.dart
          git commit -m "Fix push-first multi-device sync"
          git push origin HEAD:fix/live-sync-push-20260805

      - name: Upload Android APK
        uses: actions/upload-artifact@v4
        with:
          name: counter-live-sync-arm64
          path: build/app/outputs/flutter-apk/app-arm64-v8a-release.apk
          if-no-files-found: error
