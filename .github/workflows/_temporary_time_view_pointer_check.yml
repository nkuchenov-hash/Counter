name: Temporary Time View pointer routing check

on:
  pull_request:
    branches:
      - main
    paths:
      - 'lib/features/planning/time_view/time_view_interaction_block.dart'
      - 'test/time_view_resize_handle_test.dart'
      - '.github/workflows/_temporary_time_view_pointer_check.yml'

permissions:
  contents: read

jobs:
  focused-check:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Flutter 3.41.6
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.41.6'
          channel: stable

      - name: Create env.dart
        run: |
          mkdir -p lib/core/env
          printf '%s\n' 'abstract final class Env {' '  static const String placeholder = '\'''\'';' '}' > lib/core/env/env.dart

      - name: Install dependencies
        run: flutter pub get

      - name: Analyze
        run: flutter analyze --no-fatal-infos --no-fatal-warnings

      - name: Run three focused pointer tests
        run: flutter test test/time_view_resize_handle_test.dart
