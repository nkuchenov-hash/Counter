name: Sleep foreground current-main finalize

on:
  pull_request:
    branches: [main]

permissions:
  contents: write

jobs:
  finalize:
    if: github.head_ref == 'refactor/sleep-foreground-lifecycle-20260818'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: refactor/sleep-foreground-lifecycle-20260818
      - name: Apply wiring and docs parity
        shell: bash
        run: |
          python scripts/manual/_finalize_sleep_foreground_current_main.py
          git rm scripts/manual/_finalize_sleep_foreground_current_main.py
          git diff --check
      - name: Architecture
        shell: pwsh
        run: ./scripts/audit/architecture_guard.ps1 -Strict
      - name: Commit parity
        shell: bash
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -A
          git commit -m 'Restore sleep foreground ownership on current main'
          git push origin HEAD:refactor/sleep-foreground-lifecycle-20260818
